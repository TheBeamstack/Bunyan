# brahim-orchestrator.md — moved

**Canonical: `diwan/docs/prompts/brahim-orchestrator.md`.** The box orchestrator loop protocol is org-wide and lives in one place; this repo keeps no copy.

- Resolve it as `$DIWAN/docs/prompts/brahim-orchestrator.md`, or `../diwan/docs/prompts/brahim-orchestrator.md` if `$DIWAN` is unset.
- This repo's own paths, gate commands and git identity are in `.agent.toml` at the repo root.
- Product invariants and owner-gated classes specific to this repo are in `docs/INVARIANTS.md`.

**Do not re-add content here.** A second copy is the defect this stub exists to close: two copies of one
protocol drifted 206 lines apart, one missing the fix for a hazard that had already happened, and
nothing reported it. Edit the canonical file instead.
