{
  "name": "@bunyan/kernel-mock",
  "version": "0.0.0",
  "private": true,
  "description": "Protocol-conformant fake kernel (no OCCT). Unblocks the P4 shell before the real kernel lands.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./worker": "./src/worker.ts"
  },
  "scripts": {
    "typecheck": "tsc --build"
  },
  "dependencies": {
    "@bunyan/kernel-core": "workspace:*",
    "@bunyan/protocol": "workspace:*"
  }
}
