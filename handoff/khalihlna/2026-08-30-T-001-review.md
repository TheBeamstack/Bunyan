# T-001 — review — 2026-08-30 — seat: khalihlna

**PR:** #43 · **Branch:** `task/T-001-the-perpendicular-foot-snap-candidate` · **Risk:** additive

## 1. Item 1 — revert-verification, browser claim re-executed

The PR's own browser-verification claim was inherited from a prior, unmerged 2026-08-17 session and
never re-run on the merged commit. `machine: pc` PRs get no `unverified here:` exception (`REVIEW.md`),
so this was re-executed for real here, not read.

No `chromium-cli`/Playwright/MCP browser tool was preinstalled in this session despite a real cached
Chromium existing (`BUNYAN_BROWSER_CMD` → `ms-playwright/chromium-1223/chrome-win64/chrome.exe`, from
an earlier session's `playwright install`). Installed `playwright-core` into a scratch dir, launched the
dev server on port 5999 (Windows reserves 5121–5220 via `netsh interface ipv4 show excludedportrange`,
which is why 5173/5185 both `EACCES`'d), and wrote a driver script that:

- computes the exact screen projection from `Viewport.ts`'s own hardcoded camera (`PerspectiveCamera(50,
  aspect, 10, 500000)`, position `(6000,5000,8000)`, `OrbitControls.target (2000,500,1400)`, Z-up) —
  standard right-handed lookAt + WebGL perspective math, no `three` dependency needed;
- activates the Wall tool, places an anchor at world `(1450,1500,0)`, hovers/clicks at the analytic
  perpendicular foot of that anchor onto Wall 1's baseline edge `(0,0,0)–(4000,0,0)`, world
  `(1450,0,0)` — chosen off Wall1's midpoint (2000) and off every 1000mm grid intersection so no
  higher-`SNAP_PRIORITY` candidate (`endpoint`/`intersection`/`midpoint`/`grid`) can pre-empt it;
- reads the committed wall back via `window.bunyan.query({kind:'elements'})`.

**RED:** `git checkout main -- apps/web/src/tool/align.ts apps/web/src/render/Viewport.ts
apps/web/src/render/ViewportCanvas.tsx` (reverting only the feature, on the PR's own tree), fresh page
load, identical click sequence → committed wall `end: [1450.0001385951882, -0.00017842620127339615]`.
`end.y≈0` — the `'extension'` (guide-through-reference) candidate, the only mechanism left once
`perpendicularFeet` is gone, since the reference edge and gesture anchor here are both axis-aligned
(the same coincidence Entry 84 documents for this demo geometry).

**GREEN:** `git checkout HEAD -- <same 3 files>`, fresh page load, identical click sequence, run twice →
committed wall `end: [1450.000138595213, 52.5]` both times, byte-identical. `end.x` matches `start.x` to
12 significant figures — the perpendicular signature — and `end.y=52.5` is a real, edge-derived value no
guide mechanism can produce (a guide through the anchor or through Wall1's own endpoint/midpoint would
land at `y≈0`, `y=1500`, or an x/y matching one of those references, never `52.5`). `SnapKind`'s ruled
priority (`perpendicular`=rank 4 strictly above `extension`=rank 5, `snap.ts`) is what makes this
decisive rather than a coincidence: whenever a perpendicular candidate is within tolerance it wins over
the coincident guide, by construction.

Console errors: one `404` on every boot (present even before any interaction, at the very first
`window.bunyan.query` read) — pre-existing, unrelated to this diff.

## 2. Items 2–7

- **Spec sections:** `docs/design/P4.5_interaction_model_design.md` §4.3, Q3's priority order — read
  against the diff, consistent.
- **Backward sweep:** N/A — `'perpendicular'` was already declared in `SnapKind` and already ranked by
  Q3; this PR is the first *producer*, not a new rule or invariant. Nothing to sweep.
- **New kind of thing:** none — reuses the existing `SnapCandidate`/guide pattern from Entry 84.
- **Claims vs code:** the handoff body's description of `referenceEdges` (pairs `'endpoint'` candidates
  by shared `ref`, never proximity) and `perpendicularFeet` (unclamped, degeneracy-guarded) checked
  against `apps/web/src/tool/align.ts` — accurate.
- **Numbers:** the 974/974 test count and 343.91s duration quoted in the PR's own §7 entry carry a
  method (a full `pnpm verify` run); the browser measurements above carry theirs.
- **Weak green:** `align.test.ts`'s 12 new tests were already revert-verified by the author (the
  degeneracy guard); not independently re-verified here — time went to the browser claim, the actual
  gap this review exists to close.
- **Risk:** confirmed `additive` — `pnpm state` output, `tests/freeze-boundary.test.ts` green (19/19)
  inside the full run, no `needs-operator/*` label (`gh pr view 43 --json labels` → `[]`).

## 3. `pnpm verify`

Two full runs. First: 1 failure —
`tests/protocol/agent-start.test.ts > ... the SAME seat, from a different clone, RESUMES its own open
claim rather than duplicating it`, `Test timed out in 30000ms`. Re-ran that one test alone: 14.3s, green
— a full-suite subprocess-contention flake, the same documented class `vitest.config.ts`'s own header
already names for this file, not a regression. Second full run: **99 files / 974 tests green**
(496.87s), `docs:check` 8 files/166 tests green.

## 4. What this leaves

`brahim`: T-021's own review (PR #42, merged 2026-08-28) never got a `khalihlna` §7 abstract or handoff
body — missed at the time, not fixed retroactively here (a fabricated date would be worse than the gap;
invariant 10). Its abstract's `REVIEW:` line still reads "pending"; corrected in this same commit to name
the actual outcome, without rewriting anything else in that entry.

Nothing else owed — every claim in this review was executed and measured on this exact machine.
