# T-003 — review

## 1. What this review was

`AGENTS.md §1.2`/`REVIEW.md`, one turn (`risk: normal`, not `risk: high`) — PR #46, `apps/web/src/ui/
LicensesScreen.tsx` + `licenseTexts.ts`, the in-app open-source licences screen.

## 2. Item 1 — revert-verification, re-executed here (mandatory)

**Headless half.** Edited `apps/web/src/ui/licenseTexts.ts` on the branch, replacing
`export const NOTICE_TEXT: string = notice;` with a stub string. Ran
`vitest run src/ui/licenseTexts.test.ts` from `apps/web`:

```
✗ NOTICE_TEXT is byte-identical to the repo-root NOTICE file
   Test Files  1 failed (1)
        Tests  1 failed | 2 passed (3)
```

Restored the line verbatim, re-ran: `3 passed (3)`; `git status --short` on the file came back empty —
byte-identical to the committed version.

**Browser half — re-executed for real, not inherited (this seat carries no `unverified here:` exception).**
No project run-skill exists for this repo. Installed `playwright-core` into a scratch dir (no preinstalled
driver was present this session), started the real dev server (`vite --port 5300` from `apps/web`, invoked
directly since `pnpm --filter @bunyan/web dev -- --port ...` doesn't pass the arg through — same finding
T-003's own handoff body records) and drove `BUNYAN_BROWSER_CMD`'s Chromium
(`ms-playwright/chromium-1223/chrome-win64/chrome.exe`) against it.

⚠ **One environment finding not in the builder's body:** the dev server bound to `[::1]:5300`
(IPv6 loopback) only — `http://127.0.0.1:5300/` connection-refused, `http://localhost:5300/` worked. Worth
carrying forward for the next session driving this box.

Method: booted the app, waited for `Kernel:` in the header (real OCCT boot, not stubbed), clicked
"Licences", read the dialog back via Playwright locators, then closed it.

- `role=dialog[name="Open-source licences"]` visible; `NOTICE` heading present; full text present
  (73,595 characters), mentions OCCT ("Open CASCADE").
- All 10 `.license-section h4` headings present, and their text matches `ls licenses/` on disk exactly,
  byte for byte in file name: `fflate-LICENSE.txt`, `js-tokens-LICENSE.txt`, `loose-envify-LICENSE.txt`,
  `OCCT_LGPL_EXCEPTION.txt`, `OCCT-LICENSE_LGPL_21.txt`, `planegcs-LICENSE.txt`, `react-LICENSE.txt`,
  `react-dom-LICENSE.txt`, `scheduler-LICENSE.txt`, `three-LICENSE.txt`.
- Console errors identical before opening the screen and after: one pre-existing
  `Failed to load resource: 404`, the same single error T-002's own review reproduced on clean `main` —
  no new console error from this screen.
- Close button dismisses the dialog (`isVisible()` false afterward).

## 3. Spec / done-when check (`docs/BACKLOG.md` T-003)

- "a reachable screen in `apps/web` renders `NOTICE` and the `licenses/` texts" — confirmed above, real
  browser.
- "OCCT's text is present, not summarised" — confirmed: both `OCCT_LGPL_EXCEPTION.txt` and
  `OCCT-LICENSE_LGPL_21.txt` render as full sections, sourced verbatim via `?raw` import
  (`licenseTexts.test.ts` proves byte-identity headlessly, for every file including these two).
- "console-error-free boot with the screen open" — one pre-existing, branch-unrelated 404 persists
  (documented, reproduces on clean `main` per T-002's review); no *new* error. Read literally
  ("console-error-free") this is not fully clean, but it is the same state as `main` and every other
  PR this week — not a regression this PR introduces, so not blocking (`REVIEW.md`: "you cannot prove it
  ⇒ opinion, never block on it" — and here there is nothing to prove differently).
- "touches nothing in `CLA.md`" — `git diff main...HEAD --name-only` confirms; `CLA.md` absent from the
  diff.

## 4. Other checklist items

- **Backward sweep / new kind of thing:** N/A — no new rule, invariant, entity or collection. Pure
  additive UI reading two existing repo-root artifacts.
- **Claims vs code:** cross-checked the entry's claim about T-002's `REVIEW:` line correction
  (`gh pr view 45` → `MERGED`, `mergedBy: Davidian-Abdo`) — real, not fabricated. Confirmed the "8 files"
  vs working-tree "9 files" figures reconcile: `fe9f5db` (code+docs, 8 files) vs `9d81ab7`
  (`agent-finish.mjs`'s own status-flip commit, 2 files) — no discrepancy.
- **Numbers:** `pnpm verify` re-run in full, locally, this session — **exit 0, 100 files / 987 tests**,
  `docs:check` **8 files / 166 tests** — matches the entry's quoted figures exactly.
- **Weak green:** `licenseTexts.test.ts`'s three assertions are direct `readFileSync` byte comparisons
  plus a non-vacuous length guard against an empty glob. No path to a false-titled green found.
- **Risk:** `RISK: additive` confirmed independently — `tests/freeze-boundary.test.ts` 19/19 green inside
  the full run, CI's `PR shape · reserved classes` job passed with **zero labels** (confirmed distinct from
  "the labeller never executed" — the job itself shows SUCCESS), `typecheck · lint · geometry harness`
  green. No `needs-operator/*`.

## 5. Verdict

**APPROVE and MERGE**, on `Davidian-Abdo` — PR #46, merge commit `ec21a92`.

## 6. What this leaves

Nothing owed to a `pc` seat — every claim in this review, including the browser half, was executed on this
exact machine; no `unverified here:` debt created. `brahim`/`zayd` still owe `BUNYAN_PNPM_CMD` documentation
per T-003's own `OWES:` — already landed on `main` directly (`af8613f`, ahead of this review) by the time
this turn ran `pnpm verify`, so nothing further to chase there.
