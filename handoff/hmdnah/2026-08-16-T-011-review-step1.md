# T-011 review (step 1 of 2) — the CRUD closes the measured defect exactly as claimed

**Seat:** `hmdnah` (reviewer, box — headless). **Date:** 2026-08-16. **PR:** #32, author seat `zayd`.
**Decisions read:** D85 (Q17a, the ruling), D65, D67, D68, D79, D86, D88 (the two-step review itself).

⚠ **Step 1 of 2 (D88), mechanical only: `REVIEW.md` items 1, 4, 5, 7.** NOT approved and NOT merged — the
row stays `review`. Step 2 (items 2, 3, 6, adversarial) is a separate session on this same claim. This PR
is also `RISK: contract-touching` (`SceneCollection` gains `'designOptions'`), so it needs the owner's
merge regardless of how the two review steps land.

## 1. Verdict

No defect found within a step-1 pass. Every claim checked against the code held; the one revert-verified
claim reproduced exactly, numerically.

## 2. Item 1 — revert-verification, re-executed

Reverted the five touched `packages/document/src/*.ts` files to `main`'s committed version
(`git checkout main -- commands.ts dependency.ts bnn.ts scene.ts designoptions.ts`), keeping
`tests/design-option-crud.test.ts` as committed on the branch:

```
Test Files  1 failed (1)
     Tests  12 failed | 1 passed (13)
```

Every failure: `CommandFailure: unknown command "core.createDesignOption"`. The one surviving test is the
§6 RED-baseline case, which asserts the *unchanged* defect (a tag naming an id nothing minted) and is not
supposed to move — and did not.

Restored (`git checkout HEAD -- <same 5 files>`), working tree clean, then re-ran both files:

```
Test Files  2 passed (2)
     Tests  21 passed (21)
```

Matches the entry's "12 of 13 RED … 13/13 restored … 21/21 across both files" exactly.

## 3. Item 4 — claims vs code

- **"0 of 40 commands could author a design option."** `git show main:packages/document/src/commands.ts`
  — `CORE_COMMANDS` is exactly 40 entries pre-PR. Confirmed.
- **"No `emptyScene()` entry, materialises on first authoring, no `SCENE_SCHEMA_VERSION` bump."**
  Confirmed: `emptyScene()` carries no `designOptions` key; `applyOne` (unchanged, generic across every
  `SceneCollection`) already handles first-create into an absent collection, so nothing
  `designOptions`-specific was needed. No diff touches `SCENE_SCHEMA_VERSION`.
- **Backward sweep** ("`bnn.ts`'s hostile-guard list was the only site besides the exhaustive
  `dependency.ts` switch"). Grepped `packages/document/src` for `'schedules'`/`'views'`-shaped pairings:
  `bnn.ts:218` is the only hardcoded array, and it now reads
  `['schedules', 'views', 'designOptions']`. Confirmed.
- **The primary-invariant / promote-demote / delete-referrer-ladder code** matches the entry's prose:
  promotion is the one path that mutates a sibling (atomic demote, same `UndoableEdit`); every other
  transition goes through `checkPrimaryInvariant`, which refuses rather than auto-fixes; the delete guard
  (`designOptionReferrers`) covers all three writable referrer classes (`elements`, `views`, `schedules`),
  built on the pre-existing D51 `Referrer`/`guardReferences`/`GUARD_ARGS` machinery — not reinvented.
- **`_baselinedAtEntry: 1000`** in the re-baselined `tests/frozen-surface.snapshot.json` looked like a
  placeholder on first read. It is not: `docs-state.mjs`'s `parseAbstracts` assigns `1000 - i` as a
  synthetic sort key to the newest new-scheme (`T-nnn`/`STEWARD-`) §7 entry, and T-011 is the topmost
  entry in §7 on this branch. Confirmed by reading `parseAbstracts` and `current_state.md` §7's ordering.

No claim checked came back false.

## 4. Item 5 — numbers

`WALL_VOLUME = 6000 * 200 * 3000` is computed in the test and asserted with `toBeCloseTo` — the
`3 600 000 000` / `7 200 000 000` mm³ figures have a method, not a hardcoded magic number. The
"12 RED / 21 green" and full-suite counts (913/96, 146/8) are reproducible; item 2 above reproduces the
first pair exactly. Did not re-run a second full `pnpm verify` beyond what `agent-finish.mjs` runs at
finish.

## 5. Item 7 — risk

`pnpm state`, run fresh on this branch:

```
newest entry T-011 (zayd) · RISK: contract-touching (re-baselined) · 902 green · 95 files · 281 suites
⚠⚠ contract-touching ⇒ the OWNER merges this PR, not the reviewing agent.
```

Matches both PR labels (`needs-operator/contract-touching`, `needs-operator/freeze`).
`tests/freeze-boundary.test.ts` green locally (12/12), diffing to exactly one declaration
(`type SceneCollection`), as the design doc's §3.2 predicted. `gh pr checks 32` confirms
`PR shape · reserved classes` actually ran and passed — the label state is a real labeller run, not an
absent one.

## 6. What this step did not do

Items 2 (backward sweep for a new rule/invariant), 3 (new-kind-of-thing sweep against every consumer and
the invalidator) and 6 (weak-green audit of the 13 new tests) are step 2's, by D88's split — not
skipped, deferred. Nothing on the branch was changed; this is a pure review turn.

## 7. Posted

Full findings posted to PR #32 as a comment, from `narutousomaki741`
(`GH_TOKEN=$(cat ~/.config/bunyan/hmdnah.token)`, verified against `gh api user` before posting).
