# T-024 — review (step 1 of 2, mechanical) — 2026-08-19 — seat: `hmdnah`

**PR:** #38, `task/T-024-baselinedatentry-names-a-position-so-a-c`, author seat `zayd`.
**Checklist:** `REVIEW.md` items **1, 4, 5, 7** (D88 step 1). Items 2, 3 and 6 are step 2's.
**Machine:** box (Hetzner, headless), Node 20, pnpm 10.34.5, vitest 4.1.10. Every claim below was
executed here; nothing is owed to a `pc` seat.
**Findings comment:** `github.com/Davidian-Abdo/Bunyan/pull/38#issuecomment-5341466379`, presence
verified by reading it back through the API (15971 chars, head and tail intact).

⚠ **Not approved, not merged.** PR #38 carries `needs-operator/freeze`, so `AGENTS.md §5.3` routes the
merge to the owner at step 2 as well as step 1. No merge command was run at any point.

## 1. Revert-verification — both reverts, re-executed by hand

**(A) `abstractKey` neutralised to `return a.n;`**, everything else in place:

```
 FAIL  freeze-boundary  … (T-024) > ⚠⚠ a §7 append on a later day leaves a sound baseline GREEN
       expected [ Array(1) ] to deeply equal []
 FAIL  freeze-boundary  … (T-024) > the committed baseline records a key, and it resolves to a real §7 abstract
       STEWARD-unblock-pc-and-chrome-boot — 2026-08-18 — brahim names no abstract in §7
 FAIL  state-risk-e2e   … (Q15) > ⚠⚠ writes a baseline its own gate accepts
       expected 1000 to be 'T-024 — 2026-08-05 — zayd'
 FAIL  state-risk-e2e   … (Q15) > ⚠⚠ stays green when a LATER-DATED abstract is appended
       + "_baselinedAtEntry is 1000, which is a §7 POSITION and not an entry identity (T-024)"
       + "_baselinedAtEntry is 1000, but the newest entry that exists is 1"

      Tests  4 failed | 21 passed (25)
```

Restored ⇒ **25/25**. Exactly the author's reported shape.

**(B) the pre-fix scheme entire** — nine files restored from `origin/main` (`docs-state.mjs`,
`docs-state.d.mts`, `frozen-surface.mjs`, `frozen-surface.d.mts`, `state.mjs`,
`frozen-surface.snapshot.json`, `freeze-boundary.test.ts`, `state-risk-e2e.test.ts`,
`docs-budget.test.ts`) with this turn's §7 abstract dated `2026-08-19` present:

```
 FAIL  freeze-boundary > … (Q15) > the committed baseline names an entry that exists, and no longer names 72
       + "_baselinedAtEntry 1000 is dated 2026-08-19 in §7, but _baselinedAt says 2026-08-18"
      Tests  1 failed | 11 passed (12)
```

Restored ⇒ **17/17**. The defect verbatim, on a branch that moved **0 of 214** declarations.

**The criterion, constructed rather than confirmed.** The `done-when:` says the gate must be *repaired,
not removed* — a baseline whose recorded date genuinely disagrees with the entry that authorised it must
still fail. I built that case instead of re-running the green one: the committed baseline hand-edited to
`"STEWARD-unblock-pc-and-chrome-boot — 2026-01-01 — brahim"` / `_baselinedAt: "2026-01-01"`, **both
halves moved together** so no self-consistency check can see the lie. It goes RED —
`… names no abstract in §7` — so the criterion is met. Which test caught it is finding **F3/F4**.

## 4. Claims vs code

**Clean:** `state.mjs:281` prints `id (seat, date)`; `docs-state.d.mts:32`'s *"Never render this to a
human — render `id`"* exists and is what the seven `docs-budget` message changes answer to (seven,
counted); `_baselinedAtEntry` is the only moved byte inside `frozen-surface.snapshot.json`; §7 stood at
**32696 / 32768** chars and **10 / 10** abstracts on `origin/main`, so the rotation was budget-forced.

**The baseline names the turn that actually authorised it — traced.** `git log -S` puts the write at
**`32c6cac`** (PR #36), which moved `_baselinedAt` `2026-08-17 → 2026-08-18` and left
`_baselinedAtEntry: 1000`. §7 at `32c6cac` had `### STEWARD-unblock-pc-and-chrome-boot — … —
2026-08-18 — seat: brahim` on top, so `1000` denoted exactly that entry and the new key resolves to it.

> ⓘ For the owner, not a defect here: `32c6cac` moved **zero** declarations — `_declarationCount` stayed
> 214, no surface byte changed. It re-baselined only to clear the cross-day date, i.e. the defect T-024
> fixes. The field is therefore an accurate record of a turn that authorised no surface change; the last
> turn that actually moved the surface was `T-011` (`c18ae8e`). The field's stated purpose is *"the audit
> trail back to the ruling that permits touching the freeze"*, and after the freeze that gap matters.

### F1 — the `⚠ MEASURED` uniqueness claim is falsified by the diff that ships it

`scripts/docs-state.mjs:203` (repeated in `docs/BACKLOG.md`'s Discovered row, handoff §5, and the §7
abstract's `FOUND:`) states *"the triple is not unique across §7 today"*. Not true of §7 as this branch
leaves it: **this same PR rotated both `T-016` abstracts into `docs/history.md`**, and they were the only
§7 collision.

| | abstracts in §7 | colliding keys in §7 |
| --- | --- | --- |
| `origin/main` | 10 | **1** — `T-016 — 2026-08-17 — hmdnah` |
| this branch | 9 | **0** |

The rotation is correct and budget-forced. The claim went stale inside its own commit, stated at the
strongest available level in four places.

### F2 — the collision is larger than recorded, and not only D88's shape

Over `current_state.md §7` + `docs/history.md`: **41 new-scheme headings, 4 colliding keys** —
`T-011 — 2026-08-17 — hmdnah`, `T-011 — 2026-08-17 — zayd`, `T-016 — 2026-08-17 — hmdnah`,
`T-015 — 2026-08-16 — hmdnah`. `T-011 — 2026-08-17 — zayd` is two **builder** turns on one task in one
day, which *"D88's two review steps are separate turns by the SAME seat"* does not account for; the
second builder turn is `REVIEW.md`'s own defect-return path (`agent-start.mjs --continue`, `T-015`). The
generator is "any two turns by one seat on one task on one day", and D88 is one of at least two routes.

**Judging the stated reasoning** (*"no uniqueness gate, because one would be red on a merged abstract and
invariant 10 forbids rewriting it"*):

- At **§7 scope** — where every other docs gate operates — the premise is **false**: a §7 uniqueness gate
  would be **green** today, precisely because this PR rotated the collision out. It would bind only
  future entries, invariant 7's normal case.
- At **whole-record scope** the premise is **true**: four merged, unrewritable abstracts collide.
- The **conclusion holds either way**, but not for the reason given: a §7-scoped gate goes red the next
  time two turns by one seat land on one day — a *correct* turn, on a freshly-merged abstract invariant
  10 then protects. A gate that cries wolf, which is the disease this whole area is about.

Not blocking. The Discovered row is what `brahim` acts on, and it currently names the wrong scope, the
wrong count and an incomplete mechanism.

### F3 — the new §7-resolution test reintroduces T-024's own failure shape, six appends out

The diff's central claim is that a key **outlives rotation** (`frozen-surface.mjs:280` *"it cannot rot
with §7's rotation"*; handoff §2 *"They survive rotation out of §7"*; the §7 abstract's *"now holds
**forever** rather than only while the named entry is in the window"*).

True of `baselineEntryIssues`. **Not true of the test that enforces it on the real file.**
`freeze-boundary.test.ts` → *"the committed baseline records a key, and it resolves to a real §7
abstract"* does `parseAbstracts(readCurrentState(ROOT)).find(a => a.key === snapshot._baselinedAtEntry)`
then `expect(named).toBeDefined()` — which **requires** the authorising abstract to still be inside §7's
rotating window, and **fails** rather than skips when it leaves.

```
§7 depth: 9 / cap 10 ;  authorising entry at §7 index 4  →  6 appends until it rotates out
```

Executed, not simulated — I deleted the `STEWARD-unblock-pc-and-chrome-boot — … — brahim` abstract from
§7 (what rotation does) and ran the suite unchanged:

```
 FAIL  … > the committed baseline records a key, and it resolves to a real §7 abstract
       STEWARD-unblock-pc-and-chrome-boot — 2026-08-18 — brahim names no abstract in §7
      Tests  1 failed | 16 passed (17)

 baselineEntryIssues(snapshot, rotatedAbstracts) -> []      ← the FUNCTION skips, correctly
```

The function behaves as designed; the test does not. In ~6 turns `pnpm docs:check` goes red for everyone
on a turn that moved no declaration — T-024's own shape with a longer fuse. The predecessor check was
deliberately built to skip on rotation (*"which is why it never cries wolf"*, `frozen-surface.mjs:284`);
this one is not. It lands after the P5 freeze, when `--rebaseline` is owner-gated and the cheap exit is
gone.

### F4 — `baselineEntryIssues` never consults §7 on the keyed path (compounds F3)

The keyed branch compares the key's embedded date to `_baselinedAt` and returns; `abstracts` is never
read. Measured against the real §7:

```
"STEWARD-unblock-pc-and-chrome-boot — 2026-01-01 — brahim" / "2026-01-01"  ->  []
      (§7 records that entry as dated 2026-08-18)
"T-999 — 2026-01-01 — nobody" / "2026-01-01"                               ->  []
      (names an entry that has never existed)
```

Self-consistency only. A defensible trade — it is what buys rot-freedom — and the repo is **still covered
today**, because F3's resolution test catches both (I ran the first; it goes red). But the two compound:
the entire §7 cross-check now rests on the one test F3 shows will go red on a correct turn, and the
obvious repair then is to loosen or delete it, leaving the gate with no tie to §7 at all — which is how
Q15 happened.

**Suggested shape** (a finding, not a fix): move the resolution into `baselineEntryIssues` and give it
the predecessor's skip — *if the key resolves in §7 its date must agree; if it has rotated out, skip* —
so the function keeps its self-contained check and the §7 tie returns without a rotation fuse. The test
then asserts the function's verdict rather than doing its own lookup.

**Not fixed on the branch:** step 1 is mechanical, nothing is red today, and F3/F4 are a scoping choice
on the author's own criterion — `AGENTS.md §1.2` returns that to the builder on the existing claim.

## 5. Numbers

| Claim | Method | Verdict |
| --- | --- | --- |
| suite **936 → 942** | full `vitest run --reporter=json` on `origin/main` and the branch | ✅ 936 / 942, 0 failed both |
| `docs:check` **154 → 159** | per-file counts from the same two JSON reports | ✅ 154 / 159 |
| `freeze-boundary` **12 → 17** | same | ✅ 12 / 17 |
| **0 of 214** declarations moved | `diffSurface(baseline.surface, buildSurface('.'))` | ✅ `{added:[],removed:[],changed:[]}`, 214 = 214 |
| §7 **32696 / 32768**, **10 / 10** | `section7(origin/main:current_state.md).length` | ✅ exact |
| key not unique **across §7 today** | key multiplicity over §7 | ❌ **F1** — 0 collisions on this branch |

**The growth is all new coverage, and nothing that used to run stopped running.** Set-differenced by
fully-qualified test name rather than trusting totals:

- **removed on the branch: 2** — both **renames** of tests still present (*"writes the entry number …"* →
  *"… identity …"*; *"… an entry number that disagrees …"* → *"… an entry whose date disagrees …"*).
- **added on the branch: 8** — the 2 renamed titles, 5 in the new `freeze-boundary` T-024 block, 1 in
  `state-risk-e2e`.

Net **+6**, entirely in `freeze-boundary.test.ts` (+5) and `state-risk-e2e.test.ts` (+1). Per-file counts
for `docs-budget` (21), `protocol.test.ts` (8) and all five `tests/protocol/*` (17 / 33 / 8 / 9 / 46) are
**identical** on both sides. No file lost a test; `pending` = 0 on both runs, so nothing was skipped.

⚠ For step 2, since a rename can hide content drift: the renamed *"catches Q15's own shape"* test no
longer asserts against the **real** §7 on its keyed half — it uses a hardcoded `'T-024 — 2026-08-19 —
zayd'` — and its legacy half moved onto a synthetic fixture. Coherent with F4; item 6's call, not mine.

## 7. Risk

- `pnpm state` ⇒ **`RISK: additive`**, `942 green · 97 files · 288 suites`. **Matches the diff:** no
  `packages/` byte, no `apps/web` byte, no `SCENE_SCHEMA_VERSION`, no frozen declaration,
  `_declarationCount` 214 unchanged.
- **Label `needs-operator/freeze`**, and ⚠ the labeller **did execute** — `PR shape · reserved classes`
  **pass, 14 s** (run `32203506067`) — so this is a verdict, not a silent absence, which is item 7's own
  *"no label and the labeller never ran look identical"* trap. `typecheck · lint · geometry harness`
  **pass, 9 m 34 s**. Both green.
- Mechanism confirmed at source: `scripts/reserved-classes.mjs:151` pushes `freeze` on a bare path match
  against `tests/frozen-surface.snapshot.json`, so a metadata-only edit labels. The author's §6 read is
  accurate and the cry-wolf is already a `## Discovered` row.

⇒ **`AGENTS.md §5.3`: the owner merges, at step 2 as well.** No approval given, no merge run.

## Verdict — step 1

**Mechanically sound.** The defect is real, both reverts reproduce independently, the repair holds on the
case I constructed rather than the one I was shown, every number re-derives, and the risk verdict matches
the diff. Four findings, none blocking, none red today: **F1** and **F2** are corrections to the record;
**F3** is the one worth a decision, the author's own *"repaired, not removed"* criterion holding for the
function but not for the test that enforces it; **F4** compounds it.

## Owed

- `hmdnah` — **step 2** (items 2, 3, 6, plus re-confirming item 7's `needs-operator/*` immediately before
  the merge), separate session, same claim, row stays `review`. Reconcile against this report; do not
  re-run it.
- The **owner** — the merge. `needs-operator/freeze` is applied and `AGENTS.md §5.3` is unconditional.
- `zayd` — **F3/F4** if step 2 agrees, returned on the existing claim via `agent-start.mjs --continue
  T-024`; **F1/F2** as corrections to `docs/BACKLOG.md`'s Discovered row and the four copies of the
  uniqueness claim.
- `brahim` — the Discovered row's scope, count and mechanism are wrong (F1/F2) before it is acted on.

## Harness notes

- `agent-finish.mjs --seat hmdnah T-024 --review --step 1` printed **no** merge commands. `REVIEW.md`
  item 7 records that it does print them for step 1 and is wrong to (`T-014`); it did not here, so that
  defect did not recur on this path.
- The `review/step-1` label and the findings comment were applied separately and **both** verified by
  read-back, per the recorded defect where the label lands and the comment does not.
- §7 stood at 31578 / 32768 with 9 of 10 abstracts, so `T-017`'s two abstracts rotated to
  `docs/history.md` §E to make room for this one — maintenance, not work.
