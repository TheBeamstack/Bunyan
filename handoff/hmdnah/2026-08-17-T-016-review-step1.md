# T-016 — review (step 1, mechanical): the revert holds at both levels, and the fixture the `done-when:` names is reachable

**Seat:** `hmdnah` (reviewer, box — headless). **Date:** 2026-08-17. **PR:** #33. **Step:** 1 of 2
(D88, `REVIEW.md` items 1, 4, 5, 7). **Verdict:** clears — no defect that blocks; two documentation
corrections for step 2.

## 1. Revert-verification — re-executed here, at both levels

**Unit.** `resolveBuilder`'s body → `return seat;` (the pre-fix shape), then
`vitest run tests/protocol/agent-finish.test.ts tests/protocol/agent-start.test.ts`:

```
 × resolveBuilder … > a review finish carries the PRIOR builder forward, never the reviewing seat
   → expected 'hmdnah' to be 'zayd' // Object.is equality
 Test Files  1 failed | 1 passed (2)
      Tests  1 failed | 49 passed (50)
```

Restored: **50/50 green**. The entry's claim ("1 of 3 RED, `expected 'hmdnah' to be 'zayd'`") is exact.

**Fixture, end to end** — the reproduction `done-when:` bullet 4 actually names, which the PR delivers
only as the pure unit test above. A `makeFixture` repo plus one `package.json` whose `verify` script is
a no-op clears step 1 and reaches the baton write; `risk: normal` keeps the run off `gh`, and the
closing `git push` fails (no upstream) only after the baton is on disk. Prior baton: `seat: zayd`,
`builder: zayd`, finished. Finishing seat: `hmdnah`, `--review`.

| run                          | resulting baton                     |
| ---------------------------- | ----------------------------------- |
| `resolveBuilder` reverted    | `seat: hmdnah` · **`builder: hmdnah`** |
| fix in place                 | `seat: hmdnah` · **`builder: zayd`**   |

So the measured defect reproduces at the level the task specified, and the fix closes it there.

## 2. Claims vs code (item 4)

Checked against the source and true: `parseBaton` parses `| field | value |` rows generically and
needed no change; `renderBaton` has exactly two production callers, so `agent-start.mjs`'s claim write
is the only place `builder` is set from scratch; `seats.builderFor` derives the admitted seat from the
task's `machine:`.

Three claims that do not hold as written:

1. **`resolveBuilder`'s doc-comment and handoff §2/§5** — "past what this repo's fixtures can reach",
   "the only way to test-first and revert-verify it without a fixture that can pass a real
   `pnpm verify`". Section 1 above reaches it with one added file. The consequence is not the
   extraction (a pure `resolveBuilder` is right either way) but the coverage: nothing committed
   exercises `agent-finish.mjs`'s baton write, so `done-when:` bullet 4 is discharged by this review's
   re-execution alone.
2. **`scripts/agent-start.mjs`'s file header and its `--continue` gate comment** — both still say the
   §0b baton is one a `--review` finish "rewrites to name the REVIEWER, not the builder", which is the
   condition this PR removes. Leaving `--continue`'s derivation alone is a deliberate call (handoff
   §5); the reason both comments give for it is now stale.
3. **Handoff §2** — a pre-T-016 baton does not render "identically to before": it gains a
   `| builder | <seat> |` row, which is what the new `renderBaton` test asserts. Wording only.

## 3. Numbers (item 5)

Every figure re-measured on this box, all matching: `pnpm test` **95 files / 907 tests**;
`pnpm docs:check` **8 files / 151 tests**; both protocol suites **50/50**; the revert's red count and
its assertion message as quoted above.

## 4. Risk (item 7)

The diff touches `scripts/`, `tests/protocol/` and docs — no path in `frozen-surface.mjs`'s `WATCHED`
list, and `tests/freeze-boundary.test.ts` is 12/12 green, so `RISK: additive` matches the diff. PR #33
carries no `needs-operator/*` label **and** the labeller ran: `PR shape · reserved classes` and
`typecheck · lint · geometry harness` both report SUCCESS on the tip, so "no label" is a verdict rather
than silence (`REVIEW.md` item 7's own warning). Step 1 does not approve and does not merge.

## 5. Scope note for step 2

On a baton written before this PR merges there is no `builder` row, so a `--review` finish falls back to
the finishing seat — measured on the fixture: `builder: hmdnah`. The field is therefore trustworthy only
on branches claimed after the merge. This is the documented fallback and is the right one: reading
`priorBaton.seat` instead would name the step-1 reviewer on a `risk: high` step-2 finish.

Items 2, 3 and 6 are step 2's, unrun here.
