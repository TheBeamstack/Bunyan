# T-015 — review (step 2): the fix holds, backward sweep and weak-green clean

**Seat:** `hmdnah` (reviewer, box — headless). **Date:** 2026-08-16. **PR:** #27, opened by
`Davidian-Abdo` (`zayd`), reviewed and merged on `narutousomaki741`. **Decisions:** D88 (two-step
`risk: high` review), T-015.

## 0. Which step this is

Step 1 (a separate session, 2026-08-16) ran items 1, 4, 5, 7 and found one real defect: the `--continue`
admission gate called `seats.builderFor(root, continueTask)` with no `finishingSeat`, so a
`machine: any` task's fallback always resolved to `zayd`/`box` regardless of who actually built it.
Posted as https://github.com/Davidian-Abdo/Bunyan/pull/27#issuecomment-5305288044, no approval, row
stayed `review`. A second `zayd` turn fixed it on the same branch via `agent-start.mjs --continue T-015`
(no new claim) — commit `2798b2c`, one production line, one new regression test. This turn is step 2:
items 2, 3, 6, reconciled against step 1's report rather than re-running it, then approve-and-merge.

## 1. Reconciliation — the fix, spot-checked myself

`git diff 902e827 2798b2c` shows exactly two hunks: `scripts/agent-start.mjs:400`
(`seats.builderFor(root, continueTask)` → `seats.builderFor(root, continueTask, seat)`) and one new test
in `tests/protocol/agent-start.test.ts`. Nothing else moved. Reverted the one-line fix by hand and ran
`tests/protocol/agent-start.test.ts`: **1 failed | 16 skipped**, failing at
`✖ T-001's builder is 'zayd', not 'amer'.` — the exact defect shape step 1 reported, reproduced
independently rather than trusted from the report. Restored the fix: **17/17** in that file. Full
`pnpm verify`: **847/847**, exit 0, `tests/freeze-boundary.test.ts` green. Working tree clean before and
after (`git status --short` empty both times) — the round-trip left no residue.

## 2. Item 2 — backward sweep

`builderFor` has exactly one production call site: `scripts/agent-start.mjs:400`, now fixed. Every
`reviewerFor` call site checked for the same missing-argument shape:
`scripts/agent-finish.mjs:324` and `:475` both pass `seat`; `scripts/agent-start.mjs:507` passes
`undefined` deliberately — that line is the pre-existing (untouched by this PR) reviewer-routing
*display* loop, which lists every open PR's routing for an at-a-glance view rather than admitting one
seat onto one branch, so it has no single finishing seat to resolve `machine: any` against. No second
instance of the bug exists.

## 3. Item 3 — new kind of thing

The new entity is the `--continue` door and `builderFor` as a second seat-resolution axis beside
`reviewerFor`. Consumers of "who owns this task's branch": the admission gate itself, and `findTaskPR`
(used only there). `agent-finish.mjs`'s new `existingPR` check does **not** key on whether `--continue`
was the entry path — it re-queries `gh pr view --json number,url,state` fresh on the current branch, so
it behaves identically whether the builder arrived via `--continue` or via the pre-existing "resume your
own open claim" path (`agent-start.mjs`'s ordinary claim-continuation branch, unrelated code). No
cache/invalidator analog exists in this script suite for `--continue` to have missed.

Two non-blocking observations, recorded as opinion per `REVIEW.md`'s refusals section, not a defect:

- The reviewer-routing display loop (`agent-start.mjs` ~L502-513, pre-existing) re-implements
  `title.match(/^T-\d{3}/)` rather than reusing the now-exported `findTaskPR`. Duplication, not a
  correctness bug — a `simplify`-class cleanup, not a review finding.
- `gh pr list --state open --json ... --limit 10` (both call sites, unchanged by this PR) would silently
  miss a task whose PR sits past the 10 most recently updated open PRs. Pre-existing, not introduced
  here; today's open-PR count is 1.

## 4. Item 6 — weak green

The new regression test (`"a machine: any task admits the caller's OWN builder, never a hardcoded box
default"`) asserts admission succeeded only indirectly: it checks the run does NOT fail at the
builder-mismatch message and DOES fail one gate later, at `"No open PR names T-001"` — because this
fixture's `origin` is a bare local repo with no real PR to find. Reverted the fix (see §1) and watched it
fail at exactly the predicted message, confirming the assertion is pinned to the real defect and not to
an unrelated symptom.

Considered a false-green route: if `builderFor`'s `any` fallback used the raw `finishingSeat` string
directly as a machine value (instead of `machineOf(finishingSeat)`), the registry lookup would find no
builder for machine `'amer'` and throw `"no builder registered for machine 'amer'"` — a different
message, which fails the test's second assertion (`/No open PR names T-001/`) too. That class of
regression is also caught, not just the exact line reverted. The sibling test using `machine: box`
(`amer` on a box task) is unaffected by this fix and independently guards against blanket
over-admission. The test is not weak.

## 5. Item 7 — risk and gate, re-confirmed immediately before merging

`gh pr checks 27`: both jobs (`PR shape · reserved classes`, `typecheck · lint · geometry harness`)
SUCCESS. `gh pr view 27 --json labels`: `[]` — no `needs-operator/*`. `mergeStateStatus: CLEAN`,
`mergeable: MERGEABLE`. `current_state.md`'s frozen-surface verdict: `RISK: additive`, unchanged.

## 6. Identity

`gh api user --jq .login` returned `narutousomaki741` under the per-turn `GH_TOKEN` from
`~/.config/bunyan/hmdnah.token`; PR #27 was opened by `Davidian-Abdo`. Checked before approving or
merging.

## 7. Verdict

**APPROVED and MERGED**, `RISK: additive`. Step 1's defect is fixed, on this branch, confirmed by an
independent revert-verify rather than trust in the report. No new blocking finding.
