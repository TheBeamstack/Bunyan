# T-009 — review (step 1, mechanical) — a hosted void may only host on its host's own base part

**Seat:** `hmdnah` (reviewer, box — headless). **Date:** 2026-08-16. **Task:** `docs/BACKLOG.md` T-009,
PR #31. **Risk:** `high` — D88's two-step review; this is step 1 (mechanical, items 1, 4, 5, 7 of
`REVIEW.md`). No approval, no merge — the row stays `review`.

## 1. REVERT-VERIFICATION (item 1)

Neutralised `requireHostFaceIsBasePart`'s guard condition in `packages/document/src/commands.ts`:

```
if (false && (decoded.nodeId.includes('~') || decoded.role.startsWith('cut('))) {
```

Ran `vitest run tests/document-openings.test.ts`: **RED — 2 of 14 failed**, exactly the two new D84
tests (`createElement REFUSES a host face a prior cut already created or modified` and
`retargetReference REFUSES a host face a prior cut already created or modified`), both on
`.rejects.toThrow(/own base part/)` — the guard silently accepted the derived face and the assertion
never threw. Restored the real condition, re-ran: **14/14 green**. Working tree confirmed clean
(matches `HEAD` exactly) after restoring. Matches the author's own revert-verification claim exactly.

## 2. CLAIMS vs CODE (item 4)

Read every structural claim the fix's legitimacy rests on against the actual code, not the prose:

- `decodeSubShapeRef` (`packages/protocol/src/subshape.ts:63`) is a real structured decode — splits on
  `/`/`#`, validates `kind`/`occurrence` — not string-splitting. Confirmed.
- `cutNodeId` (`packages/document/src/geometry.ts:113`) is the only definition site in the repo;
  `packages/document/src/build.ts:488` is its only caller. Confirmed by grep — `~`'s presence in a
  `nodeId` can only mean "produced by a cut," as claimed.
- `checkNameSafe` (`commands.ts:192-206`, unchanged) already refuses `/`, `#`, `~` in every authored
  name, a few dozen lines above the new function. Confirmed.
- `derivedRole` (`packages/kernel-occt/src/naming.ts:222-225`) composes a `REL_DERIVE` role as
  `` `${opTag}(...)` ``. The boolean-cut handler (`kernel.ts`'s `boolean:` op) calls
  `register(id, nodeId, payload.kind, [a, b])`, and `payload.kind` is literally `'cut'` for a cut.
  Confirmed — `role.startsWith('cut(')` is sound.
- Call-site placement matches the handoff's description exactly: `createElementCommand.execute` checks
  right after `hostId` is validated to exist and before the new element object is built
  (`commands.ts` ~L656–661 checks, L690–700 builds); `retargetReferenceCommand.execute` checks right
  after the belongs-to-cycle guard and before the edit is built (~L1091–1105). Confirmed by reading
  both sites directly.
- `hostRef` is written in exactly two places in `packages/document/src/*.ts` — the two now-guarded
  commands. The one other site (`layerRenameReferrers`'s `redirect`, D51, `commands.ts:464`) rewrites
  only the layer-name segment of an *already-valid* `hostRef` — a pure string substitution keeping the
  same face — never accepts a new one from a caller. Confirmed by grep, matches §6 of the handoff.

No claim-vs-code mismatch found.

## 3. NUMBERS (item 5)

- `tests/freeze-boundary.test.ts` — ran locally: **12/12 green**.
- Full suite — ran `pnpm test` independently (fresh run, not from any cache): **95 files, 902 tests,
  all green**, matching the handoff's "95 files, 902 tests" claim and `pnpm state`'s regenerated §8
  exactly (`.vitest-summary.json` timestamped after this run, 902/902 passed, 281/281 suites).

## 4. RISK (item 7)

`pnpm state` reports `RISK: additive`, matching the PR's own claim; `tests/freeze-boundary.test.ts`
green confirms no frozen-surface byte moved. **CI was still `pending` on both jobs
(`pr-shape · reserved classes`, `typecheck · lint · geometry harness`) at review time, and no
`needs-operator/*` label was present yet.** Per `REVIEW.md` item 7's own warning — "no label" and "the
labeller never executed" look identical from here — this is recorded as unresolved-at-this-turn, not
as clearance: step 2 must re-confirm the label state once CI has actually finished before merging.

## 5. What was not run this turn

Items 2 (backward sweep), 3 (new kind of thing), 6 (weak green) are step 2's, per D88/`REVIEW.md`'s
split table. Not run here.

## 6. Verdict

Nothing found that blocks. Report posted to PR #31
(https://github.com/Davidian-Abdo/Bunyan/pull/31#issuecomment-5309505380). Row stays `review`. Step 2
(a separate `hmdnah` session) should re-confirm CI/labels before merging, per its own mandate.
