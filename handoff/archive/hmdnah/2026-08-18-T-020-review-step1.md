# T-020 — review, D88 step 1 of 2 (mechanical) — 2026-08-18 — seat: `hmdnah`

**PR:** #37, tip `82e1c60`. **Items:** `REVIEW.md` 1, 4, 5, 7. **Outcome:** no defect; no approval, no
merge, row stays `review`. **Findings comment:** `#37 issuecomment-5333851516`, verified present.

## 1. Revert-verification — RED here

Reverted the diff's only fix (`}, 120_000)` → `})` in `tests/document-persistence.test.ts`), branch runner
`vitest@4.1.10`:

```
   × ⚠⚠ THE CLEAN DELTA IS COMPUTABLE FROM A .bnn — 250+ edits, an undo, two revisions (D40)  19001ms
     → Test timed out in 5000ms.
 Test Files  1 failed (1)   Tests  1 failed | 8 passed (9)
```

Restored: 9/9 green, and 18 740 ms inside the full run below.

## The behavioural claim, measured

Same commit, same reverted file, each runner installed from its own lockfile via
`pnpm install --frozen-lockfile`:

| runner | the clean-delta test | result |
| --- | --- | --- |
| `vitest@2.1.9` | 18 855 ms | ✓ passed |
| `vitest@4.1.10` | 19 001 ms | ✗ `Test timed out in 5000ms` |

A probe written for this review (temporary file, deleted; no kernel involved) isolates the mechanism from
the cost:

| probe test, no explicit timeout | `2.1.9` | `4.1.10` |
| --- | --- | --- |
| **A** — microtask-only chain busy ~8000 ms | ✓ passed, 8001 ms | ✗ timed out in 5000 ms |
| **B** — `setTimeout` 8000 ms | ✗ timed out in 5000 ms | ✗ timed out in 5000 ms |

A passes under 2.1.9 and fails under 4.1.10 while B fails under both, so vitest 2's default deadline is a
timer that a chain resolving through `queueMicrotask`
(`packages/kernel-client/src/transport.ts:63`) never lets reach the timer phase. The author's account is
correct: the test's cost moved 0.8%, the enforcement moved, and invariant 8 caught a real weak green.

## 5. Numbers

Fresh full runs on this branch, each runner installed in turn:

| runner | tests | files | suites | duration |
| --- | --- | --- | --- | --- |
| `vitest@2.1.9` | 936 passed | 97 | 287 | 274.63 s |
| `vitest@4.1.10` | 936 passed | 97 | 287 | 271.46 s |

Diffing the two json summaries by `(file, test title)` gives an **empty symmetric difference** — the same
936 tests, not merely the same count, which is what the `done-when:` ⚠ is actually asking. `pnpm docs:check`
is 154 · 8 under both runners.

Duration population from the 4.1.10 summary: exactly three of 936 tests exceed 5000 ms (58 813, 18 740,
16 785 ms); next-slowest is `geometry-cache-d29` at 1878 ms, the same test the author priced at 1980 ms, so
the margin is 2.7× here.

## 4. Claims vs code

- Imports from `vitest`, 96 files: exactly the eight enumerated; no `test(`/`suite(` site.
- Third-argument form: 131 numeric `}, N)` sites (130 before this diff), **zero** options-object sites.
- `.toThrow` 58, `.toEqual` 394 — exact; no `mockReset`, `spyOn`, snapshot, custom reporter,
  `workspace`/`projects` or coverage config anywhere.
- Lockfile resolves one `vite@6.4.3` and one `vitest@4.1.10` where `main` resolved `vite@5.4.21` too, and
  CI's `pnpm install --frozen-lockfile` succeeded, so pin and lockfile move together.
- Nit: the `vi.*` enumeration is complete but counted as five where the tree has six call sites of four
  distinct methods.

## 7. Risk

`node scripts/reserved-classes.mjs --base <main>` → `none — RISK: additive`, matching §8 and the diff.

⚠ `PR shape · reserved classes` was **red** when this review started, failed inside `Set up job` on
`429 (Too Many Requests)` downloading `actions/checkout@v4` after three retries — infrastructure, no repo
content involved, and the labelling step never executed. Item 7's *"no label and the labeller never ran look
identical"* case exactly, so the failed job was re-run; both jobs are now `success` on `82e1c60` and the
empty label set is informative. The `CI green on the self-hosted runner` `done-when:` item is discharged.

## Not verifiable here

`unverified here: the five protocol files collect on Windows — the pc seats to confirm`. This box parses all
five under both runners, so it can neither reproduce the failure nor witness the repair; **T-021** closes it
and the criterion is not ticked here.

## For step 2

Items 2, 3 and 6 remain, plus item 7's `needs-operator/*` re-confirmation immediately before merging. The
two nits above are recorded, not blocking.
