# T-016 — review (step 2, adversarial): the field is correct and its wiring is untested

**Seat:** `hmdnah` (reviewer, box — headless). **Date:** 2026-08-17. **PR:** #33. **Step:** 2 of 2
(D88, `REVIEW.md` items 2, 3, 6, plus item 1 and item 7's re-confirmation). **Verdict:** approve and
merge — no defect. Two findings, neither provable with a failing test, both carried as `OWES:`.

Step 1's findings comment never reached PR #33 (`docs/BACKLOG.md ## Discovered`, 2026-08-17), so this
step reconciled against `handoff/hmdnah/2026-08-17-T-016-review-step1.md` and its `§7` abstract instead.
Every step-1 claim re-checked here held.

## 1. Revert-verification (item 1) — re-executed independently of step 1

`resolveBuilder`'s body → `return seat;`, then both protocol suites:

```
 × resolveBuilder … > a review finish carries the PRIOR builder forward, never the reviewing seat
   → expected 'hmdnah' to be 'zayd' // Object.is equality
 Test Files  1 failed | 1 passed (2)
      Tests  1 failed | 49 passed (50)
```

Restored, then the whole `tests/protocol/` tree: **113/113 green**, working tree clean.

## 2. Backward sweep (item 2)

Every site the new field governs, enumerated:

| site                                          | reads/writes                    | state after this PR              |
| --------------------------------------------- | ------------------------------- | -------------------------------- |
| `agent-start.mjs` claim write (`~889`)        | writes `builder: seat`          | correct, covered by a real spawn |
| `agent-finish.mjs` baton rewrite (`~505`)     | reads prior, writes resolved    | correct, **uncovered** (§4)      |
| `agent-start.mjs` `liveClaims` (`~207`)       | reads `seat`/`task`/`status`    | unchanged, correct (§3)          |
| `renderBaton`/`parseBaton`                    | round-trip                      | correct, covered                 |
| `tests/protocol/fixture.mjs` (`~70`)          | a pre-T-016 baton placeholder   | exercises the fallback path      |

**Finding — one rationale, four copies, all four now stale.** The reason `--continue` derives the
admitted seat from `machine:` rather than the baton is written out in full at four sites, and this PR
falsifies the version each of them gives: `scripts/agent-start.mjs`'s file header (`~48`), its
`--continue` gate comment (`~499`), `seats.builderFor`'s doc-comment (`scripts/seats.mjs` `~459`) and
`tests/protocol/seats.test.ts` (`~306`). Each says a gate on the baton "would admit the reviewer and
refuse the builder in every real call", which is true of the baton's `seat` and false of its new
`builder`. Step 1 flagged the first two; the other two are the same class. The derivation itself should
stay — `builder` is absent from every baton claimed before this merges (§3) — so only the stated reason
is wrong. Not fixed here: `seats.mjs` is outside this diff, nothing on step 2 reviews what step 2
writes, and a comment is not provable with a failing test (`REVIEW.md`'s own routing).

The duplication is the mechanism: `AGENTS.md §7.2` asks for one copy and a pointer, and four copies is
how a single change left four disagreements.

## 3. New kind of thing (item 3)

`builder` is a new relationship — task → the seat that built it — distinct from the branch's occupant.
What quantifies over every one of them is `liveClaims()`, which reads every `task/*` branch's own §0b
and feeds two refusals: the ready-loop skip (`agent-start.mjs` `~811`) and the claim-collision `die`
(`~854`).

Both still compare `seat`, and that is the right field: a collision gate asks who is holding the branch
now, not who built it. `builder` is provenance. No consumer is dirty.

**Measured, over every live baton** — the sweep this item asks for, run rather than reasoned:

| branches with a `builder` row | without |
| ----------------------------- | ------- |
| 1 (`task/T-016-…`)            | **7**   |

The other seven read `seat: hmdnah` with no `builder`, so after this merges their next `--review` finish
takes `resolveBuilder`'s documented fallback and stamps `builder: hmdnah` — the reviewer named as the
builder on seven open PRs. Step 1 identified the fallback; the count is the size of it. The fallback is
still the right one (reading `priorBaton.seat` would name the step-1 reviewer on a step-2 finish), but
it converts a silence into a false positive, and omitting the row when no builder is on record would
not. That is a design question for the steward, not a defect against this task's `done-when:`.

## 4. Weak green (item 6)

The three `resolveBuilder` cases assert a pure function; nothing asserts it is called. Measured by
mutation — the `builder` key deleted from `agent-finish.mjs`'s `claim` object, `resolveBuilder` left
correct, which restores the exact pre-T-016 defect at the call site (`renderBaton`'s `builder ?? seat`
then names the reviewer):

```
 Test Files  5 passed (5)
      Tests  113 passed (113)
```

So the suite's title — "the §0b baton `builder` field survives a review finish" — can be true while the
baton does not survive anything. `done-when:` bullets 2 and 3 rest on re-execution, not on a committed
test. `agent-finish.test.ts`'s own header states this boundary deliberately (everything it covers
happens before `pnpm verify`), and step 1 measured that one added `package.json` with a no-op `verify`
crosses it — so the gap is closable, and closing it is a change to `zayd`'s fixture harness with a
113-test blast radius, not a reviewer's edit on a merge turn.

The claim-write half is not weak: `agent-start.test.ts`'s "a successful claim is pushed before work
begins" spawns the real script and asserts `| builder | \`zayd\` |` on disk, discharging bullet 1.

**The wiring is nonetheless proven, on this branch's own history** — which is what `zayd`'s `OWES:`
asked step 2 to check:

| commit                          | finish             | baton                                 |
| ------------------------------- | ------------------ | ------------------------------------- |
| `0173d1a` claim (pre-fix script) | —                 | `seat: zayd`, no `builder`            |
| `153c957` `zayd`                | plain              | `seat: zayd` · `builder: zayd`        |
| `c9ac027` `hmdnah`              | `--review`         | `seat: hmdnah` · **`builder: zayd`**  |

A live `--review` finish left the builder's identity intact, which is the whole of the owner's ruling.
`zayd`'s `OWES:` is discharged.

## 5. Risk (item 7) — re-confirmed immediately before merging

Tip `c9ac027`: labels are `review/step-1` alone, no `needs-operator/*`, and the labeller ran — `PR shape
· reserved classes` and `typecheck · lint · geometry harness` both pass, so "no label" is a verdict.
`RISK: additive` matches a diff confined to `scripts/`, `tests/protocol/`, `handoff/` and docs. Re-checked
on this turn's tip before the merge command ran.

## 6. Reconciliation with step 1

All three of step 1's claims-vs-code corrections hold, and its scope note is confirmed and quantified in
§3. Its verdict — clears, no defect that blocks — is step 2's verdict too. Nothing step 1 recorded was
overturned.
