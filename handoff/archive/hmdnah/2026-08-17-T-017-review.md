# T-017 — review: the new gate has teeth on the real file, not only on its fixture

**Seat:** `hmdnah` (reviewer, box — headless). **Date:** 2026-08-17. **PR:** #34. **Task:**
`docs/BACKLOG.md` T-017, `risk: normal` ⇒ the ordinary one-step review (`REVIEW.md`).

## 1. Revert-verification (item 1) — re-executed here, not inherited

Baseline `vitest run tests/docs-budget.test.ts`: **21/21 green**. Applied the author's neutralisation
in `outOfDateOrder` (`return above && above.date < below.date` → `return false && …`):

```
× every §7 abstract is well formed > ⚠⚠ refuses an older entry sitting above a newer one — what `.n` could not see
  → expected [] to deeply equal [ Array(1) ]
  - Array [ "T-001 (2026-01-02) is below T-002 (2026-01-01)" ]
  + Array []
 Tests  1 failed | 20 passed (21)
```

**1 of 21 RED**, exactly the case and the message the entry claims. Restored: **21/21 green**, tree
clean.

## 2. Item 6, weak green — what would it take to pass while §7's order is genuinely wrong

The fixture proves the helper. It does not prove the **gate** — the fixture case would stay green if
line 156's real-file assertion were deleted. So I made the real §7 genuinely out of order rather than
a synthetic one: on the working tree, `### T-013 — … — 2026-08-16 — seat: zayd` (§7's last abstract)
re-dated to `2026-08-18`, which puts a newer entry below an older one in the actual file.

```
× every §7 abstract is well formed > is written newest-first, judged by each entry’s own date
  → abstracts must be newest-first: expected [ Array(1) ] to deeply equal []
  + Array [ "T-013 (2026-08-18) is below T-013 (2026-08-16)" ]
```

On that same input, the check this PR replaced:

```
OLD .n-descending check on this genuinely out-of-order real file: GREEN (blind)
ns: 1000, 999, 998, 997, 996, 995, 994, 993, 992, 991
```

Old green, new red, one real file. Restored with `git checkout -- current_state.md`, 21/21 green. The
"vacuous → has teeth" claim is externally true, and it binds `current_state.md` itself.

Three further ways the new check could pass while the order is wrong, enumerated:

- **Same-day swap.** Real, named by the author, and the ceiling is the whole point. Scope on this tip:
  §7 holds **7** entries dated `2026-08-16` and **3** dated `2026-08-17`, so the only ordering the gate
  can currently observe on the live file is across that one day boundary. Accepted — day granularity
  is what a heading carries, and closing it is a §7 schema change (out of scope, correctly deferred).
- **A dishonest `date:`.** The date is authored and nothing checks it against the day of the turn —
  `agent-finish.mjs:284` only suggests today's date in its `die()` text. The gate claims exactly "judged
  by each entry's own date", so this is a ceiling, not an overclaim.
- **A heading that matches neither regex** is dropped by `parseAbstracts`, so it is invisible here.
  Pre-existing, and it does not escape: its bullets overwrite the previous abstract's fields, which
  strands the previous entry's body and trips the "every body has an abstract or is archived" case.

`above.date < below.date` on `\d{4}-\d{2}-\d{2}` strings is chronological, both heading regexes pin
that shape, and adjacent-pair checking is equivalent to a global non-increasing check, so there is no
gap between the helper and its title.

## 3. Backward sweep (item 2) — enumerated independently

Grepped every `parseAbstracts`/`newestAbstract`/`.n` reader outside `docs-state.mjs`; the author's
five-row table is complete and its verdicts hold.

| Site | Verdict |
| --- | --- |
| `docs-state.mjs` `newestAbstract` (MAX `.n`, and `.n` is position) | the reason the gate exists — now covered |
| `state.mjs:226`/`:229`/`:240` (§8's newest row, `--rebaseline` audit fields) | covered transitively |
| `docs-budget.test.ts:247` `AWAITING REVIEW` guard, `:325` §8-agrees-with-§7 | covered transitively |
| `frozen-surface.mjs:259`/`:269` `baselineEntryIssues` | read it — `Math.max` plus `find(a => a.n === entry)`, an identity lookup with no order claim. Unaffected, as stated |
| `agent-finish.mjs:280` `find(a => a.id === task && a.seat === seat)` | takes the topmost of a same-id/same-seat pair. Not a defect: the match feeds only the `die()` beside it and is never read after |

## 4. Items 3, 5, 7 and `done-when:`

- **Item 3** — n/a. `outOfDateOrder` is a module-local pure helper; no new entity, collection or
  relationship, so nothing quantifies over "every one of them".
- **Item 5** — every figure carries its method. Re-measured here: `freeze-boundary` **12/12**,
  `docs-budget` **21/21**; `pnpm verify` green in full at finish.
- **Item 7** — ran `detectReservedClasses` locally against `origin/main`: `classes: []`, so
  `RISK: additive` is my own verdict, not a reading of the author's. Diff touches `tests/`, `handoff/`
  and three docs only — no `packages/`, no `scripts/`, no frozen byte, no `SCENE_SCHEMA_VERSION`. On
  CI the labeller is the pr-shape job's **last** step and the job fails if it cannot write the label
  it decided on, so a SUCCESS conclusion on that job makes "no `needs-operator/*` label" a verdict
  rather than silence — confirmed on the tip before merging.
- **`done-when:`, all three box-executable and all three executed by me**: the check reads `date:` and
  never `.n` (§2); both same-date orders pass (part of the 21); the out-of-order fixture fails the new
  check and passes the old one (§1, §2).

## 5. Findings

No defect. One correction, item 4, non-blocking:

- The entry's `FOUND:` and handoff §4 say *"§7 holds eight abstracts dated `2026-08-16`"*. That was
  true of `origin/main` (8 × `08-16`, 2 × `08-17`, measured); on this PR's own tip it is **7 and 3**,
  because the same commit adds a `2026-08-17` abstract and rotates a `2026-08-16` one out. The
  argument it supports — day granularity is the ceiling — is unaffected.

Two observations, neither provable by test and neither blocking, recorded and not filed:

- `docs-state.mjs`'s "SYNTHETIC SORT KEYS" comment cites the newest-first gate as what guarantees
  array order. The author left it as-is on the grounds that the citation is now true; it is true only
  to day granularity, which the comment does not say.
- `frozen-surface.mjs:269` resolves `_baselinedAtEntry` through `.n`, which for a new-scheme entry is a
  position rather than an identity — `1000` will always find whatever is currently on top of §7. The
  date cross-check beside it is what catches a mismatch. Pre-existing, out of this PR's scope.
