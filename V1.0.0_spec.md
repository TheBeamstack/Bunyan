# Bunyan — Browser-Native Parametric BIM/CAD Platform — v1.0.0 Technical Specification

**Status:** Validated (internal)
**Version:** 1.0.0
**Revision:** **2026-07-12b** — supersedes 2026-07-12. Adds **decisions D19–D23: Bunyan is agent-native.** One command layer serves humans *and* AI agents (D19); verbs are type-driven (D20); the agent's tool list is **generated from the registries** (D21); the transport is a zero-install `window.bunyan` API, with MCP deferred to v1.0.x (D22); every command returns its state delta as a verifiable diff, and the query API returns semantics, not triangles (D23). New **§4.6**; **§8a**; `core_logic.md` gains the **agent-operable north-star**. *The reasoning that made this cheap: the document model, the registries and the command layer had not been written yet — this is a greenfield decision on the one layer that does not exist, not a retrofit.*
**Prior revision:** **2026-07-12** — supersedes 2026-07-11. Incorporates the owner rulings from building the real kernel (Entries 3–7 of `current_state.md`): **Bunyan is open source (AGPL-3.0 + commercial dual-licence)**; the kernel is **our own upstream-OCCT 7.9.3 WASM build**, not `opencascade.js`; **v1.0.0 ships single-threaded** (so COOP/COEP and the two-artifact fallback are gone); **IFC import is IfcOpenShell → exact solids**, not `web-ifc` → triangles; and a **`measure` op** is now part of the protocol. Prior: 2026-07-11 (verification re-scoped, golden seeding corrected), 2026-07-08, 2026-07-06.
**Scope:** v1.0.0 — free, open-source, client-only, serverless, static hosting.
**Companion documents:** `core_logic.md` (domain model & long-term north-stars), `architecture.md` (full architecture reference), `v1.0.0_imp_plan.md` (phased build plan), **`current_state.md` (live build status — read it before starting work)**.
**Forward reference only:** v1.1.0+ (optional multi-tenant SaaS backend) — out of scope for this document, but the architecture below is designed not to require rework when that layer is added.

> The most consequential decision remains that **persistent sub-shape naming is a first-class core subsystem (Section 4.5)** — solved from day 1 with functional/generative naming. The 2026-07-08 review made IFC import-only, added the offline PWA, specified operation-failure behaviour, and reframed verification. The 2026-07-11 review re-scoped verification to **"we trust OCCT; we verify our own code"** (D9) and corrected the golden-seeding toolchain to `cadquery-ocp`.
>
> **The 2026-07-12 revision is the largest since the draft, and it comes from actually building the kernel.** Five things changed, all recorded as decisions D14–D18 in Section 14:
> 1. **Bunyan is open source — AGPL-3.0, dual-licensed with a commercial option** (D15). This is now a *product* decision, not a footnote: openness is the wedge against Revit, and AGPL closes the SaaS loophole that plain GPL leaves open.
> 2. **The kernel is our own build of upstream OCCT 7.9.3** (D14). `opencascade.js` is rejected — it cannot even be linked on the build box, it is 3 years stale, and it exposes *all of OCCT* to JavaScript. **We compile our own C++ op set into the WASM; JavaScript never touches OCCT's API.**
> 3. **v1.0.0 ships single-threaded** (D8, rewritten). This **removes** the COOP/COEP cross-origin-isolation requirement, the `SharedArrayBuffer` dependency, and the dual multi-threaded/single-threaded artifact — a large, welcome simplification of Section 8.
> 4. **IFC import is IfcOpenShell, producing exact B-Rep solids** (D16). `web-ifc` is rejected: it returns triangles, and a triangle cannot be measured, sectioned, booleaned or parametrically edited — importing one would break the core invariant that B-Rep is the source of truth.
> 5. **A `measure` op** (D17) — exact volume/area/length from OCCT's `BRepGProp`, never from the mesh.

---

## 1. Vision & Guiding Principles

Build a browser-native, serverless-by-default authoring platform for building design that matches professional CAD/BIM tools (Revit, ArchiCAD) in geometric accuracy, while reaching that parity **progressively** rather than all at once.

Non-negotiable principles for every design decision in this document:

- **Exact geometry, always.** The source of truth for any shape is B-Rep solid geometry from the kernel — never a mesh. Meshes are a display artifact, regenerated on demand.
- **Additive, not disruptive, growth.** Every release from v1.0.0 onward must be able to add a new shape type, a new tool/action, or a new file format without modifying the core engine or breaking existing projects. This is the central architectural requirement — see Section 4.
- **MVP ≠ mini-Revit.** v1.0.0 does not replicate Revit's feature set. It must be genuinely usable for a real (if simple) building-design task, end to end, using a small, well-chosen set of primitives the extensibility model can grow from.
- **Free, open source, zero backend.** Static hosting on Cloudflare Pages. No accounts, no servers. **AGPL-3.0, dual-licensed with a commercial option** (decision D15, Section 11) — openness is a *product* decision, not a compliance detail: it is the wedge against Revit's lock-in.
- **Works offline after first load.** No network dependency is required to *use* the app once it has loaded once: a **service worker precaches the app shell and the WASM kernel** (Section 8), and all documents live locally (Section 7). First load necessarily downloads the app + kernel over the network; every subsequent use — including fully offline — is served from cache.
- **Agent-native by construction** *(decisions D19–D23, Section 4.6)*. An AI agent must be able to **explore, understand and operate Bunyan through instructions — with no SDK to install and no per-feature API wiring.** This is achieved not by building an agent integration, but by refusing to build a *second* API: **humans and agents act through the same Command layer**, the registries that generate the ribbon also generate the agent's tool list, and every command returns the state delta it produced so an action can be **verified**, not merely issued. **The UI has no private path to the kernel, and neither does an agent.** *Strategically this is the same wedge as openness (Section 11): "you will never lose access to your models, you can verify what the geometry engine does — and an agent can drive it out of the box, because there is one public API and it is the same one the buttons use."*
- **No architectural dead ends.** Nothing in v1.0.0 should have to be thrown away when v1.1.0 adds an optional multi-tenant backend and real-time collaboration. The domain model is designed against explicit long-term north-stars — see `core_logic.md`.

---

## 2. Technology Stack

| Layer | Technology | Rationale |
|---|---|---|
| Geometry kernel | **Our own build of upstream OCCT 7.9.3 → WebAssembly** (`tools/kernel-build/`), **single-threaded** *(decisions D14, D8)* | Industrial-grade B-Rep kernel: exact NURBS, booleans, fillets, sweeps, lofts. **We compile OCCT ourselves and statically link it with our own C++ op set** — see the boundary rule below. Multi-threading is deferred to v1.0.x (D8). |
| **The kernel boundary** *(D14 — the architectural point)* | **Our C++ ops are the WASM module. JavaScript never calls OCCT.** | The `.wasm` *is* the kernel: `kernel.cpp` implements `makeBox`/`measure`/`tessellate`/`releaseShape`, statically linked against OCCT; JS calls **our ~10 ops**, not OCCT's API. Consequences: **one JS↔WASM crossing per op** (not per OCCT call); the linker keeps only the OCCT code our ops reach (**3.98 MB / 1.47 MB gzip**, vs 62.8 MB / 13.1 MB for `opencascade.js`); the JS binding surface **never grows** — the product grows by *writing more C++ ops*. Crucially, it is also the only route by which **IfcOpenShell can link against our OCCT** (D16). |
| Persistent naming | **Functional / generative sub-shape naming** — roles assigned in C++ from each operation's own semantics; `nodeId`/`occurrence`/token encoding in TS. Via OCCT `BRepBuilderAPI_MakeShape` `Generated`/`Modified`/`IsDeleted` — core B-Rep, **no OCAF/TNaming** | Sub-shape identity is *derived* from operation-DAG provenance, not *recovered* by matching, so parametric references (openings, fillets) survive rebuilds. See Section 4.5. |
| IFC interoperability | **IfcOpenShell** → **exact B-Rep solids** — **import only in v1.0.0** *(decision D16)* | **`web-ifc` is rejected**: it returns **triangles**, and a triangle cannot be measured, sectioned, booleaned or parametrically edited — an imported model would be a *backdrop*, not a building, breaking the core invariant (Section 1). IfcOpenShell yields **true solids**, and it is C++ that **must link against *our* OCCT build** — possible only because we own the build (D14). **IFC export + embedded-IFC-on-save deferred to v1.0.x.** Target IFC4 (ADD2 TC1); mapping abstraction IFC5/IFCX-ready. ⚠ *IfcOpenShell is LGPL — compatible with our AGPL-3.0 (D15).* |
| Rendering | **three.js**, `WebGPURenderer` (automatic WebGL 2 fallback) | Mature ecosystem, largest body of prior art for CAD-kernel and IFC browser viewers. Compute shaders unlock GPU-side culling/instancing for large models. Custom shading uses **TSL** (compiles to WGSL and GLSL), not raw GLSL, since the legacy `ShaderMaterial`/`EffectComposer` path isn't supported on the WebGPU backend. |
| App shell / UI | **TypeScript + React** | Hot path (modeling, rendering, interaction) must be JS/TS talking directly to the WASM kernel — non-negotiable for interactivity (Section 3). ⚠ **But the UI reaches the kernel only through the Command layer** (D19): no React component holds a `KernelClient`. |
| **Agent surface** *(D19, D22)* | **`window.bunyan`** — the Command layer exposed on the page. **Zero install.** MCP bridge deferred to v1.0.x. | An in-browser agent (e.g. Claude in Chrome) can discover and drive the app with **no SDK and no local process** — the hard requirement. An MCP bridge would be exactly the "complex setup" that is excluded, and it would need a local process, puncturing the client-only/zero-backend promise. **MCP is not rejected, it is deferred** — and it is cheap later precisely because it is *another transport over the same verbs* (D19), not a second API. |
| Client persistence | **File System Access API + OPFS + IndexedDB** | No backend required; see Section 7. |
| Offline & install | **Service Worker + Web App Manifest** | Precaches app shell + versioned WASM keyed by build id so the app runs offline after first load (Section 1, Section 8). Installable PWA. **One artifact to cache, not two** (D8). |
| Hosting | **Cloudflare Pages** (static) | Matches the free, zero-backend constraint. **No COOP/COEP headers needed in v1.0.0** — single-threaded means no `SharedArrayBuffer` (D8, Section 8). |
| **Licence** *(D15)* | **AGPL-3.0, dual-licensed with a commercial option** | Source public; anyone may use, study and fork; a commercial licence is sold to those who need to stay closed (Grafana/Mattermost model). **AGPL, not GPL**, because it closes the **SaaS loophole** — the moat is *hosted collaboration*, not the client code. OCCT (LGPL-2.1) and IfcOpenShell (LGPL) are both compatible. See Section 11. |
| Build & tooling | **Vite + TypeScript** (app bundle) · **Python** (orchestration) | Vite/pnpm build the TS/React app; Python drives WASM build scripts, IFC schema code-gen, CI pipelines, and asset/regression pipelines. |
| Golden-value seeding (offline) | **Python + `cadquery-ocp`** (OCP bindings to a **native OCCT build**) | One-time, **offline** generation of golden reference values (volumes, areas, topology counts) from a native build of the same kernel, used to verify **our** code — our WASM build, our op wiring, our parameters (Section 9). **Not run in CI.** *Corrected 2026-07-11: the plan originally named `pythonocc-core`, which is **not installable from PyPI** (its PyPI entry is an abandoned 0.16 placeholder; real 7.9.x releases are conda-only, and the toolchain is uv-based — Section 12). `cadquery-ocp` wraps the same OCCT 7.9.* |
| Power-user scripting | **Python via Pyodide** (optional, sandboxed, non-hot-path) | Parametric macro/family authoring in the spirit of CadQuery/build123d, exposed as a scripting console — not part of the interactive modeling loop, ships after MVP. See Section 10. |
| Future backend (v1.1.0+, not built now) | **Python (FastAPI)** | Chosen now only so v1.0.0 tooling and the future backend share one language across non-hot-path parts of the stack. |

**Explicitly not in the hot path:** Python, Pyodide, or any interpreted runtime. Real-time dragging/editing of geometry stays in TS ↔ WASM with no extra marshaling layer. Note the interactivity boundary in Section 3 (edit coalescing) — "real-time" applies to viewport manipulation always, and to geometric rebuild subject to a debounce/cancellation budget.

---

## 3. Process & Threading Architecture

```
Main thread                         Geometry Worker                  Optional: Macro Worker
─────────────                       ────────────────                 ──────────────────────
UI (React)                          OUR WASM KERNEL                  Pyodide (lazy-loaded,
Scene graph (three.js)     <──────    = our C++ ops                  only if macro panel
Command dispatcher          msg         + OCCT 7.9.3 (static)        is opened by the user —
Undo/redo stack             ────>   Command executor                 post-MVP, Section 10)
Persistence manager                 Persistent-naming resolver
Service worker (cache)              Tessellation (mesh + provenance)
                                    IFC read (IfcOpenShell → solids)
                                    Operation-failure marshaller
```

- **The WASM module *is* the kernel** (D14). JavaScript calls **our op set** — `makeBox`, `measure`, `tessellate`, `releaseShape`, … — not OCCT's API. Geometry logic lives in C++, inside the module. This is the opposite of `opencascade.js` (which exposes all of OCCT to JS and pays a boundary crossing per OCCT call), and it is what keeps the JS↔WASM surface small and stable however far the product grows: **new capability is a new C++ op, not more binding surface.**
- **Mesh buffers cross the boundary as zero-copy views, not element-by-element.** Reading an embind `std::vector` costs **one crossing per element** — ~1.8 M crossings for a 200k-vertex model, on every drag frame. The kernel hands JS `typed_memory_view`s instead (one crossing), which the TS adapter copies out synchronously. ⚠ Those views are invalidated by the next `tessellate` call.
- The geometry kernel **always** runs in a dedicated Web Worker, communicating via `postMessage` with transferable `ArrayBuffer`s. This keeps the UI thread responsive during expensive boolean/fillet operations regardless of model complexity.
- **v1.0.0 is single-threaded (D8).** Persistent naming (D1) is the project's #1 risk, and multi-threaded OCCT orders boolean output **non-deterministically**, which makes naming bugs far harder to reproduce. **Get correctness right on one core; chase speed in v1.0.x.** This also removes the cross-origin-isolation requirement entirely (Section 8).
- The kernel worker's public API is a **flat, versioned message protocol** (not a live object reference) so it can later be swapped, upgraded, or moved server-side in a future edition without touching the UI layer. This is the seam that keeps the future **headless/server-side kernel** north-star (`core_logic.md`) open.
- **Tessellation carries sub-shape provenance.** Every mesh buffer the worker emits includes a **provenance channel**: a per-triangle → face `SubShapeRef` map and per-edge-polyline → edge `SubShapeRef` map (Section 4.5). This is what makes viewport **sub-shape picking** (select a face/edge) able to return a stable `SubShapeRef` — the *input* side of the naming system. It is part of the frozen kernel protocol (Section 12).
- The persistent-naming resolver (Section 4.5) lives inside the geometry worker, alongside the kernel: it consumes the kernel's `Generated`/`Modified`/`IsDeleted` maker maps in TypeScript today, with a possible future C++ shim for performance. The worker protocol is kept engine-agnostic so that move requires no UI change.
- **Edit coalescing / interactivity budget.** Rapid parameter edits (e.g. dragging a slider or a handle) are **debounced and coalesced**, and an in-flight kernel operation superseded by a newer edit is **cancelled or its result discarded**. OCCT operations are not interruptible mid-call, so the dispatcher must drop stale results rather than block. Viewport camera manipulation never waits on the kernel.
- Pyodide (if/when the macro console is used) runs in its own worker, isolated from the modeling hot path, and is lazy-loaded only on first use so it never affects baseline load time.

---

## 4. Core Extensibility Architecture (the central requirement)

This section answers "the MVP must be 100% scalable through the 1.0.x line until it reaches Revit parity." **Five** registries/subsystems make this possible. Every future feature is an addition to one of these, never a change to the core. The full contracts and their interaction are documented in `architecture.md`; this section is the normative summary.

### 4.1 BIM Object Type Registry

Every modeled thing (wall, slab, door, column, eventually roof, curtain wall, stair, MEP run…) is a BIM Object Type implementing one shared contract:

```ts
interface BimObjectType<Params, Style> {
  typeId: string;                       // e.g. "core.wall.v1"
  defaultParams(): Params;

  // ⚠ D30 — AN ELEMENT IS NOT ONE SOLID. It builds an ORDERED LIST OF PARTS, each one solid with one
  //    material (blockwork | insulation | plaster). A single-solid element is just an element with
  //    one part. This is what makes a QUANTITY answerable at all — see §5.4.
  buildGeometry(params: Params, style: Style, ctx: KernelContext): BuildResult; // BuildResult = Part[] | typed failure (§6.4)

  // ⚠ D31 — WHAT IS SHARED LIVES ON THE STYLE. The layer stack, the section, the materials: a Style
  //    is a named, versioned, SHARED parameter set ("EXT-200-Concrete", "IPE300"). Editing it
  //    rebuilds every instance referencing it. This is Revit's Family Type, and it was missing.
  styleSchema: ParamSchema;             // drives the Style editor; the instance schema below is what is UNIQUE to one element

  parameterSchema: ParamSchema;         // drives auto-generated property panel UI (INSTANCE params only)
  ifcMapping: IfcClassMapping;          // how this maps to/from IFC on import (export: v1.0.x)
  quantities?: QuantityMapping;         // typed quantities — REAL now: a Part carries a Material, and `measure` takes a `ref`
  version: number;                      // for migrating saved projects as the type evolves
}
```

- New types are registered — the core scene graph, persistence format, and undo system never need the concrete list of types in advance.
- `version` + a per-type migration function keeps a v1.0.0 project file loadable after the Wall type gains new parameters in v1.0.4.
- `buildGeometry` returns a `BuildResult` (success with a `ShapeHandle`, or a typed failure — Section 6.4), never throwing across the worker boundary.
- The optional `quantities` field is declared now (a no-op consumer in v1.0.0) so the **analysis & quantities** north-star (`core_logic.md`) does not require a contract change later.
- MVP ships a small number of these (Section 5); everything after v1.0.0 is a new entry.
- **⚠ `buildGeometry` returns PARTS, and this costs the kernel NOTHING (D30).** A `SubShapeRef` is `{nodeId, kind, role, occurrence}` and **`nodeId` is an opaque string**, so a part is simply **its own node in the operation DAG** (`wall-1.structure`, `wall-1.finish`). **No new kernel op, no protocol change, no change to `SubShapeRef`** — which is why parts land before the P5 type freeze **without touching the P3 protocol freeze.**
- **⚠ The anti-fuse rule is unchanged and still absolute:** never fuse two *elements* (it re-owns the first's faces and retroactively breaks every reference hosted on it). **A part is not a second element** — it belongs to one element, is built by its recipe, and dies with it. A boolean **among an element's own parts** (an Opening cutting every layer of its host wall) is an ordinary *intra*-element operation.

### 4.2 Command / Action Registry

Every action **any actor** can take — a human at the ribbon **or an AI agent** (Section 4.6, D19) — implements:

```ts
interface Command {
  commandId: string;                    // e.g. "core.createElement"
  argsSchema: ArgsSchema;               // ⟵ D21. Machine-readable, like a Type's parameterSchema.
                                        //    Generates BOTH the input UI and the AGENT'S TOOL DEFINITION.
  execute(ctx: DocumentContext, args: CommandArgs): CommandResult; // UndoableEdit on success; typed failure otherwise
  contributesUI?: RibbonContribution;   // optional: where it appears in the toolbar/ribbon
}
```

- The undo/redo stack operates purely on `UndoableEdit` objects, not command internals — adding commands never touches the undo system.
- **`execute` RETURNS the `UndoableEdit`** *(D23)*, it does not merely push it. The delta undo needs to *reverse* an action and the diff an actor needs to *verify* it are **the same object** — so verification is free, and an agent can check its own work.
- A command whose kernel work fails returns a **failure** and produces **no** `UndoableEdit` (Section 6.4). The failure is **typed** (one of the protocol's codes), i.e. machine-readable by an agent, not a stack trace.
- The ribbon/toolbar is **generated** from the registry, not hand-wired — a new command appears purely by registering it with a `contributesUI` descriptor.
- **The agent's tool list is generated from the SAME registry** *(D21)*: `argsSchema` is to an agent what `contributesUI` is to the ribbon. **One registry, three consumers** — ribbon, property panel, agent. A new command becomes an agent verb **with no way to forget**, because there is nowhere else for the truth to live.
- ⚠ **`DocumentContext` is the only holder of a `KernelClient`** (D19). No React component, and no agent, may call a kernel op directly (Section 4.6).

### 4.3 File Format Codec Registry

Import/export formats are registered as independent codecs against one interface (`Importer` / `Exporter`), decoupled from the BIM Object Type Registry via the `ifcMapping` field. Adding a format is additive.

- **v1.0.0 ships the IFC *importer*** (**IfcOpenShell → exact B-Rep solids**, decision D16) only; the IFC *exporter* is a sibling registration deferred to v1.0.x (Section 2, decision D3).
- **⚠ AND v1.0.0 SHIPS THE ECOSYSTEM'S TWO NATIVE CODECS (D34)**, because IFC is the door for the **outside world** — it is **not** the ecosystem's internal transport, and it cannot be: **IFC carries neither the parametric recipe, nor a `SubShapeRef`, nor a stable id**, which are the three things the ecosystem runs on.
  - **`.bnn`** — the **model**: full, parametric, round-trippable. **Miqdar** reads *and writes* it.
  - **The Clean Delta Package exporter** — the **change feed**: *what changed since revision N*. **Planitor** consumes it (and so does Miqdar, to re-analyse after an architectural revision). It is **an existing contract** shared with **BIMsync** (`Planitor/v2.2_spec.md` §4) — Bunyan emits it as `source: "bunyan"`, with `change_type` **read from the `UndoableEdit` log** rather than inferred, and `quantity.basis: "exact"` **per Part, per Material**. See §7a.
- The IFC codec's schema binding is abstracted (`IfcSchemaVersion`) so an IFC5/IFCX codec — and later STEP, DXF, glTF — are sibling registrations, not rewrites.

### 4.4 View / Representation Registry

3D view, 2D plan-cut view, 2D elevation, and (future) schedules/sheets are all "representations" of the same underlying document graph, registered independently, so 2D drafting and (north-star) quantity schedules can be added later without touching the 3D authoring core.

### 4.4a Material Library Registry *(decision D33 — new, 2026-07-13)*

**Materials are registered ENTITIES, not strings on objects.** `C25/30`, `S235`, `EPS-80`, `Plaster-15` — each named, versioned, shared, carrying appearance **and physical properties** (density, E, f_ck, f_y, λ).

⚠ **The old model called Material *"a property of an object/type"*, and that quietly foreclosed both consumers it was reserved for.** A material that is a *string* cannot carry `f_ck`, cannot be grouped by a schedule, and cannot be read by an analysis engine — so the concept could not do the two things it existed to do. **A Part references a Material** (§5.4); **a Style's layer stack references Materials** (§4.1); **Miqdar reads their physical properties as solver input.**

### 4.4b Section Catalogue Registry *(decision D33 — new, 2026-07-13)*

**Named, versioned profiles:** `IPE300`, `HEA200`, `RECT-300x600`, rebar bars. A **LinearMember** (§5.1) is swept from one, **via its Style**.

⚠ **A free-form authored loop is not a section.** `IPE300` must be a thing *with a name and an identity* — otherwise it cannot be scheduled ("all IPE300"), cannot be design-grouped by **Miqdar**, and cannot survive a round-trip as anything but coordinates. The authored `Profile` remains the escape hatch for a shape the catalogue does not have.

*(Both are ordinary registries — domain rule 5: new kinds of things are additive registrations. Both are therefore **agent-discoverable for free**, via the same capability endpoint as Types and Commands: D21.)*

### 4.5 Persistent Naming Subsystem *(decision D1)*

**Problem.** When an `Opening` subtracts from a `Wall`, or a `fillet` is applied to an edge, that reference must survive a **rebuild** after the host's parameters change. OCCT booleans do **not** guarantee stable face/edge identity across regeneration. This is the "topological naming problem" that has historically plagued parametric CAD (e.g. FreeCAD). The validated decision is to solve it **from day 1** with a **functional (generative) naming** model rather than the a-posteriori geometric matching most open-source CAD relies on.

**Approach — functional / generative naming.**

- Identity is **assigned at creation and carried forward**, never recovered after the fact. A sub-shape's identity **is its derivation**: the operation that produced it plus the identities of the inputs that produced it (e.g. "the face generated by `Fillet(op#7)` acting on `Edge{generated by Extrude(op#3), role=lateral, k=2}`"). Because the identity is a structural path, it is immutable by construction.
- Propagation uses OCCT's `BRepBuilderAPI_MakeShape` maker interface — `Generated()` / `Modified()` / `IsDeleted()` — which is **core B-Rep** (no OCAF/TNaming dependency). Each operation labels its outputs from its inputs as it builds; there is **no geometric guessing on the resolve path**.
- **Division of labour between C++ and TypeScript** *(as built — `current_state.md` Entry 7)*. The kernel's C++ assigns **roles**: it answers *"which sub-shape is this, structurally?"* — a face's role comes from **the operation's own semantic accessors** (e.g. `BRepPrimAPI_MakeBox::BackFace()`), and an edge's role from **topology** (the canonical pair of the two faces that generate it — floating-point-free, and a bijection for a box). TypeScript answers *"whose is it?"*: it binds those roles to the owning DAG node and encodes the `SubShapeRef` token (`nodeId` + `role` + `occurrence`). **Geometry never enters the identity path in either half.**
- **⚠ The naming layer refuses what it cannot derive.** A sub-shape whose identity cannot be derived structurally (today: an edge not bounded by exactly two named faces) makes the operation **fail loudly** rather than invent an identity. A wrong-but-plausible name is far more expensive than a refusal, because it surfaces only much later — as an opening silently re-targeted onto a different wall.
- **⚠ A translation-table hazard, recorded because it already bit us.** Deriving a role from the operation still means *naming* what the operation returns, and the kernel's vocabulary is not ours. **OCCT's `LeftFace()` is the y-min face and its `FrontFace()` is x-max** — not what the names suggest. A first implementation mislabelled **four of a box's six faces**, and **every geometric check still passed**: the geometry was exact and only the *names* were wrong. The harness therefore **re-measures the role labels on every run** (Section 9.1). This is a check of *our labelling code* against geometry, which is **not** the same as deriving identity from geometry — the derivation is unchanged.
- Every sub-shape reference stored in `scene.json` is an **opaque, stable `SubShapeRef`** — a structured path (`nodeId` + `role` + `occurrence`) through the operation graph, never a raw OCCT index. `SubShapeRef` is an interface; its encoding may evolve within the `1.0.x` line via the same per-type/version migration mechanism as Section 4.1.
- **Hybrid graph granularity.** The operation graph is coarse-grained (feature-level) for the UI and model history, and fine-grained (primitive-op level) for internal topological propagation. References resolve against the fine layer; the user sees the coarse layer.

**⚠ OCCT history quality — SUPERSEDED BY MEASUREMENT, 2026-07-12 (decision D24, `current_state.md` Entry 9).**

> **The paragraph that stood here was a citation, not a measurement — and on OCCT 7.9.3 it is wrong.** It said, from the OCCT docs and the FreeCAD topological-naming literature, that `BRepBuilderAPI_MakeShape` history is *"robust for faces but weakest for edges and vertices generated by boolean section curves"*, and that the resolver's hardest case is chaining a fillet onto an edge produced by a boolean. **We measured it** (`tools/kernel-build/probe.cpp`, committed, re-runnable in ~60 s) rather than continuing to design around a fear. The results:

| Measured on OCCT 7.9.3 | Result |
|---|---|
| **Boolean history (cut through a wall; a circular penetration; a corner fuse)** | **COMPLETE.** Every output face, edge **and vertex** traces to an input. **Zero orphans.** Section edges appear in `Generated()` of **both** intersecting faces; section vertices in `Generated()` of the edges. **The literature's warning does not hold here.** |
| **The relation that is actually missing** | **Identity.** `Modified()` returns only the *splits* of a shape; a sub-shape the operation never touched is passed through as the **same** shape and appears in **no history list at all**. Read naively, that silence looks like *"unknown"*; it means *"unchanged"*. On a simple cut, **four of the wall's six faces** are accounted for by nothing else. |
| **The genuine weak spot** | **The FILLET, not the boolean.** `BRepFilletAPI_MakeFillet` reports its new face as `Generated` from the edge it rounds (exactly what naming needs) but **rebuilds the surrounding edges and vertices as new objects with no history**: 4 edges + 4 vertices lost on a box; 6 + 4 on the boolean case. |
| **The spec's "hardest case" (fillet on a boolean-made edge)** | **Fully nameable.** 23/29 edges from history, 4 from the face pair, **2 from the endpoint tie-break** — and 0 refused. |
| **The sanctioned positional key** (centroid, below) | **NOT NEEDED in any of the six cases** — and it stayed unbuilt until a seventh case (a duct through a **round column**) needed it. **✅ Now BUILT (D28, 2026-07-13):** it is the **third** sort term, consulted only after derivation *and* structural signature have both tied, and it is **the only place geometry touches the identity path.** |
| **⚠ TRANSFORM — rotate, mirror, and a rotate of a wall that already has an opening** *(measured 2026-07-13, Entry 11; probe cases 7-10)* | **A topological ISOMORPHISM.** 100% of output faces AND edges accounted for through history, **1:1, zero orphans**, in both copy modes and for a **mirror** (a negative, handedness-flipping transform, whose solid comes back with **positive** volume). ⇒ `transform` is **pure INHERIT**: it creates **no identities**, and a rotated wall is the **same wall**. |
| **⚠⚠ THE SPLIT FACE — a groove across a wall** *(measured 2026-07-13, Entry 11; probe case 6b)* | **The hole Entry 9 left.** Two faces, one identical derivation — and the kernel **refused**, so a wall with a **chase** in it could not be modelled. **They differ by ADJACENCY** (the lower half touches `z-min`, the upper `z-max`), which is pure topology. Faces now carry the same structural tie-break the edges always had. **See the ⚠⚠ block below.** |
| **⚠ REVOLVE — a full 360° turn** *(measured 2026-07-13, Entry 14; probe cases 12-15)* | **Three surprises, none of them in the literature, and the last one nearly fatal.** (1) A full revolve has **no caps** — and `FirstShape()`/`LastShape()` do **not** return null for it; they return a face that is **not in the result**, so a prism-style cap check refuses every column. (2) Each lateral face **closes on itself**: its seam edge **IS the authored profile edge**, returned by *identity* — so unlike the cylinder, the seam needs no accessor. (3) **⚠ A segment PERPENDICULAR to the axis reports NO HISTORY AT ALL** (`Generated()` empty, `IsDeleted()` true) **while its face sits in the result** — that face is the flat bottom of every column and the base of every dome. It is named from **the circles its segment's endpoint vertices sweep** (pure topology), and the same test distinguishes it from a segment lying *on* the axis, which sweeps no circle and correctly has no face. |
| **⚠⚠ THE GENUINELY SYMMETRIC TIE — HIT AT LAST, and by an ordinary shape** *(measured 2026-07-13, Entry 14; probe case 11b)* | **A duct drilled CLEAN THROUGH a ROUND column.** Every boolean this project ever tested cut a **box**, where a duct enters via `y-min` and leaves via `y-max` — two faces, two derivations, nothing to disambiguate. **A round column has ONE lateral face that wraps all the way around**, so the duct enters and leaves through **the same face**: both rims share a derivation, bound the same two faces, and have endpoint signatures that are identical. **No structural rule can separate them.** The kernel **refused**, loudly and correctly — but a service penetration through a circular column is not exotic, it is Tuesday. ⇒ **This was the signal this section reserved the positional key for. ✅ RULED IN AND BUILT: D28, 2026-07-13 (Entry 15) — see the block below.** |

**⇒ The resolver (implemented, `packages/kernel-occt/src/naming.ts` + `tools/kernel-build/src/kernel.cpp`).** Every output sub-shape is accounted for by exactly one of four relations, tried in order; if none applies the **operation fails**:

| Relation | Meaning |
|---|---|
| **PRIMITIVE** | The operation names it from its **own semantic accessors** (`MakeBox::BackFace()`, `BRepPrim_Cylinder::LateralFace()`). Primitives have no ancestors, so nothing else could. |
| **INHERIT** | One ancestor **of the same kind**, and this is its only output ⇒ it **IS** that sub-shape still, and **keeps its token verbatim**. ⚠ **This is the relation that makes BIM work:** cutting a window leaves the wall's front face `Modified` but still *one* face, so it stays `wall-1/face/y-min#0` and a window already hosted on it is **not re-targeted**. It also covers the pass-through case OCCT reports nowhere. |
| **DERIVE** | It has ancestors but is none of them — a **split**, a coplanar **merge**, or a wholly new sub-shape **generated** by them (a section curve; a fillet's face). Named as *the operation applied to its canonically-sorted ancestors*, plus an occurrence index. ⚠ **The occurrence index is assigned by a structural tie-break, never by traversal order** — see *"Splits"* below, and the ⚠ note under it. |
| **ADJACENT** | No history, and not a pass-through — OCCT rebuilt it (the fillet). An edge is named by **the pair of faces bounding it**; those faces are named, so the name stays structural and FP-free. If two edges share a face pair, the **endpoint vertices** (themselves named by the faces meeting at them) break the tie. |

**⚠⚠ THE SPLIT FACE — a hole in the above, found 2026-07-13 (Entry 11) and now closed. Read this before touching the resolver.**

> The four relations were shipped in Entry 9 with the *edges* given a structural tie-break (their endpoint signature) and the *faces* given **none**. Faces with an identical derivation were simply **refused**. Entry 9's probe never produced such a case, so the gap was invisible.
>
> **It is trivially reachable, and by an utterly ordinary BIM operation.** A **groove across a wall** — a chase, a rebate, a shadow gap, a recessed band — **splits the wall's front face into two.** Both halves derive from the same parent face, by the same relation, from the same operand. History cannot separate them. **So the kernel refused: a wall with a chase in it could not be modelled at all.**
>
> **The halves are not indistinguishable — they touch DIFFERENT faces.** MEASURED (`probe.cpp` case 6b, the `touches` field): the half below the groove meets the wall's `z-min` face; the half above meets `z-max`. That is **pure topology** — no coordinate, no tolerance, nothing that drifts when the wall is resized or the groove is moved.
>
> **⇒ Faces now get exactly the tie-break the edges always had:** sort by derivation, then by a **structural signature** (the *derivations of the neighbouring faces*); assign the occurrence index in that order; and **refuse only when derivation AND signature are both identical.** This is not a new rule — **it is the rule this section already required** (*"children are ordered by a structural tie-break"*, below); it had simply never been implemented for faces.
>
> **The refusal that remained was the genuinely symmetric tie** — and *that* is the case the positional key was always reserved for. It fired in Entry 14 (a duct through a **round column**; it turned out to be two **edges**, not two faces) and the key was **ruled in and built** in Entry 15 (**D28**). What still refuses is a **collision on the mm grid itself**, which is a determinism defect (§6.5), not a naming gap.

- **The seam.** A full cylinder's lateral face closes on itself, so its seam edge is bounded by **one** face twice — no face *pair* exists. Only the operation can name it, and it does (`lateral.seam`). ⚠ **This, not the boolean, is the first wall the old kernel hit:** it refused to build a cylinder at all.
- **v1.0.0 still constrains itself to operations whose coverage is verified** (the naming regression suite, Section 9, is the gate). An operation whose history is inadequate is wrapped or deferred — it does **not** silently fall back to geometric guessing.
**✅✅ RULED AND BUILT — THE BOUNDED POSITIONAL KEY IS IN (D28, owner ruling 2026-07-13; Entry 15). A DUCT NOW GOES CLEAN THROUGH A ROUND COLUMN.**

> The line below says: *"If a real model ever hits a genuinely symmetric tie, that refusal is the signal to implement the key **deliberately**."* **It has.**
>
> **A duct drilled clean through a ROUND column.** Not a contrived shape — a service penetration through a circular column, which every building has. Measured (`probe.cpp`, case `cut_round_column_by_duct`): the column's lateral face **wraps all the way around**, so the duct enters and leaves through **the same face**. The two rims are `Generated` by **the same face pair**, bound **the same two faces**, and their endpoints carry **the same signature**. Where the groove's two halves touched *different* faces (Entry 11's structural rescue), these two rims touch *nothing that differs*. **They are topologically indistinguishable.** No adjacency rule, no endpoint rule, no neighbour signature reaches them.
>
> ⚠ **It has NOTHING to do with `revolve`, and predates it.** The identical failure occurs on a plain `makeCylinder` column and has been reachable since **Entry 9**. It went unseen for five entries because **every boolean ever tested cut a box** — where the duct enters via `y-min` and leaves via `y-max`, two different faces, two different derivations, no tie to break. It surfaced only when a revolve test cut a *round* column, which is the third time this project has learned the same lesson: **the probe only measures the shapes you think to cut.**
>
> **The owner ruled the key IN on 2026-07-13, and it is built** (`kernel.cpp`, `centroidKey`; D28). The refusal is gone: a duct now cuts clean through a round column, and the two rims come back as `#0` and `#1` — one derivation, two identities.
>
> **What the key is, and what it is NOT.** It orders two sub-shapes by their **mm-rounded centroid**, lexicographically, and it is consulted **ONLY as the third sort term** — after the derivation and the structural signature have *both* tied. **It never identifies a sub-shape; it only breaks a tie between two the kernel has already proven interchangeable.** A collision on the grid is a **determinism defect** (§6.5) and is **refused**, exactly as this case was refused before the key existed.
>
> **⚠ THE RISK THE RULING ACCEPTED, WRITTEN DOWN SO NOBODY REDISCOVERS IT:** re-authoring a recipe so the duct crosses to the **other side** of the column swaps which rim sorts first — and a reference into one rim would then resolve to the other. That is inherent to any positional key, it is why this one is bounded, and it is why the decision was the Architect's.
>
> **⚠ AND WHY IT IS SMALLER THAN IT SOUNDS — a structural fact, not a hope.** `transform` is **pure INHERIT** and mints no identities at all (D25), so the key is computed in the element's **own build frame**. **Moving or rotating a column anywhere in the world therefore cannot re-rank a tie** — a moved column is the same column, rim for rim, and the key never even runs. Only an edit to the duct's placement *within* the column can flip it. Both halves are pinned by tests (`naming-revolve.test.ts`).
>
> **This is the ONLY place in the system where geometry touches the identity path.** `naming.ts` still reads no coordinate, and the rule there is unchanged.

- **✅ The `centroid` positional key is the sole sanctioned last resort, and it is now BUILT (D28, 2026-07-13).** It was withheld for six entries on the correct grounds that an untested geometric rule on the identity path could fire by accident on a case nobody had looked at — and it was released only when a **real, ordinary model hit the genuinely symmetric tie** and the Architect ruled. It fires **only** where structure has tied twice over, and a collision on the mm grid is still **refused** (§6.5), not tolerated.
  - ⚠ **The prime suspect was the MIRROR, and it was acquitted — by measurement** (Entry 11). A mirror is a **topological isomorphism**: every face maps to exactly one face, so `transform` is **pure INHERIT** and creates no identities at all. It cannot produce a symmetric tie. **The case that actually got close was the split face** (a groove), and that is separated **structurally**, by adjacency — see the ⚠⚠ block above.
  - ⚠ **And the real one, when it came, was neither.** It was not a face at all: it is **two EDGES** — the rims of a hole drilled through a round column — with the same derivation, the same bounding faces and the same endpoint signature. **The shape of the case was mis-predicted twice** (first the mirror, then "two faces with the same parent and neighbours"). Both guesses were wrong, and the measurement was right. *Do not predict the third one; cut the shape and look.*
  - ⚠ **The one way to force a genuine collision is to ask an incoherent question:** fusing a shape with a **transform of itself**. Both operands then carry identical tokens, and two different faces resolve to one identity. The kernel **refuses**. That is correct — an element instance is in **one** place; a mirrored copy of a wall is a **second wall**, a new element with its own `nodeId`, which the document layer creates and transforms (P3). Given distinct `nodeId`s there is no collision and never was.
- **The fallback contract**, if the general resolver ever proves too costly: constrain the *very first* internal milestone to purely positional openings and non-persisted fillets, but ship v1.0.0 only once `SubShapeRef` is real — retrofitting it later would break `scene.json` and violate the "no dead ends" principle. *(This contract is now unlikely to be needed: the resolver is built and green.)*

**Splits, merges, and determinism.**

- When a parameter change makes one sub-shape split into many (or merge), the maker interface returns a 1→N (or N→1) map. Children are ordered by a **structural tie-break**: the identities of the tool/input entities that caused the split (pure graph data, no geometry). Each operation type defines its ordering rule. **⚠ AS BUILT (Entry 11):** an **edge**'s tie-break is its **endpoint signature** (the faces meeting at each end); a **face**'s is its **neighbour signature** (the derivations of the faces it touches). *Faces went un-tie-broken from Entry 9 until Entry 11 — the rule was specified here and simply not implemented, and a groove across a wall was unmodellable for it. If you add a sub-shape kind, give it a tie-break.*
- For a **genuinely symmetric** split where structural data cannot break the tie, a single **bounded positional key** (centroid rounded to the mm grid, lexicographic) is the *sole* sanctioned last resort — the only place geometry touches the resolve path. **✅ BUILT 2026-07-13 (D28).** See Section 6.5 for the cross-machine determinism rules this key must obey — it obeys them. **⚠ This line guessed the shape of that case WRONG TWICE, and was corrected by measurement both times.** It first said *"e.g. mirrored openings"* — wrong: a mirror is an isomorphism and cannot tie. It then said *"two faces with the same parent and the same neighbours"* — also wrong. **The real case, measured 2026-07-13 (Entry 14), is two EDGES: the rims of a duct drilled through a ROUND column**, which share a derivation, share both bounding faces, and have identical endpoint signatures. **⇒ It was HIT, it is ordinary, and the key was RULED IN and BUILT (D28) — see the block above.** *Do not predict the next one; cut the shape and look.*
- Each operation's `Generated`/`Modified` output is passed through a **canonical re-sort** (the structural tie-break above) **before** IDs are assigned, so **the kernel's traversal order can never leak into a name**. *(Rewritten 2026-07-12: this rule was originally justified by multi-threaded boolean non-determinism. **v1.0.0 is single-threaded (D8), so that justification no longer applies today — but the re-sort remains a hard requirement**, both because OCCT's single-threaded traversal order is still not something identity may depend on, and because it is the precondition for turning multi-threading on in v1.0.x without invalidating a single saved file. It is implemented.)*

**Rebuild, load, and broken references.**

- **⚠ CORRECTED 2026-07-13 (review; owner-ruled).** This line used to say *"the identity **token map** is persisted in `scene.json`… on load, deterministic replay re-binds the stored tokens."* **There is no token map, there never was, and there must not be.** What `scene.json` persists is **the `SubShapeRef` tokens that objects actually hold** — an Opening's `hostRef`, a fillet's target edge — i.e. **the references that were authored**, and nothing else. Every other identity is **re-derived** by replaying the recipe on load, because the derivation is deterministic (§4.5). **Storing the full set would be storing a RESULT**, which §2's invariant forbids and which buys nothing the re-derivation does not already give. *(This claim sat in five places across two documents with zero code behind it — the same shape as the BREP-cache bug of Entry 13. The difference: here **the code was right and the prose was wrong**. Deleted, not built.)* On an edit, only the affected subgraph is rebuilt and identities are re-propagated (incremental rebuild).
  - **⚠ ONE THING IS DEFERRED, DELIBERATELY, AND NAMED SO IT IS NOT LOST:** a consumer **without an OCCT kernel** — i.e. **Miqdar** — cannot enumerate face/edge tokens from a `.bnn` without rebuilding, yet `core_logic.md` §3.15 promises the ecosystem *sub-element* bindings (*"the formwork area of that beam's soffit"*). Nobody has asked for it yet (Planitor binds to elements and **parts**, both derivable from the recipe), and **unlike the change feed it is cheap to add later** — an additive, optional, explicitly-derived key in a JSON file, which the P5 freeze does not make expensive. **Open, deferred, and it is not a token map.**
- On rebuild, each `SubShapeRef` is re-resolved along its derivation path. If a reference becomes **unresolvable** (the host was edited so the referenced sub-shape no longer exists), the dependent feature is marked **broken** and surfaced in the UI for **manual retargeting** — it is **never** silently reattached to a different element.

**Risk (recorded honestly).** This remains the single hardest engineering item in v1.0.0 and can dominate the timeline. It is on the critical path for parametric `Opening` and `fillet`/`chamfer`. It is materially de-risked versus the draft by depending only on the **core maker interface** (no unverified OCAF/TNaming binding) — but the edge/vertex-history caveat above is the residual risk to watch.

**Why this matters concretely:** shipping "add a Door type" or "add a Fillet command" in v1.0.3 should be an additive PR against a registry — not a cross-cutting change. This is what keeps parallel work conflict-free in the core.

---

### 4.6 The Agent Surface — *the command layer is the API* **(decisions D19–D23, owner rulings 2026-07-12)**

**The requirement, in the owner's words:** an AI agent (Claude, or any LLM-based agent) should be able to **explore, understand and operate the app through instructions — with no complex setup: no heavy SDK install, no manual API wiring per feature.**

**The design answer is not to build an agent integration. It is to refuse to build a second API.**

#### 4.6.1 D19 — One command layer. No actor bypasses it.

```
  human (button / drag) ─┐
  agent (window.bunyan) ─├──►  COMMAND REGISTRY (§4.2)  ──►  DocumentContext  ──►  KernelClient ──► OCCT
  MCP bridge [v1.0.x]  ──┘          the only door
```

Every action, from every actor, is a Command. **The UI gets no private channel to the geometry kernel, and neither does an agent.**

> ⚠ **Why "no exceptions" is not pedantry.** A "quick path" from the UI straight to the kernel (for a drag preview, say) is always tempting and always corrosive: **every capability reachable only through the UI is a capability the agent can never have** — and nobody notices the gap until an agent is asked to use it, by which time the two surfaces have diverged for a year. **If the UI can do it, a Command expresses it. If no Command expresses it, the UI cannot do it.**

**⚠ Two layers exist, and only one of them is the agent's.** Confusing them is the expensive mistake available here:

| | **Kernel ops** (`@bunyan/protocol`) | **Document commands** (§4.2) |
|---|---|---|
| Vocabulary | `makeBox`, `tessellate`, `measure` | `createElement`, `setProperty`, `deleteElement`, `queryElements` |
| Level | geometry primitives | BIM semantics |
| Freezes | **end of P3** (D13) | **P5**, after Wall + Opening (D13) |
| Who may call it | **`DocumentContext` only** | **every actor** |

An agent calling a kernel op directly would produce a solid that **no entity owns, no undo can remove, and no `SubShapeRef` names.** The kernel is not an API surface; it is an *implementation* of one.

#### 4.6.2 D20 — Granularity: type-driven verbs; primitives as the escape hatch

The primary verb is **`createElement(typeId, params)`** — *"create a `core.wall.v1`, 4000 mm long, 2500 mm high"* — **not** *"draw a rectangle, then extrude it 2500 mm"*.

This costs nothing, because **a BIM Object Type already *is* the recipe from parameters to geometry** (§4.1). The parametric type registry makes semantic verbs the *natural default*; the sketch-level primitives of §5.2 remain, as the **GenericSolid** escape hatch (§5.1) rather than the main road.

**Composite verbs** ("add a room" = 4 walls + a slab + a door, atomically) are **deferred to v1.0.x** — they require transaction grouping, for which the contract now **reserves room** (D23).

#### 4.6.3 D21 — Capability discovery: generated from the registries, never hand-maintained

The capability endpoint is a **derived projection of the registries.** The registries are already the **only** place a Type or a Command can be declared (§4: *"the core never hard-codes the concrete lists"*) — so a schema generated from them **cannot** drift out of date. **A hand-written agent document certainly would**, and within weeks.

⇒ `Command` gains **`argsSchema`** (§4.2), exactly as `BimObjectType` already carries `parameterSchema`. **An agent needs no external documentation — it asks the app.**

#### 4.6.4 D22 — Transport: `window.bunyan` first. MCP deferred, not rejected.

| | **`window.bunyan` [v1.0.0]** | **MCP bridge [v1.0.x]** |
|---|---|---|
| What it is | the Command layer, exposed on the page | a small **local program** relaying an *external* agent (Claude Desktop / Claude Code) into the browser |
| Serves | an **in-browser** agent (e.g. Claude in Chrome) | agents outside the browser |
| Install cost | **ZERO — it ships with the app** | a download + a config file + a running process |
| Backend | **none** | a local process and a port |

**`window.bunyan` is built first because "no complex setup" is a hard requirement, and an MCP bridge *is* that setup.** It would also puncture the property that defines v1.0.0 — **client-only, zero backend, designs never leave the device** (§1). **MCP is deferred, not rejected**, and it is cheap later *precisely because of D19*: an MCP server is **another transport over the same verb set**, not a second API. (The same move the kernel seam already makes: `Transport` is swappable, and `WorkerTransport`/`InProcessTransport` prove it.)

```ts
window.bunyan = {
  version:      () => ({ agentApi: 1, protocol: 1, kernelBuildId, app }),
  listCommands: () => CommandDescriptor[],  // GENERATED from the Command registry (argsSchema)
  listTypes:    () => TypeDescriptor[],     // GENERATED from the BIM Object Type registry
  execute:      (commandId, args) => CommandResult,  // → UndoableEdit (the diff) | typed failure
  query:        (q) => SemanticView,        // the recipe — ids/types/params/relations, NOT triangles
};
```

**Versioned separately (D22).** The surface carries its own **`agentApi` version**, independent of `protocolVersion` (the kernel) and of the P5 type-contract freeze (D13) — so agent ergonomics may improve **without amending a frozen contract**. An agent asks what it is talking to, and adapts.

#### 4.6.5 D23 — Explore, act, verify

- **Read/query, two tiers.** **Document-level** queries (`getSceneGraph`, `getElementById`, filter by type/level/property) read the **parametric recipe**, which *is* the truth (§1) — **they need no kernel op at all.** **Geometric** queries (tight bounds, spatial proximity/containment) need the B-Rep, so they are **kernel ops, added before the protocol freezes at the end of P3** (adding an op is one line in `OpMap` today; a contract amendment afterwards).
- ⚠ **A query API returns SEMANTICS, not triangles.** A `Float32Array` of 200 k vertices is unreadable to an agent. The agent reads a **semantic Representation** (§4.4) — ids, types, params, levels, quantities, relationships. *The 3D view is the projection for eyes; this is the projection for reasoning. Both are derived; neither is truth.*
- **Every command returns its diff.** `execute` returns the `UndoableEdit` (§4.2). With the exact **`measure`** op (D17 — B-Rep truth, never the mesh) and typed failures (§6.4), an agent has a **closed verification loop: act → read the diff → measure the result.** Most CAD tools cannot tell a caller what an edit actually did.
- **Transactions reserved.** `UndoableEdit` carries a **transaction id**; edits sharing one undo/redo as a **single all-or-nothing unit**. **v1.0.0 emits one edit per command** (each its own transaction) — but the field exists **now**, because retrofitting grouping into a frozen undo contract is invasive and every composite verb will need it (§6.1).
- ⚠ **Agent commands opt OUT of edit-coalescing.** The dispatcher supersedes an in-flight request when a newer one shares its `coalesceKey` (§3) — right for a human dragging a slider, **wrong** for an agent, which issued one deliberate command and would get a `SUPERSEDED` failure that is meaningless in its context. **Coalescing is a property of interactive input, not of commands.**

#### 4.6.6 What this already gets for free (and why it was cheap)

The rulings above were affordable because **the document model, the registries and the command layer had not been written yet** — this is a greenfield decision on the one unbuilt layer, not a retrofit. Four things were *already* in place and simply had to be pointed at the agent:

1. **The renderer was already decoupled** — the kernel is a pure function that knows nothing of the DOM (which is why 62 tests run headlessly). There was no model/view entanglement to unwind, because there is no view yet.
2. **Registries were already the only way to add a type or command**, and were already required to *generate* the UI. Generating an agent tool list is the same mechanism, aimed one step further.
3. **`UndoableEdit` was already a state delta, not a command replay** (D-fold). A delta *is* a diff.
4. **Identity was already stable *below* the entity level** (`SubShapeRef`, §4.5). An agent can say *"a window on the south face of that wall"* and the reference **survives the wall being resized** — the thing most CAD tools cannot promise, and the thing that makes agent-authored parametric edits trustworthy rather than merely possible.

**⚠ Trust boundary.** `window.bunyan` is a **page-global** API — any script in the tab can drive the model. Acceptable in v1.0.0 (no backend, no account, no secret, the user's own tab), and it is **not** a privilege escalation: it exposes exactly what the ribbon exposes, which is the point of D19. It becomes a **real boundary** the moment the page runs code Bunyan did not author — the **Pyodide macro console** (§10) or any plugin surface. **That code must go through the same Command layer, and its capability grant must be decided before it ships, not after** (architecture §10).

**⚠ An agent cannot silently save to a file.** The File System Access API needs a **user gesture** to re-grant write permission after a reload (§7). An agent can author the entire model; **a human still clicks to save**. Autosave to OPFS/IndexedDB is unaffected, so agent work is never *lost* — it is just never written to the user's file without them.

---

## 5. MVP Definition — v1.0.0

The MVP is deliberately **not** a small Revit. It is a small number of primitives and actions chosen because the extensibility model (Section 4) grows directly from them with no rework.

### 5.1 BIM Object Types (v1.0.0)

| Type | Notes |
|---|---|
| **Wall** | Straight, constant-height. **COMPOSITE (D30):** its Style carries an ordered **layer stack** (`{material, thickness}`), so the element builds **Parts** — blockwork + insulation + plaster — not one lump. OCCT extrude + boolean. |
| **Slab / Floor** | Planar boundary + thickness extrude. **Layered, like the Wall** (structural slab + screed + finish). |
| **LinearMember → Beam · Column** | **⚠ ONE CONCEPT, TWO SPECIALISATIONS (D32).** A **Section** (from the catalogue, §4.4b, via its Style) swept along an **axis**, with a **cardinal point / justification** and a rotation. `Beam` and `Column` are the same thing with a different axis — which is exactly how **IFC** sees it (`IfcBeam`/`IfcColumn` are both an `IfcExtrudedAreaSolid` swept along an axis) and how an engineer sees it. **⚠ There was no Beam at all before 2026-07-13, and that made the Miqdar round-trip lossy at launch — see D32.** Kernel cost: **zero** (`extrude` already sweeps a profile along a direction). |
| **Opening** | Boolean-subtracted void in a Wall or Slab, referencing its host via a `SubShapeRef` (Section 4.5). **⚠ It cuts EVERY part of its host** (D30) — a window goes through the plaster, the insulation *and* the blockwork; that is an ordinary *intra*-element boolean. Carries an **anchoring parameter** — `fixed` (absolute offset from a datum edge/corner, the v1.0.0 default), `proportional`, or `centered` — that defines how it repositions when the host is resized (decision D12; domain rationale in `core_logic.md`). The same mechanism will later host real Door/Window types as "Opening + inserted family." |
| **GenericSolid** | Free sketch + extrude/revolve — the escape hatch that keeps the MVP useful for shapes not yet modeled as a named BIM type, and the import target for unmapped IFC (Section 4.3). |
| **Site → Building → Level → Space** | **⚠ THE SPATIAL CONTAINER TREE (D35) — IFC's own decomposition, and Bunyan had ONLY `Level`.** **`Building`** was missing, so **a two-tower project was unmodellable**. **`Space`** (IfcSpace) was missing too — and worse, this spec modelled *"a room"* as a **composite verb** (4 walls + a slab + a door), i.e. **a macro rather than a thing**. A Space carries a name, a number, and an **area/volume computed from the real geometry** — **floor area being architecture's most-scheduled quantity** (paint, ceilings, screed). **Level** keeps its old job (elevation datum; required scaffolding). ⚠⚠ **AND THE TREE *IS* PLANITOR'S LOCATION BREAKDOWN STRUCTURE** (*"Tower B → Level 03 → Zone B3-East"*) — so the LBS now falls out of the authored model for free. |

**Every element also carries a `classification` (D36, ⚠ AMENDED BY D45):** **`loadBearing`** (bool) and its **IFC class**. ⚠ **Miqdar imports *"the structural elements"* — and nothing in Bunyan could say which those are.** A Wall may be a shear wall or a partition: that is a **property, not a type** (IFC carries `LoadBearing` for exactly this reason), and **Miqdar must never guess** — a guess yields a *wrong* structural model rather than a refused one. ⇒ **`loadBearing` IS Miqdar's import filter**, and it is a property of the **member**, never of a layer (plaster is not "load-bearing").

> ### ⚠⚠ **`discipline` IS NOT ON THE ELEMENT — IT IS ON THE PART** *(decision **D45**, owner ruling 2026-07-13)*
>
> It was on the element, and that was **wrong about every element that matters**. *An RC wall is structural **and its plaster is architectural** — and those are **two PARTS of one element** (D30). A slab is structural; the ceiling under it is not.* **An element is almost never of a single discipline**, so declaring one on it states something false about the parts that differ.
>
> **And nothing needed it.** IFC has **no discipline attribute** on an `IfcWall` (it is an MVD/property-set concern); **Miqdar filters on `loadBearing`**; and **Planitor routes work packages *by trade*** — *the plasterer bills plaster*. All three consumers want either the element's `loadBearing` or the **part's** discipline. A *derived* element-level value was considered and **rejected**: it is a lossy summary of data already in the payload, and someone would eventually route off the wrong one.
>
> ⇒ **`discipline` is authored on the Style's LAYER (§4.1), carried by every PART, and required.** A discipline view is a **filter over parts**: ask for *architectural* and you get the ceiling and the wall's plaster skin — **not** the slab, and **not** the RC core. **An Opening has no parts and therefore no discipline** — it is an *absence*; its structural significance is a penetration in **the host's** structural part, which Miqdar reads from the host's own B-Rep. A type with **no style** stamps its parts from its **`defaultDiscipline`** (never inferred from the material: a concrete screed is not structural, a steel handrail is not, a timber shear wall is).
>
> ⚠ **AND IT PROPAGATES INTO THE CLEAN DELTA (§7a): `discipline` is carried PER PART.** *That is what makes "a task binds to the part it actually builds" true* — the concreter's task binds to `structure` and the plasterer's to `finish.interior`, **on the same wall, under the same PEI**. Agreed while the schema is still soft (D36b): **free today, a three-repo amendment once it is signed.**
| **Grid / Axis** | **⚠ LEVEL'S MISSING TWIN (D32).** Named structural axes (`A`,`B`,`C` / `1`,`2`,`3`) and their intersections, as placement hosts — a column sits at **B-3** and *stays* at B-3 when the spacing changes. Level organizes the model vertically; **Grid organizes it horizontally**, and every BIM tool and every structural tool has both. Miqdar lays a frame out on grids. |

**And two shared libraries the types above point AT, not copies they each carry:** the **Material Library** (§4.4a) and the **Section Catalogue** (§4.4b). Plus the **ElementStyle** layer (§4.1) — `EXT-200-Concrete`, `IPE300` — which is where a type's *shared* parameters live.

### 5.2 Actions (v1.0.0)

**Every action below is a Command (§4.2), and therefore automatically an agent verb (§4.6) — there is no separate agent feature list, and there never will be.** The *primary* authoring verbs are type-driven (D20) — `createElement` / `setProperty` / `deleteElement` / `queryElements` against the BIM Object Type registry — and the sketch-level primitives below are the **GenericSolid escape hatch**, not the main road.

- **2D sketching:** line, rectangle, circle, arc, dimension entry (numeric input, **not** full geometric constraint solving yet). Sketches are **validated** (closed, non-self-intersecting) before being handed to extrude/revolve.
- **Extrude**, **boolean** (union/subtract/intersect), **fillet/chamfer** edge (persisted via `SubShapeRef`).
- **Move, rotate, copy, array** (linear/grid).
- **Level management**, object-to-level assignment.
- **Section-cut-based 2D views** — a plan view **and one elevation** — generated from the 3D model as real cuts of the real geometry (itself a proof that the kernel is the source of truth), **not** a separate drafting engine.
- **IFC import** (mapped-or-GenericSolid). *IFC export is v1.0.x.*
- **Save / Open** (local `.bnn`, Section 7).

### 5.4 The element model, concretely — Style, Parts, Materials *(D30/D31/D33, owner rulings 2026-07-13)*

The three rulings are one idea seen from three sides, and they compose:

```
  ElementStyle "EXT-200-Concrete"          <- SHARED. Edit it, 400 walls rebuild. (D31)
      layers: [                                Miqdar's DesignGroup writes HERE.
        { material: "Plaster-15",  t:  15 },
        { material: "Blockwork-200", t: 200 },  <- each layer names a MATERIAL (D33),
        { material: "EPS-80",      t:  80 },       a shared entity with f_ck, E, density
      ]
        |
        v
  Wall instance "wall-1"                   <- UNIQUE: only its axis, height, level, grid refs.
      styleId: "EXT-200-Concrete"
      params:  { start, end, height }
        |
        v  buildGeometry(params, style)
  Parts (ORDERED, one solid each)          <- D30. An element is NOT one solid.
      wall-1.finish.interior   -> Plaster-15
      wall-1.structure         -> Blockwork-200
      wall-1.insulation        -> EPS-80
```

**What this buys, and none of it was possible before:**
- **"How much plaster is on this wall?"** — answerable, because the plaster **is a part with a material**. Against a monolithic solid it is not answerable at any price, and **quantity take-off is a declared north-star that domain rule 8 forbids foreclosing.**
- **"Change every EXT-200 wall to 250mm blockwork"** — one Style edit. This is the most-used operation in Revit and Bunyan could not express it.
- **Miqdar takes the *structural* layer** of a wall and idealizes it, ignoring finishes — which is what a structural engineer actually models.
- **Stairs, railings and curtain walls stop being blocked** — they were all sitting in the non-goals list for the same reason (they are multi-solid elements), so this is **one decision, not five**.

**⚠ AND IT COSTS THE KERNEL NOTHING.** `nodeId` is an opaque string, so a part is **its own DAG node**. **No new op, no protocol change, no `SubShapeRef` change** — the P3 protocol freeze is untouched. It lands in `scene.json` and `BimObjectType`, which freeze at **P5**.

**⚠ The anti-fuse rule survives verbatim.** *Never fuse two **elements*** (that re-owns the first's faces and retroactively breaks every window hosted on it — measured, Entry 12). **A part is not a second element.** An Opening cutting through all three layers of its host is an *intra*-element boolean, which was always allowed.

### 5.3 Explicit non-goals for v1.0.0

Deferred to later 1.0.x releases (all additive registry entries — none require a v1.0.0 redesign): **multi-threaded OCCT** (decision D8 — v1.0.0 is single-threaded, and the canonical re-sort in 4.5 is what keeps that door open); **IFC export & embedded-IFC-on-save** (decision D3); geometry **auto-repair** of failed operations (Section 6.4); a real geometric **constraint solver**; curtain walls; roofs beyond a flat/single-slope slab; stairs/railings as parametric families; **wall-to-wall corner joins/cleanup** (a quantities/display concern, never a fuse — Entry 12); MEP routing; **schedules** (the *quantities* they read are now real — §5.4 — but the schedule **view** is 1.0.x); sheet/print layout; rendering materials/lighting beyond basic shading; the Pyodide **macro console**; multi-user real-time co-editing; cloud storage; code-compliance checking.

> **⚠ WHY THE DEFERRED LIST IS NOW HONEST, WHERE BEFORE IT WAS QUIETLY BLOCKED.** Stairs, railings and curtain walls were all deferred *"as parametric families"* — but they share one property nobody had named: **they are multi-solid elements**, and until D30 the model **could not express one at any price**. They were not deferred; they were **foreclosed**. With **Parts**, they become what this list claims they are: **additive registrations, on schedule, requiring no redesign.** *(The same is true of schedules: they were listed as deferred while the thing they read — a per-material quantity — was unrepresentable.)*

---

## 6. Data Model & File Format

Project files are a single **`.bnn`** package (a zip), so "save locally, re-import later" works with zero backend:

> **⚠ THE FORMAT IS NAMED `.bnn` (renamed from `.bimproj`, 2026-07-13 — D38).** It is **our own format, and we invented it** — as every serious BIM authoring tool must. It is a **zip of documented JSON**, versioned and migratable.
>
> **Why a native format exists at all, when IFC is a real open standard:** **IFC is an *interchange* format, not an *authoring* one.** It stores the *results* of modelling — geometry, properties, relationships — but **it cannot store the parametric recipe, it has no concept of a `SubShapeRef`, and its GlobalIds churn between exports.** Save to IFC and reopen and you have *a building made of frozen shapes*, not *a building you can still edit*. That is why `.rvt`, `.pln` and `.skp` all exist too. **IFC remains the door to the outside world** (import v1.0.0; export v1.0.x, D3) — it is simply not the thing that can *hold* a Bunyan model.
>
> **And it is not lock-in:** the source is **AGPL** (D15), `scene.json` is **documented, versioned and migratable**, and **Miqdar — a different product — reads *and writes* it** (Miqdar §3.2), which is the proof it is genuinely readable rather than nominally so. **The recipe is truth**, so the recipe *is* your model; handing you an IFC export instead would be handing you the ashes rather than the fire.

```
project.bnn (zip)
├── manifest.json         # app version, schema version, KERNEL BUILD ID (occt-7.9.3-emcc-6.0.2),
│                         #   list of BIM object type versions used, AND (D34) the MODEL REVISION:
│                         #   { snapshot_number, previous_snapshot_number, lineage, issued_at, issued_by,
│                         #     issued_at_seq }   ⚠ issued_at_seq IS THE JOURNAL POSITION (D40) — without
│                         #     it a "baseline" is not one: "what changed since revision N?" is
│                         #     journal.filter(e => e.seq > revN.issued_at_seq), and that sentence is the
│                         #     entire ecosystem. A revision with no anchor into the log is a date stamp.
│                         # ⚠ SAVING IS NOT ISSUING. A revision is a DELIBERATE act ("this is the model I
│                         #   am handing downstream"). ⚠ AND ISSUING IS A COMMAND (D41) — core.issueRevision
│                         #   — because it is an actor acting on the Document, and rule 9 admits no second
│                         #   path. An agent that can author a building must be able to release one.
├── scene.json            # THE PARAMETRIC TRUTH. The building IS this file.
│                         #   objects:   {id, typeId, styleId, params, containerId, gridRefs[],
│                         #               placement, hostId, hostRef, classification}       (D31/D35/D36)
│                         #               ⚠ id is a PREFIXED ULID — wall-01J8Z3K7Q2 (D44). NOT a counter:
│                         #                 a document-local counter collides between two co-editing peers,
│                         #                 and fixing that needs an id allocator = a backend (D37 forbids
│                         #                 one) ⇒ a counter FORECLOSES a north-star (domain rule 8). A
│                         #                 ULID also makes id REUSE impossible by construction.
│                         #               ⚠ classification is {ifcClass, loadBearing} — discipline is on
│                         #                 the PART (D45), never here.
│                         #   styles:    named SHARED parameter sets — layer stacks {name, materialId,
│                         #              thickness, discipline}, sections                   (D31/D45)
│                         #   materials: the Material library, by id                        (D33)
│                         #   sections:  the Section catalogue, by id                       (D33)
│                         #   containers / grids: the spatial tree and the axes              (D32/D35)
│                         #   brokenRefs: the first-class broken-reference state (domain rule 3)
│                         # ⚠ NO "IDENTITY TOKEN MAP" — see §4.5. Only the tokens objects AUTHOR (hostRef)
│                         #   are stored; the rest is re-derived. Storing them is storing a RESULT.
│                         # ⚠ PARTS ARE NOT STORED (D30): they are BUILT from params+style,
│                         #   exactly as geometry is. The recipe is truth.
├── geometry-cache.brep   # ⚠ PROVISIONAL — NOT BUILT, AND NOT YET DECIDED (D29). See the box below.
├── history.json          # ⚠⚠ THE JOURNAL (D40) — NOT "optional session continuity". This is the
│                         #   ECOSYSTEM'S CHANGE FEED, and change_type is READ from it (domain rule 14).
│                         #   Append-only; every edit carries a monotonic `seq`; NEVER trimmed; an UNDONE
│                         #   edit is journalled as a REVERSAL, never erased (a consumer may already hold
│                         #   the state being reversed from). ⚠ IT IS NOT THE UNDO STACK — that is bounded
│                         #   and an undo POPS it. Conflating the two cost us the moat for a whole phase:
│                         #   see §6.1.
└── thumbnail.png         # generated on save from the active 3D view (Section 6.6)
                          # (v1.0.x adds export.ifc — deferred, decision D3)
```

> ### ⚠⚠ `geometry-cache.brep` — **D29 IS RULED: IT SHIPS IN v1.0.0** *(owner ruling, **2026-07-14**, made WITH the measured number, exactly as the deferral intended)*
>
> **THE MEASUREMENT THAT DECIDED IT** (`tests/document-scale.test.ts`, dev box, real OCCT WASM): a **195-element building cold-loads in 7,334 ms** from `scene.json` alone — **37.6 ms/element, and 100% of it inside OCCT** (our plumbing is 0.2%). It **scales linearly**, so a 2,000-element project is **~75 s**. That is past what a splash screen can hide. *(Zayd recommended dropping it, on the grounds that the same OCCT cost is paid on every interactive edit where no cache can help — see §4j-3. The owner ruled to ship: a 75-second open is a product failure regardless of what else is also slow.)*
>
> **⚠⚠ AND THE HARD PART, WHICH IS NOT PERFORMANCE — IT IS IDENTITY.** A `.brep` stores **shapes, not their names.** Every `SubShapeRef` in Bunyan is **derived by re-running the recipe** (D1: *identity is assigned by the operation that mints it, never recovered afterwards*). **Load a solid from a cache and the recipe never runs** — so no identities are minted, and the window hosted on `wall-7.plaster/face/y-min#0` has **nothing to attach to**. ⇒ the cache must carry the tokens too, which is a **persisted name→shape index**: precisely the "identity token map" that §4.5 **deleted** one session earlier and that D1 forbids.
>
> **⇒ THE RULED DESIGN — and it is what keeps D1 intact rather than quietly repealing it:**
>
> 1. **Bind by CANONICAL ORDER, never by a raw OCCT index.** The tokens are stored in the order of the **same deterministic canonical re-sort the resolver already applies before assigning identities** (D8). A token is therefore re-attached by a rule **derived from the shape itself**, not by trusting a position recorded in a file.
> 2. **VERIFY, THEN TRUST.** `exportBrep` also emits a **`fingerprint`** — a digest over each named sub-shape's measured geometry (area / mm-rounded centroid — the D28 basis). `importBrep` **recomputes it from the shape it actually read** and refuses with **`CACHE_STALE`** on any disagreement.
> 3. **A REFUSAL IS FREE, AND THAT IS THE WHOLE SAFETY ARGUMENT.** The recipe is truth and can always rebuild. A stale, re-ordered, corrupted or **hostile** cache therefore costs *a rebuild* — **never a wrong name.** ⇒ **The cache is a bet the document is always free to abandon.** A silently mis-attached token, by contrast, is a window that moves to a different wall and nobody ever finds out.
>
> **Reserved in the protocol (2026-07-14, before the freeze): `exportBrep` · `importBrep` · failure code `CACHE_STALE`.** ⚠ **Bodies NOT yet written** — the shape is frozen; the implementation is the next Zayd work item. It does **not** block Amer.
>
> **The standing invariants, unchanged and non-negotiable:**
> - **Nothing may assume the file exists.** The **no-cache load path is the PRIMARY one** and must remain supported forever — it is the only path **Miqdar** will ever have (it holds a solver, not an OCCT kernel, so it **cannot** produce a cache even in principle).
> - **A stale or corrupt cache is never treated as truth** — it is discarded and the recipe rebuilds.
> - **It is purely additive to a zip**, so shipping it breaks not a single saved file.
> - ⚠ **The security cost is now paid deliberately, not accidentally:** a `.bnn` is a file a user can be **SENT**, and the `.brep` is **the one part fed as binary to OCCT's deserializer**. `importBrep` must treat its bytes as hostile — bounded, validated, and refused with a typed failure rather than a crash.

- **Units & precision.** All geometry is authored and stored in **millimeters** (project internal unit), double precision. IFC declares the corresponding `IfcSIUnit` (`MILLI`, `METRE`); the mm grid is also the rounding basis for the Section 4.5 positional last-resort tie-break. Large-coordinate robustness is addressed in Section 6.5.
- `scene.json` is the **editable parametric truth**; `geometry-cache.brep` — **if it ships at all (D29, undecided)** — is a performance cache, rebuildable from `scene.json` if missing or stale (a corrupted cache never loses the actual design). ⚠ **The invariant is what matters and it holds either way:** the document must load, and edit, with **no cache present at all**. Today none is, and none ever has been.
- `manifest.json`'s per-type version list drives the Section 4.1 migration mechanism on load. The recorded **kernel build id** — `occt-<occt-version>-emcc-<emcc-version>`, e.g. `occt-7.9.3-emcc-6.0.2` — guards BREP-cache compatibility: if the loading app's kernel build differs, the cache is treated as stale and rebuilt. It changes only when OCCT or the toolchain moves, so it is stable across ordinary releases.
- `scene.json` sub-shape references are opaque `SubShapeRef`s (Section 4.5), never raw OCCT indices.

### 6.1 Undo/redo semantics (decision D-fold)

The undo stack is a stack of `UndoableEdit` **state deltas**, not a blind command replay. Because sub-shape references are stable (`SubShapeRef`) and geometry is deterministically rebuildable from `scene.json`, undo restores the prior parametric state and re-resolves references, avoiding the "replay + nondeterministic boolean topology" hazard. Undo/redo may therefore trigger an incremental kernel rebuild of the affected subgraph (not O(1)); it participates in the same edit-coalescing budget as Section 3. Manual retargeting of a broken reference (Section 4.5) is itself an `UndoableEdit`.

> ### ⚠⚠ THE UNDO STACK IS **NOT** THE CHANGE FEED — AND THIS SENTENCE USED TO SAY IT WAS *(decision **D40**, owner ruling 2026-07-13)*
>
> **What this paragraph said until today:** *"`history.json` persists this stack for **session continuity only**; it is **optional** and **never the source of truth**."* **What §7a and D34 say:** *"`change_type` is **read off the `UndoableEdit` log**"* — the log three products compute schedules and payments from. **Both cannot be true.** An optional, session-continuity-only, never-the-truth stack cannot be the thing the ecosystem runs on — and the implementation faithfully built the *first* sentence, so the change feed came out as a **200-deep undo stack that `undo()` pops from, with no anchor to a revision**. ⇒ *"What changed since revision N?"* **had no answer in the file**, and a Clean Delta producer would have had to **diff two models** — the exact guessing that BIMsync exists to do for foreign models and that Bunyan's whole argument says it never has to. *(The contradiction sat in the spec for months, in two sections written apart. It was found by review, not by reading.)*
>
> **The ruling — TWO STRUCTURES, NOT ONE:**
>
> | | **The undo stack** | **The journal (`history.json`)** |
> |---|---|---|
> | is | a session convenience | **the model's record of what happened** |
> | bounded? | **yes** (a cap is correct — memory) | **NO. Never trimmed.** |
> | an undo | **pops** the edit off it | **appends a REVERSAL.** Nothing is ever erased — a consumer may already hold the state being reversed *from* |
> | ordering | LIFO | **a monotonic `seq` on every edit** |
> | optional? | yes | **NO — it is part of the model transport** |
>
> A **Model Revision** records **`issued_at_seq`**, so the delta is `journal.filter(e => e.seq > revN.issued_at_seq)` — **read, not inferred, by construction.**
>
> ⚠ **Recorded honestly:** the `seq` is a document-scoped monotonic integer, which is sufficient for **v1.0.0 (single-user)**. **Real-time co-editing (a north-star) will need a merge-ordered journal** (a Lamport/vector clock, or a CRDT), and that is *not* solved here — but it is not *foreclosed* either, because `seq` is a field on an edit and the PEIs are already globally unique ULIDs (D44). **Named so the next reader does not mistake silence for a solution.**

**Two properties added by D23 (Section 4.6), both load-bearing for the agent surface:**

- **The delta is returned, not just pushed.** `Command.execute` **returns** its `UndoableEdit`. The state delta undo needs in order to *reverse* an action and the diff an actor needs in order to *verify* it are **the same object** — so verification costs nothing extra, and an agent (or a test, or a future co-editing peer) can check what an edit actually did rather than assume.
- **Transaction grouping is RESERVED, not built.** An `UndoableEdit` carries an optional **transaction id**; edits sharing one undo and redo as **a single all-or-nothing unit**. **v1.0.0 emits exactly one edit per command** (every edit is its own transaction) — the mechanism is *not* implemented. The **field and the semantics are fixed now** because the undo contract freezes at P5 (D13), retrofitting grouping into a frozen contract is invasive, and **every composite verb the product grows toward** ("add a room", "import and place") is a set of edits that must never be left half-applied.

### 6.2 `ShapeHandle` lifecycle & WASM memory management *(new 2026-07-08)*

OCCT objects live on the Emscripten heap and are **not** garbage-collected by JS — each `TopoDS_Shape` and intermediate must be explicitly released. Left unmanaged, a long editing session leaks WASM memory until the tab OOMs. Normative rules:

- A `ShapeHandle` is an opaque token owned by the worker; the main thread never holds raw OCCT pointers.
- The worker maintains an explicit **handle registry** with deterministic ownership: handles referenced by the live document graph (**and by the persisted BREP cache, if D29 lands it**) are retained; all intermediates from a rebuild are freed at the end of that rebuild.
- Superseded geometry from a coalesced/cancelled edit (Section 3) is freed when its result is discarded.
- The worker exposes heap-usage telemetry to the app so a soft budget can warn before exhaustion (Section 8).

### 6.3 (reserved)

### 6.4 Operation-failure contract *(new 2026-07-08, decision D10)*

OCCT operations fail routinely on legitimate user input — a fillet radius larger than the adjacent geometry, a self-intersecting or open profile, a boolean that yields an empty/invalid shape, or an internal `Standard_Failure`. v1.0.0 behavior is specified, not left to chance:

- **Safe marshalling.** The worker wraps every kernel call so an OCCT C++ exception (`Standard_Failure`) or an invalid-result check is caught and returned as a **structured, typed failure** across the message protocol — it never crashes the WASM instance or the tab.
- **Reject + keep last-good.** A failed operation produces **no** `UndoableEdit`; the document remains at its last valid state. The user sees a typed, dismissible error explaining what failed (e.g. "fillet radius too large for edge").
- **No silent geometry.** v1.0.0 does **not** attempt auto-repair (`ShapeFix`, tolerance relaxation, retry). Auto-repair is a **flagged v1.0.x** refinement, as is a **downloadable repro bundle** (`scene.json` + the failing op) for field diagnostics — both are recorded as deferred work items here so they are not forgotten (there is no backend telemetry channel, so the repro bundle is the intended field-diagnostics path).

### 6.5 Cross-machine & large-coordinate determinism *(new 2026-07-08)*

The two build actors run different OCCT builds on different hardware (Section 12), and CI checks values seeded elsewhere. Determinism rules:

- **Structural IDs are floating-point-free.** The adjacency-graph hash (Section 9) is computed over structural `SubShapeRef` strings, so it is reproducible across machines by construction.
- **The positional tie-break is the only FP-sensitive identity path — ✅ BUILT 2026-07-13 (D28), and it obeys every rule below.** Its mm-grid rounding uses a **fixed rounding mode** (`llround` — round-half-away-from-zero, guaranteed identical on both build actors' platforms) and the **millimetre grid**, which is the unit everything is authored in (§6.1); the key is therefore an **integer triple**, compared **exactly**, with no epsilon anywhere. The golden-seeding environment is **pinned** (documented OCCT build + platform) so the seeded keys are reproducible. **If two distinct sub-shapes round to the same key, that is a modelling-determinism defect and the kernel REFUSES** — `UNRESOLVED_SUBSHAPE_REF`, exactly as it did before the key existed. It is surfaced, never silently tolerated. ⚠ **And the key is computed in the ELEMENT'S OWN BUILD FRAME, not world space** — which is what keeps it stable: `transform` is pure INHERIT and mints no identities (D25), so moving or rotating an element in the world **cannot re-rank a tie**. Implementation: `kernel.cpp`, `centroidKey`.
- **Large coordinates.** Buildings modelled in mm produce large coordinate magnitudes (a 100 m span = 100 000 mm) against OCCT's default linear tolerance, which can stress boolean robustness for fine (mm-scale) features on large models. v1.0.0 documents a working-precision policy and the naming/golden suites include at least one large-extent model to catch regressions.

### 6.6 Thumbnails

`thumbnail.png` is generated on explicit save by rendering the active 3D view offscreen at a fixed small resolution; it is a convenience artifact only and never affects load.

---

## 7. Persistence Architecture (no backend)

| Mechanism | Purpose | Browser support |
|---|---|---|
| **File System Access API** (`showSaveFilePicker`/`createWritable`) | Primary Save / Save As to a real local file, with a stored handle for silent re-save **within the same session** | Chrome/Edge (Chromium) |
| **Zip-blob download + `<input type=file>` import** | Universal fallback Save As / Open, works everywhere | All browsers |
| **OPFS** (Origin Private File System) | Fast local scratch storage for the geometry cache during a session | All modern browsers |
| **IndexedDB** | Autosave snapshots, undo-history persistence, app settings, stored file handles | All modern browsers |
| **Service Worker Cache Storage** | Offline precache of app shell + WASM kernel (Section 8) | All modern browsers |

- **Save-handle permission reality (2026-07-08 correction).** A stored File System Access handle allows silent re-save **within a session**. **After a page reload the browser requires a user gesture to re-grant write permission** (`queryPermission`/`requestPermission` return `prompt`). The UX is therefore a one-click "resume saving to *file*" affordance on the next session, not truly silent cross-session writes. The universal download/upload fallback has no such restriction but is not silent.
- **Autosave.** Autosave writes a lightweight snapshot — `scene.json` (**plus `geometry-cache.brep` only if D29 lands it**) — to OPFS/IndexedDB on an interval **and** after every *N* undoable edits, independent of whether an explicit save file is open. Snapshots are kept as a **capped ring** (the most recent ~5, oldest dropped), and the app requests `navigator.storage.persist()` so this crash-recovery layer resists browser eviction.

---

## 7a. The Ecosystem — identity is the product *(decisions D34–D37, owner rulings 2026-07-13)*

**Bunyan is the authoring head of a four-product ecosystem.** This section exists because the joint between them was **undefined**, and an undefined joint gets invented three times, differently.

| Product | Role | Reads from Bunyan |
|---|---|---|
| **Bunyan** | authors the building — exact B-Rep, **persistent identity** | — |
| **Miqdar** | structural analysis & design | the **model** (`.bnn`) + the **change feed** |
| **Planitor** | 4D/5D construction management (schedule, cost, EVM, resources) | the **change feed** |
| **BIMsync** | BIM collaboration — and **the on-ramp for FOREIGN models** | *(peer producer of the same contract)* |

```
  FOREIGN MODELS (Revit, ArchiCAD, IFC)
        │   identity is LOST on revision → it must be reconstructed
        ▼
     BIMsync ── mints a PEI, fingerprints, HUMAN resolution queue ──┐
                                                                     ├─► CLEAN DELTA ─► Planitor
     BUNYAN  ── identity BY CONSTRUCTION; the queue is always EMPTY ─┘   (one contract)  └─► Miqdar
        │
        └────► .bnn (the full parametric model, round-trip) ─────────────────────────► Miqdar
```

### The claim, and it is structural rather than promotional

> **Re-issue the model, and the schedule does not break.**

Every other BIM tool loses element identity on revision — IFC GlobalIds churn — so when the architect re-issues, **every schedule binding, every quantity and every progress record attached to those elements breaks.** That single failure is why contractors abandon model-driven scheduling and go back to spreadsheets.

**Bunyan's element `id` IS its PEI** (Persistent Element Identity): derived from the recipe DAG, never from geometry (D1/D18), and therefore stable across every rebuild, resize and re-issue. Sub-element bindings use **`SubShapeRef`**, so *"the formwork area of that beam's soffit"* survives the beam being resized.

**⚠⚠ THE PROOF OF WHAT THIS IS WORTH IS THAT SOMEONE BUILT A PLATFORM TO FAKE IT.** BIMsync exists to manufacture, for foreign models, the property Bunyan has for free: it **mints** a PEI, **fingerprints** elements — `H(ifc_class, predefined_type, material, container, geometry_signature, base_quantity)` — to re-link them after GUID churn, classifies each change, and **where it cannot be sure, it puts a human in front of a "confusing-change resolution queue"** which Planitor **blocks on until it reaches zero**. ⇒ **For a Bunyan-authored model, that queue is empty. Always. By construction.** Nothing was lost, so nothing must be guessed back.

**⚠ BIMsync is therefore NOT made redundant — it is the ecosystem's on-ramp for the world's existing models**, which is most of the market. Two producers, **one contract**.

### What Bunyan emits

- **`.bnn`** — the model. Full parametric recipe, stable ids, per-part materials. **Miqdar reads and writes it.**
- **The Clean Delta Package** — the change feed, an **already-existing contract** shared with Planitor and BIMsync (`Planitor/v2.2_spec.md` §4). Bunyan emits it as `source: "bunyan"`, and **strictly stronger than any IFC-based producer can**:
  - `change_type` is **read off the JOURNAL** — Bunyan *knows* the wall moved; it does not diff two models and infer it. ⚠ **Off the JOURNAL (D40), not the undo stack** — an append-only log with a monotonic `seq`, against a Revision that records its **`issued_at_seq`**. *The delta is `journal.filter(e => e.seq > revN.issued_at_seq)`.* **Read the box in §6.1 before touching this**: the first implementation used the undo stack, and the delta was **not computable at all**;
  - **`reidentified` cannot occur**; `split` / `merge` are reported **authoritatively** (they were *commands*);
  - `quantity.basis` is **`exact`** — measured on the B-Rep, **per Part, per Material** (D30/D33). ⚠ **And what cannot be measured is OMITTED, never zeroed** (D45): an unresolvable Material yields exact volume and area and **no mass**;
  - **⚠ `discipline` is carried PER PART, not per element** (**D45**). **This is the field the work-package routing runs on**, and an element-level value cannot express what every real element is: an RC wall is a **structural core with architectural plaster on it**. ⇒ **the concreter's task binds to the `structure` part and the plasterer's to `finish.interior` — of the same wall, under the same PEI.** *That is what "a task binds to the part it actually builds" means, and a per-element discipline made it impossible.* ⚠ **Agreed while the schema is still soft (D36b): free today, a three-repo amendment once it is signed.**

**⚠ THAT LAST LINE DELETES A LAYER OF GUESSING DOWNSTREAM, AND IT IS THE CLEAREST PAYOFF OF D30/D33.** Planitor today reconstructs a steel column's weight through a fallback ladder — *"section area from the IFC profile **or** a parameter; length from a declared quantity **or** the geometry"* — and **hardcodes steel's density at `7850`**, because IFC so often will not tell it. **Bunyan holds every one of those inputs exactly**: the **Section** (§4.4b), the **Material's density** (§4.4a), the axis length, and the **part's true volume**. **The ladder collapses into a lookup.**

### What it costs Bunyan — one new concept

**A Model Revision.** An **issued** snapshot (`snapshot_number`, `previous_snapshot_number`, lineage, `issued_at`) in `manifest.json`. **⚠ Saving is not issuing.** A revision is a deliberate act — *"this is the model I am handing downstream"* — and it is the anchor every delta is computed against. **`.bnn` had no revision concept at all**, and you cannot diff against a file that was never declared a baseline.

### The contract's home *(D36)*

The Clean Delta Package is **one versioned JSON Schema**, and **every repo validates its producer/consumer against it in CI**. The prose is **generated from the schema**, never maintained beside it.

⚠ **This is not tidiness.** Today it is a hand-maintained duplicate that says so in its own text: *"The two share one identical contract… **Any change to it must be made in both files.**"* Bunyan makes it **three** — and **a contract maintained by remembering to edit N files will drift.** This project has already been bitten by that exact class three times (`capabilities` drifting from the handler map for two sessions; the docs describing a shape nobody had cut). **This contract carries money and schedule.** *(It is D21's own rule — "generated, never maintained" — applied across repos.)*

---

## 8. Deployment, Offline & Load Budget

> **⚠ REWRITTEN 2026-07-12 (decision D8 — single-threaded).** This section previously required cross-origin isolation (`COOP`/`COEP`), `SharedArrayBuffer`, **two** WASM artifacts, and boot-time capability detection to choose between them. **All of that is gone.** Shipping single-threaded is not only a naming-determinism decision — it is a large deployment simplification, and it *widens* where the app can run: cross-origin-isolated pages cannot embed non-CORP resources, and `SharedArrayBuffer` is blocked outright in some embedded/enterprise contexts. Bunyan now has **no such constraints**.

- **Cloudflare Pages**, static output only, for v1.0.0.
- **No COOP/COEP headers. No `SharedArrayBuffer`. No capability detection. ONE WASM artifact.** The kernel is single-threaded (D8), so none of it is needed. *(When multi-threading lands in v1.0.x, the headers, the second artifact and the detection all come back — Section 13. The `_headers` mechanism is therefore designed for, but not used by, v1.0.0.)*
- WASM assets served with correct `application/wasm` MIME type (Cloudflare Pages default) and preloaded/streamed to minimize first-load latency.
- **Offline & PWA (D11).** A **service worker** precaches the app shell and the WASM kernel, keyed by the **kernel build id** (`occt-<occt-version>-emcc-<emcc-version>`, Section 6) so a new release invalidates the cache atomically; a **Web App Manifest** makes the app installable. This backs the Section 1 "works offline after first load" principle. **One artifact to cache.**
- **First-load & size budget.** v1.0.0 sets an explicit **first-load size and time budget** (measured in CI on a throttled profile), met with streaming compilation, preloading and code-splitting. Exceeding it blocks release (Section 9).

  **⚠ Current kernel: 14.53 MB raw / 4.22 MB gzip** *(2026-07-13, Entry 12 — `extrude` + `chamfer` added ~0.09 MB gzip, negligible)* — **up from 3.98 MB / 1.47 MB**, because the kernel now reaches OCCT's boolean, fillet, extrema and solid-classifier code (`TKBO`, `TKBool`, `TKFillet`, `TKPrim`). **That 2.8× on the wire is the price of the ops that make it a modeller rather than a box viewer**, and it is mostly irreducible: `-Os` was measured and saves only 1.5% gzip (4.07 MB), so the build stays on `-O2`. Still **3× smaller over the wire than `opencascade.js`** (13.1 MB gzip), because the linker keeps only the OCCT code our ops actually reach (D14).

  **⇒ OPEN, and it needs an owner call before P4 (§13):** 4.22 MB gzip is a real first-load cost on a slow connection. The levers, cheapest first: **(a)** accept it — the service worker caches it once and the app is then offline-first (D11), so the cost is paid on first visit only; **(b)** **lazy-load the kernel** behind the app shell, so the UI paints before the WASM arrives; **(c)** split the kernel into a core module + a booleans/fillet module fetched on first use — real work, and it re-opens the dead-stripping question. **No decision is needed to continue P2/P3**; it is recorded here so it is not discovered under release pressure.

---

## 9. Quality & Testing Strategy

### 9.0 Scope of verification *(decision D9, rewritten 2026-07-11)*

**We trust OCCT. We verify our own code.**

OCCT is an industrial B-Rep kernel with decades of production use. Validating *it* is not Bunyan's job, is not achievable within this project, and would require a second, genuinely independent geometry kernel that we are not going to build or adopt. Every check in this section exists to catch **our** errors:

- a **mis-wired parameter** (we built a 200 mm wall where we meant 2000);
- an **operation applied wrongly** (wrong primitive, wrong order, wrong input shape);
- a **broken or mis-configured WASM build** (our OCCT-in-the-browser behaves unlike a known-good native OCCT);
- a **regression** (something that used to be right no longer is);
- a **mistake in our own measurement/oracle code** (see the worked example below — this is a real category, not a hypothetical).

This scoping is deliberate and it *simplifies* the harness. The prior spec's claim that golden values "certify the geometry is actually right, because they come from an independent kernel" is **withdrawn**: `pythonocc-core`, `cadquery-ocp` and our WASM build are all bindings to **the same OCCT C++ code** — a different *build*, not a different *kernel*. That claim was never true, and pursuing it was never the goal.

### 9.1 The harness

Every BIM Object Type's `buildGeometry` is gated in CI **against the WASM build only** — the OCCT-native Python oracle is **never** run in CI.

Every check runs against **both** the real kernel and the protocol-conformant mock, through the protocol, so the two cannot drift apart.

1. **Reference-build oracle (primary).** Gross **volume / area / length** and **solid / face / edge counts** are seeded **offline** from a **native OCCT build** (`cadquery-ocp`) on a **pinned environment** (Section 6.5), then committed. They are compared against the kernel's **`measure` op** (D17) — exact values from **`BRepGProp`**, *never* derived from the mesh. Because the reference runs the same kernel through a *different binding, a different build and a different code path*, a disagreement means **our WASM build or our wiring is wrong** — which is exactly what we are trying to catch. It cannot, and is not asked to, certify OCCT itself.
   > **Why `measure` must exist (D17).** Measuring the *tessellation* is exact only for **planar-faced** solids. A tessellated cylinder under-reports its volume by the chord error — so a circular Column could not be gated at all, and any quantity schedule built on the mesh would quietly under-bill every column in the project. **Quantities come from the B-Rep, which is the source of truth; the mesh is a disposable projection** (Section 1).
2. **Mesh-fidelity check (planar cases).** The same quantities are **re-derived from the triangles** and required to reproduce the exact values. This is a genuinely *independent* second view: a mesh that is mis-wound, not watertight, or missing a face still yields a **perfect `measure` result**, because `measure` reads the B-Rep and never looks at the mesh. This is the only check that can see the *mesh itself* is wrong. Applies to planar-faced solids only — a curved solid legitimately fails it, which is precisely why `measure`, not this, is the gate.
3. **Closed-form sanity check (where a formula exists).** For shapes with an exact closed form — boxes, extrusions of polygonal profiles, cylinders, and a box-minus-box opening, i.e. essentially every v1.0.0 MVP shape — the analytic value is computed and cross-checked **at seed time**, and the seeder **aborts** on disagreement. Cheap (arithmetic, no kernel), and it guards the layer the reference-build oracle cannot: **our own measurement and seeding code.** Not required where no closed form exists (a fillet on a boolean edge); there, tier 1 stands alone, which is acceptable under §9.0.
4. **Regression snapshots (no-regression only).** **Per-sub-shape mass checksums** (over the naming engine's **in-memory** identity table — ⚠ *not* a persisted "token map": there is none, and §4.5 explains why there must not be) and the **adjacency-graph hash** (topology graph, nodes labelled by structural `SubShapeRef` IDs, canonically serialized, SHA-256) are **self-snapshotted from Bunyan's own engine** — no external tool can produce structural-ID labels. They guard against *drift*, and certify nothing on their own.
5. **Bounds.** Each feature's **tight** `Bnd_Box` is checked against an explicit bounds schema. ⚠ **OCCT's bounding box is *tolerant*, not tight:** `BRepBndLib::Add` enlarges it by the shape's tolerance, so a box sitting exactly on the origin reports `xMin = −1e-7`. The gap must be cleared (`Bnd_Box::SetGap(0)`) to get the tight box the schema means.
6. **⚠ Naming honesty (new 2026-07-12).** The harness **re-measures the role labels** the kernel assigns: a face named `x-min` must actually *be* the face at minimum x. See the §4.5 hazard — a first implementation mislabelled four of a box's six faces while **every numeric check still passed**. Nothing else in the harness can see this class of bug, because the geometry is perfect and only the *names* are wrong.
7. **WASM-heap leak canary.** After a rebuild cycle, the registry's live-handle count is asserted against the **WASM module's own independent count** (spec §6.2). A registry agreeing with itself proves nothing.

The harness also asserts **`SubShapeRef` resolution stability** across parameter changes, and that **the mock and the real kernel emit identical refs** for the same input — the property that makes the mock→kernel swap a no-op for the browser (Section 12).

> **Worked example — why the closed-form tier is retained (2026-07-11, found on its first run).** The closed form said a 3000×200×2500 mm box has **22,800 mm** of edges; OCCT's `BRepGProp::LinearProperties` on the solid said **45,600 mm** — exactly double, because it sums each edge once per adjoining face and every edge of a closed solid touches two. This was **our misuse of the OCCT API**, not an OCCT defect. Without the closed-form check, 45,600 mm would have been committed as the trusted golden, and a *correct* future implementation would have failed CI against it. Fixed by measuring over deduplicated edges.
>
> **This is now a pattern, not an anecdote.** Building the real kernel (2026-07-12) produced **two more of exactly the same shape**: the mislabelled box faces (§4.5) and the tolerant bounding box (tier 5 above). **Every bug this harness has ever caught has been our misreading of an OCCT API — never an OCCT defect.** That is the strongest possible evidence for the §9.0 scoping, and the reason these tiers are not negotiable.

- **Golden seeding + re-seed rule.** Golden values are generated **once, offline**, from `cadquery-ocp` on a **pinned environment** (Section 6.5), then committed. **A new or changed `buildGeometry` MUST ship with re-seeded goldens — CI fails a build that adds geometry without them.** This gate is explicit, and enforced by a machine check, so headless agents don't miss it.
- **Persistent-naming regression suite.** A dedicated corpus of "edit host, verify dependents re-resolve or correctly break" scenarios — the highest-risk subsystem, and the **gate that validates OCCT edge/vertex history coverage** (Section 4.5). This same corpus is the **reference-stability corpus** replayed by the **kernel-migration gate**: a build that upgrades OCCT within the 1.0.x line fails if any reference re-resolves differently.
- **IFC import-fidelity corpus (export deferred).** A growing library of `.ifc` sample files plus Bunyan-authored `.bnn` files, run through **import + mapped-or-GenericSolid** fidelity checks on every CI build. ⚠ **Fidelity here means *solids*, not pictures** (decision D16): an imported element must be measurable, sectionable and booleanable — so the corpus asserts volume/area against the source, not just that something rendered. **Full IFC export round-trip is a v1.0.x gate**, not v1.0.0 (decision D3).
- **Command-level unit tests.** Every Command in the registry (Section 4.2) has isolated tests against a mock `DocumentContext`, independent of the UI, including a **failure-path test** (Section 6.4).
- **Operation-failure tests.** Known-failing operations (over-large fillet, open profile, empty boolean) must surface a typed failure and leave the document at last-good state — no crash, no partial edit.
- **Import hardening tests.** Malformed / hostile `.bnn` (zip) and IFC inputs — decompression size/ratio caps, entry-count limits, schema validation, worker-timeout parsing — must fail safe without hanging or OOM-ing the tab. ⚠ **The `geometry-cache.brep` deserialization path is treated as untrusted input on load — *if the file ships at all* (D29).** Note this cuts BOTH ways: dropping the cache **removes this attack surface entirely**, which is one of the strongest arguments for dropping it. Keeping it means owning this test.
- **Visual regression** against both WebGPU and WebGL2 backends (from the first phase that produces pixels — resolves O5).
- **First-load budget check** (Section 8) runs in CI on a throttled profile and blocks release on regression.

---

## 10. Python Usage — explicit boundaries

| Use | In hot path? | Notes |
|---|---|---|
| Build scripts, WASM build orchestration, CI/CD | No | Standard Python tooling. |
| Golden-value seeding (`cadquery-ocp` → native OCCT) | No | Headless-only, **offline one-time seeding** of golden reference values on a pinned environment; **not run in CI**, never shipped to the browser. Verifies *our* code, not OCCT (Section 9.0). Re-run to re-seed when geometry changes (Section 9). |
| IFC schema / type registry code generation | No | Generates TS interfaces from IFC schema definitions. |
| Power-user macro console (Pyodide; CadQuery/build123d-style scripting) | No — isolated worker, opt-in, lazy-loaded | A genuine feature (script-defined parametric families) and a north-star (`core_logic.md`). **Ships after MVP**; registers outputs as ordinary BIM Object Type Registry entries — no special-casing. |
| Future v1.1.0+ backend (auth, multi-tenant, sync relay) | N/A (server-side) | Not part of v1.0.0; mentioned only for language consistency across phases. |

---

## 11. Licensing — **Bunyan is open source** *(decision D15, owner ruling 2026-07-12)*

> **⚠ THIS SECTION IS FULLY REWRITTEN.** It previously said *"do not build on AGPL-3.0-licensed frameworks"* and treated licensing as a pre-release compliance chore. **Bunyan is now itself AGPL-3.0.** The old guidance is withdrawn.

**The licence: AGPL-3.0, dual-licensed with a commercial option.** The source is public; anyone may use, study and fork it. A **commercial licence** is sold to those who need to stay closed. (Grafana / Mattermost model.)

**Why AGPL and not GPL.** AGPL closes the **SaaS loophole**. Under plain GPL, a rival could take Bunyan, improve it, and run it as a **hosted service** without publishing a line. AGPL compels them to publish. **The moat was never the client code — it is hosted collaboration**, and AGPL is the licence that defends precisely that.

**Why open source at all (the strategic reasoning, recorded so it is not re-litigated).** We cannot out-feature Revit — Autodesk has 30 years, thousands of engineers, and a content ecosystem. **We win where Revit is hated:** lock-in, subscription pricing, an opaque format, a heavy Windows install. Browser-native + IFC-native + no-install is the wedge — and **openness is not a footnote to that wedge, it *is* the wedge** ("you will never lose access to your models, and you can verify what the geometry engine does"). It also buys the one thing a small team cannot build alone: an **ecosystem** (content, plugins, national standards). AEC already has a strong open-source culture — IFC itself, and IfcOpenShell, which we now depend on.

**Dependency compatibility — verified, not assumed:**

| Dependency | Licence | Compatible? |
|---|---|---|
| **OCCT** (statically linked into our `.wasm`) | **LGPL-2.1** | ✅ **Yes.** LGPL-2.1 **§3** expressly permits converting a copy of the library to the ordinary GPL, and states that if a GPL newer than v2 exists, *"you can specify that version instead."* ⇒ OCCT composes with AGPL-3.0. |
| **IfcOpenShell** (D16) | **LGPL** | ✅ Same reasoning. |
| three.js, React, Vite, … | MIT / permissive | ✅ |

**⚠ The static link STANDS — and this is why it matters.** LGPL-2.1's *relink obligation* exists so that a user can rebuild the app against their own modified OCCT. **Public, buildable source satisfies that automatically** (clone the repo, rebuild). ⇒ **Do not build an OCCT side-module, and do not ship object files.** *(An earlier ruling mandated a swappable side-module; it assumed Bunyan would be proprietary and is superseded. It would have cost most of the 9× size advantage of D14, since a swappable module cannot be dead-stripped.)*

**⚠ LGPL still requires prominent attribution** that the product uses OCCT (and IfcOpenShell). This goes in the README **and** the app's About screen. **Not optional.**

**⚠ THE CLA IS A HARD PREREQUISITE, AND IT IS EASY TO GET WRONG BY WAITING.** Dual-licensing only works if the owner holds the rights to **every line**. The moment an external contribution is merged **without a signed CLA**, that contributor holds copyright on it and it can **never** be included in a commercial licence without their permission. **⇒ The CLA (contributors grant the owner a licence including the right to relicense commercially) MUST be in place BEFORE the first external pull request.** A **DCO will not do** — it certifies authorship but grants **no relicensing right**, which would foreclose the business model entirely.

**Timing.** The repo goes **public later, at a milestone** (working geometry + editor) — not immediately. Until then it stays private. **Before it goes public**, the following must land (none of it blocks kernel work): `LICENSE` (AGPL-3.0) · the **CLA** · AGPL file headers · the **OCCT + IfcOpenShell attribution notice** · a `NOTICE` / third-party licence section.

**A bonus that follows from going public:** public GitHub repos get **unlimited Actions minutes** and the **4-vCPU / 16 GB** runners (private repos get 2 vCPU / 7 GB on 2,000 min/month). This retires the "never iterate a build recipe in CI" constraint — **but only once the repo is actually public.**

*(Not legal advice. The compatibility analysis above is ours; a lawyer should confirm before the commercial licence is sold.)*

---

## 12. Build Model & Actor Structure

The real v1.0.0 build is executed by **one human architect + two Claude Code agents**, each mapped to the environment that best fits its work:

| Actor | Environment | Primary ownership |
|---|---|---|
| **Human architect** (you) | — | Registry contracts & spec ownership; MVP scope calls; AEC/domain correctness (type & parameter semantics, IFC mapping validation); cross-cutting review and merge arbitration; final release scope. |
| **Amer** (Claude Code agent) | **Local PC** (browser available for real rendering/interaction testing) | The **browser hot path**: three.js / WebGPURenderer scene, tessellation-to-mesh pipeline (consuming the worker's provenance channel), section-cut views, TSL materials; React app shell, ribbon generation from registries, property panels; Command/interaction registry, undo/redo, input handling; File System Access / OPFS / IndexedDB persistence, `.bnn`, **service worker/PWA**. Owns anything that must be *seen* running in a real browser. |
| **Zayd** (Claude Code agent) | **Hetzner CX23 dev box** (headless, Linux, CI-grade) | The **headless / kernel / tooling** side: the **OCCT→WASM build** (single-threaded, D8) **and the kernel's C++ op set** (`kernel.cpp` — the kernel *is* C++, D14); kernel worker API; Python build orchestration; the **geometry regression harness** (golden snapshots + invariants; offline `cadquery-ocp` seeding on the pinned env); persistent-naming resolver & regression corpus; **IFC importer (IfcOpenShell, linked against our OCCT)**; DevOps / Cloudflare Pages release pipeline and versioning. Owns anything that runs best on a Linux box with no display. **The dev box also runs the owner's live public sites, so heavy runs follow the box-discipline protocol in `v1.0.0_imp_plan.md`.** |

**Coordination contract.** The **seven** registries/subsystems in Section 4 are the seams between actors. **Critical-path note (2026-07-08):** the pre-freeze path P1→P2→P3 is Zayd-heavy and largely serial. To avoid Amer idling, Amer builds the P4 shell against a **kernel mock/fake worker** implementing the protocol before P2's real kernel lands. Contract-freeze timing is **split** (decision D13): the flat message protocol freezes at end of P3 to unblock parallel work; the `BimObjectType` / `Command` / `SubShapeRef` contracts stay **release-candidate** until Wall + Opening validate them early in P5, then freeze. The human architect owns the interface definitions and any change to them.

---

## 13. Version Roadmap (illustrative, to be refined)

| Version | Adds |
|---|---|
| **v1.0.0** | MVP as defined in Section 5, including the Persistent Naming Subsystem (4.5), offline PWA, IFC **import** (exact solids), and the **agent surface** — `window.bunyan`, zero-install (4.6, D19–D23). **Single-threaded** (D8). **⚠ Now also: COMPOSITE elements (Parts, D30), `ElementStyle` (D31), `LinearMember`→Beam·Column + Grid (D32), and the Material/Section registries (D33)** — the modelling layer that separates a BIM tool from a 3D modeller, and the layer the **Miqdar** round-trip needs. |
| **v1.0.x** | **Schedules / quantity take-off views** (the *quantities* they read are real in v1.0.0 — D30/D33 — the schedule **view** is here). **Wall-to-wall corner joins & cleanup** (a quantities/display concern — **never** a fuse, Entry 12). **Multi-threaded OCCT** (D8) — brings back COOP/COEP, `SharedArrayBuffer`, the second WASM artifact and boot-time capability detection (Section 8); the **canonical re-sort** (4.5) is what makes it safe to switch on without invalidating saved files. **IFC export + embedded-IFC-on-save** (IfcOpenShell writer). **Agent surface additions (4.6):** the **MCP bridge** for external agents (another transport over the same verbs — D22), **composite verbs** ("add a room" — D20) and the **transaction grouping** they require (D23, reserved in v1.0.0). New BIM Object Types (Door/Window as real families, Roof, Stair, Curtain Wall); new Commands (geometric constraint solving, advanced fillet/sweep, materials); operation **auto-repair** + repro-bundle; Pyodide macro console ⚠ *(the first in-page code we did not author — its capability grant against the Command layer is decided before it ships, 4.6)*. All additive registry entries. Still free, client-only, open source. |
| **v1.1.0+** | ~~Optional backend: multi-tenant accounts, project sharing/permissions, real-time presence~~ — **⚠ STRUCK (D37): BUNYAN DOES NOT BUILD A BACKEND.** That is a **BIM collaboration platform**, and **BIMsync already is one**. Bunyan stays client-only/serverless (its identity, §7), and BIMsync is the ecosystem's hosting/sharing/permissions/presence layer for Bunyan models *and* foreign ones. *(⚠ Direction, not description: BIMsync's own spec has not been read — see D37's caveat.)* The longer-horizon north-stars (analysis & quantities, real-time B-Rep co-editing, server-side/headless kernel, generative scripting) described in `core_logic.md`. Concurrent co-editing of shared solid geometry is scoped separately — likely locked/sequential editing + live viewing before true simultaneous B-Rep co-editing. |

---

## 14. Resolved Design Decisions (validated in review)

| ID | Question | Resolution |
|---|---|---|
| **D1** | Persistent sub-shape references across rebuilds (topological naming)? | **Functional / generative naming from day 1** (Section 4.5): identity = derivation path via OCCT `Generated`/`Modified`/`IsDeleted` (core B-Rep). **No OCAF/TNaming.** Multi-threaded booleans reconciled via canonical re-sort; symmetric-split last resort is a bounded positional key. OCCT edge/vertex history caveat + mitigation recorded. |
| **D2** | Embedded IFC on save? | **Deferred to v1.0.x** along with IFC export (was "on every explicit/interval save"). v1.0.0 `.bnn` contains no `export.ifc`. Reversed 2026-07-08; the importer is now IfcOpenShell (D16), and its writer is the v1.0.x export path. |
| **D3** | IFC scope & schema for v1.0.0? | **Import only** for v1.0.0 (mapped-or-GenericSolid), **IFC4 (ADD2 TC1)**, codec schema-binding abstracted so **export** and **IFC5/IFCX** are sibling registrations. Export is a v1.0.x gate. |
| **D5** | Internal units & precision? | **Millimeters**, double precision (Section 6); IFC declares `IfcSIUnit` `MILLI`/`METRE`. Large-coordinate policy in Section 6.5. |
| **D6** | Geometry verification strategy? | **Golden snapshots + invariants in CI; the OCCT-native Python oracle is never in CI** (offline one-time seeding only) + re-seed CI gate (Section 9). Refined by **D9**. |
| **D7** | OCCT kernel upgrades within v1.0.x? | **Allowed only behind a migration gate** that replays the reference-stability corpus and fails on any changed re-resolution (Section 9). |
| **D4** | "110-person team"? | A **metaphor**. Replaced by the real actor model in Section 12. |
| **D-fold** | Undo/redo semantics? | `UndoableEdit` **state deltas** with reference re-resolution, not blind command replay (Section 6.1). |
| **D8** | Threading model? *(2026-07-08; **REVERSED 2026-07-12, owner ruling**)* | **v1.0.0 SHIPS SINGLE-THREADED. Multi-threading is deferred to v1.0.x.** *Why:* persistent naming (D1) is the **#1 risk in the project**, and multi-threaded OCCT orders boolean output **non-deterministically** — debugging naming on top of a non-deterministic kernel is strictly harder. **Nail correctness on one core, then chase speed.** *Consequences (all simplifications):* **no COOP/COEP, no `SharedArrayBuffer`, no dual artifact, no capability detection** (Section 8 rewritten) — which also *widens* deployability, since cross-origin isolation restricts what a page may embed and `SharedArrayBuffer` is blocked outright in some contexts. **The canonical re-sort (4.5) is NOT cancelled** — it is implemented, and it is the precondition for enabling MT in v1.0.x without invalidating a single saved file. |
| **D9** | Verification: what are we actually verifying? *(2026-07-08; **rewritten 2026-07-11, owner ruling**)* | **We trust OCCT; we verify our own code** (Section 9.0). The 2026-07-08 "independent kernel / first-correctness" framing is **withdrawn** — `pythonocc-core`/`cadquery-ocp` and our WASM build are the **same OCCT**, so it was never independent, and validating OCCT is explicitly **out of scope**. The harness is: (1) **reference-build oracle** — volume/area/length + topology counts from a **native OCCT build** (`cadquery-ocp`), which catches *our* build/wiring errors; (2) **closed-form sanity check** at seed time where a formula exists, guarding *our own measurement code* (it caught a real defect on its first run — see the §9.1 worked example), not required where no closed form exists; (3) **regression snapshots** (mass-checksum + adjacency-hash, Bunyan-only, drift-only); (4) bounds. |
| **D10** | OCCT operation-failure behavior? *(2026-07-08)* | **Reject + keep last-good**, marshal `Standard_Failure` safely as a typed failure (Section 6.4). Auto-repair + repro-bundle flagged for **v1.0.x**. |
| **D11** | Offline capability? *(2026-07-08)* | **Service worker + PWA** precache app + versioned WASM (Section 8), backing the Section 1 offline principle. In v1.0.0 scope. |
| **D12** | Opening behavior on host resize? *(2026-07-08)* | **Per-opening anchoring parameter** — `fixed` (default) / `proportional` / `centered` (Section 5.1; `core_logic.md`). |
| **D13** | Contract-freeze timing? *(2026-07-08; **the freeze's MEANING settled by the owner 2026-07-13**)* | **Split freeze** (Section 12): protocol frozen at P3; `BimObjectType`/`Command`/`SubShapeRef` release-candidate until Wall+Opening validate them in P5. **✅ WHAT "FROZEN" FORBIDS IS NOW STATED, AND IT IS THE NARROW READING: _ADDING_ A NEW OP IS ADDITIVE AND PERMITTED; _CHANGING AN EXISTING OP'S ENVELOPE OR PAYLOAD_ NEEDS ARCHITECT SIGN-OFF.** That is almost certainly what was always meant — but it was **not what this row said**, and as written it forbade everything, **which foreclosed P6** (nobody noticed for 12 entries; found by Entry 13's audit). P6 needs a **`sectionCut`** op (the 2D plan + elevation: a headline v1.0.0 deliverable and the *proof* that the kernel is the source of truth) and **IFC-import ops** (D16 — IfcOpenShell as new C++ ops, by construction). Under the old wording P6 would have opened by **amending a frozen contract, which makes the freeze theatre**. **⚠ AND THE OWNER TOOK BOTH HALVES, NOT ONE:** the shapes are **ALSO RESERVED NOW** — `sectionCut` and `importIfc` are **declared in `OpMap`/`OP_NAMES` with their payload types**, and **deliberately left unimplemented** (no handler ⇒ absent from `capabilities`, answers `UNKNOWN_OP`; `RESERVED_OPS` in `@bunyan/protocol`, pinned by a test). **Why both:** the re-wording removes the contradiction, but *the expensive thing to get wrong is the PAYLOAD SHAPE, not the body* — and a payload agreed while the protocol is still soft costs nothing, while one agreed after the freeze costs a sign-off and a version bump. **Reserved ≠ missing.** |
| **D14** | Which OCCT→WASM kernel? *(**2026-07-12, owner ruling**)* | **Build upstream OCCT 7.9.3 ourselves** (`tools/kernel-build/`), and **statically link it with our own C++ op set**. **`opencascade.js` is rejected**, on four independent grounds: (1) it **cannot be linked on the build box at all** — its `-flto` object cache forces a whole-program-optimisation link that is OOM-killed even for a *6-symbol* build (proven, not assumed); (2) it is **dormant since March 2023** (OCCT 7.6.2, emscripten pinned at 2022); (3) it exposes **all of OCCT to JavaScript** (8,975 binding objects), which is why it ships 62.8 MB and pays **a boundary crossing per OCCT call**; (4) **it forecloses linking a second C++ library — i.e. IfcOpenShell (D16)**, which is the only route to exact IFC solids. Our build: LTO **off**, **modelling toolkits only** (Visualization/OCAF/DataExchange/Draw dropped), **3.98 MB / 1.47 MB gzip**. **The product grows by writing more C++ ops, not by growing the JS binding surface.** |
| **D15** | Licence & openness? *(**2026-07-12, owner ruling — the biggest of the project**)* | **BUNYAN IS OPEN SOURCE: AGPL-3.0, dual-licensed with a commercial option.** AGPL (not GPL) because it closes the **SaaS loophole** — the moat is *hosted collaboration*, not the client code. **Openness is the wedge against Revit**, not a footnote to it. Compatibility with OCCT/IfcOpenShell (both LGPL) **verified** via LGPL-2.1 §3. **⇒ The static link stands and the OCCT side-module is CANCELLED** (public buildable source discharges LGPL's relink obligation automatically). **⚠ A CLA — not a DCO — must be in place BEFORE the first external PR**, or the commercial licence is foreclosed forever. Repo goes public **at a milestone**, not now. Full reasoning: Section 11. |
| **D16** | IFC import: triangles or solids? *(**2026-07-12, owner ruling**)* | **EXACT B-REP SOLIDS, via IfcOpenShell.** **`web-ifc` is rejected** — it returns **triangles**, which cannot be measured, sectioned, booleaned or parametrically edited. An imported model would be a *backdrop*, not a building, and it would **break the core invariant** that B-Rep is the source of truth (Section 1). IfcOpenShell is C++ and **must link against *our* OCCT build** — possible **only because of D14**. ⇒ **Never adopt a kernel path that forecloses linking a second C++ library.** |
| **D17** | How are quantities measured? *(**2026-07-12, owner-approved**)* | **A `measure` op in the protocol**, backed by OCCT's **`BRepGProp`** — exact volume/area/edge-length + topology counts, **never derived from the mesh**. Measuring a tessellation is exact only for **planar-faced** solids; a tessellated cylinder under-reports its volume by the chord error, so a circular Column could not be gated and every column in a schedule would be under-billed. **Quantities come from the B-Rep; the mesh is a disposable projection.** Added before the P3 protocol freeze, as required. |
| **D18** | Where does sub-shape identity get assigned — C++ or TS? *(**2026-07-12**)* | **Both, split by question.** **C++ assigns ROLES** (*"which sub-shape is this, structurally?"*) — from each operation's own semantic accessors, and from topology for edges. **TS assigns IDENTITY** (*"whose is it?"*) — binding roles to the owning DAG node and encoding the `SubShapeRef` token. Geometry enters neither half. The kernel **refuses to name** what it cannot derive structurally rather than inventing an identity (Section 4.5). |
| **D19** | How does an AI agent operate Bunyan? *(**2026-07-12, owner ruling — the architectural one**)* | **ONE COMMAND LAYER FOR EVERY ACTOR. There is no second API, and no actor bypasses it.** Humans and agents act on the document through **the same Commands** (Section 4.6.1); **`DocumentContext` is the only holder of a `KernelClient`** — no React component and no agent may call a kernel op directly. *Why "no exceptions":* every capability reachable **only** through the UI is a capability the agent can **never** have, and the gap is invisible until an agent is asked to use it. **If the UI can do it, a Command expresses it; if no Command expresses it, the UI cannot do it.** ⚠ **Two layers exist and only one is the agent's:** kernel ops (`makeBox`/`tessellate`, frozen at P3) are an *implementation*, not an API; document commands (`createElement`/`setProperty`, frozen at P5) are the API. An agent calling a kernel op would create a solid **no entity owns, no undo can remove, and no `SubShapeRef` names.** **Promoted to a north-star in `core_logic.md` §9 and a guiding principle in §1** — so no future decision may foreclose it. |
| **D20** | Command granularity for agents? *(**2026-07-12, owner ruling**)* | **TYPE-DRIVEN VERBS by default; primitives are the escape hatch.** The primary verb is **`createElement(typeId, params)`** — *"create a `core.wall.v1`, 4000 mm long"* — not *"draw a rectangle, then extrude it"*. **This is free:** a BIM Object Type already *is* the recipe from parameters to geometry (§4.1), so the parametric registry makes semantic verbs the natural default. Sketch-level primitives (§5.2) remain as the **GenericSolid** escape hatch. **Composite verbs** ("add a room") are **v1.0.x** — they need transaction grouping (D23), for which the contract now reserves room. |
| **D21** | Where does the agent's capability/schema description live? *(**2026-07-12, owner ruling**)* | **GENERATED FROM THE REGISTRIES. Never hand-maintained.** `Command` gains an **`argsSchema`**, exactly as `BimObjectType` already carries a `parameterSchema`. The registries are already the **only** place a type or command can be declared (§4), so a generated schema **cannot drift**; a hand-written agent document certainly would, within weeks. **One registry, three consumers:** `contributesUI` → the ribbon, `parameterSchema` → the property panel, `argsSchema` → **the agent's tool list**. A new command becomes an agent verb **with no way to forget**. ⇒ **An agent needs no external documentation: it asks the app.** |
| **D22** | Agent transport — `window.*` or MCP? *(**2026-07-12, owner ruling**)* | **`window.bunyan` FIRST (zero install). MCP DEFERRED to v1.0.x — not rejected.** *"No complex setup" is a hard requirement, and an MCP bridge **is** that setup* — a download, a config file, and a local process — which would also puncture the property that defines v1.0.0: **client-only, zero backend, designs never leave the device** (§1). `window.bunyan` ships **with the app** and serves an in-browser agent (e.g. Claude in Chrome) with no SDK. **MCP stays cheap precisely because of D19: it is another *transport* over the same verb set, not a second API** (the same move the kernel's swappable `Transport` already proves). **The surface is versioned separately** (`agentApi`), independent of `protocolVersion` and of the P5 type-contract freeze, so agent ergonomics can improve **without amending a frozen contract**. ⚠ It is a **page-global** API — fine in v1.0.0 (no backend/account/secret), but a real trust boundary the moment the page runs code we did not author (**the Pyodide macro console**); that capability grant is decided **before** that code ships. |
| **D23** | How does an agent explore state and verify its own work? *(**2026-07-12, owner ruling**)* | **EXPLORE → ACT → VERIFY, all through the same layer.** (1) **Read/query in two tiers:** document-level queries (`getSceneGraph`/`getElementById`/filter) read the **parametric recipe**, which *is* the truth — **no kernel op needed**; geometric queries (tight bounds, spatial proximity) need the B-Rep and are **kernel ops added BEFORE the protocol freezes at the end of P3** (one line in `OpMap` now; a contract amendment later). (2) ⚠ **Queries return SEMANTICS, not triangles** — a `Float32Array` is unreadable to an agent; the agent reads a semantic **Representation** (§4.4). *The 3D view is the projection for eyes; this is the projection for reasoning — both derived, neither truth.* (3) **Every command RETURNS its `UndoableEdit`** — the delta undo needs to reverse an action and the diff an actor needs to verify it are **the same object**, so with the exact **`measure`** op (D17) and typed failures (§6.4) the agent has a **closed loop: act → read the diff → measure the result.** (4) **Transaction grouping is RESERVED** in `UndoableEdit` (not built): v1.0.0 emits one edit per command, but the field is fixed now because the undo contract freezes at P5 and every composite verb will need it. (5) ⚠ **Agent commands opt OUT of edit-coalescing** — `SUPERSEDED` is meaningless to an agent that issued one deliberate command; **coalescing is a property of interactive input, not of commands.** |
| **D24** | Does OCCT's history actually support persistent naming on hard topology? *(**2026-07-12, MEASURED — not a ruling; the numbers decided it**)* | **YES — and the received wisdom was wrong about WHERE it is weak.** §4.5 previously repeated the OCCT-docs/FreeCAD claim that history is *"weakest for edges and vertices from boolean section curves"*. We **measured** it on OCCT 7.9.3 instead (`tools/kernel-build/probe.cpp`, committed, ~60 s to re-run). **Boolean history is COMPLETE — zero orphans** across a window cut, a circular penetration and a corner fuse; section edges come back in `Generated()` of **both** intersecting faces. **The weak spot is the FILLET**, which rebuilds the edges/vertices *around* what it touches with no history at all. **The relation everyone misses is IDENTITY:** `Modified()` reports only *splits*, so an untouched face is passed through in silence — read as *"unknown"* rather than *"unchanged"*, it makes history look far worse than it is (4 of a wall's 6 faces, on a simple cut). ⇒ **The resolver is four relations — PRIMITIVE / INHERIT / DERIVE / ADJACENT** (§4.5) — and on the spec's designated hardest case (**a fillet on a boolean-made edge**) it names **everything, with no geometric fallback**. **The sanctioned positional key is therefore NOT implemented**: nothing has needed it, and an untested geometric rule on the identity path is a liability, so the resolver **refuses** instead. ⚠ **A cylinder's SEAM — not the boolean — was the first wall**: bounded by one face twice, it has no face *pair*, and the old kernel refused to build a cylinder at all. |
| **D25** | Which geometric query ops ship, and what may they return? *(**2026-07-12** — the D23 mandate, made concrete before the P3 freeze)* | **THREE OPS: `bounds`, `distance`, `classifyPoint`.** `bounds` answers for a whole shape **or for one NAMED sub-shape** (*"where exactly is the south face of that wall?"* — a question only persistent naming makes askable). `distance` returns the minimum separation **plus the two witness points**, so **zero doubles as the clash primitive**. `classifyPoint` returns `inside` / `outside` / **`on`** — the boundary is its own answer, because a wall's surface is exactly where things get hosted. ⚠ **They read the B-REP, never the mesh, and they return millimetres and identities — never triangles.** A tessellated column is *smaller* than the real one by its chord error, so an agent reasoning about clearances from the mesh would confidently under-report every clash in the project. **Implemented in BOTH kernels** — the mock answers them in closed form for a box, so the agent/query path is buildable without the 14 MB wasm. **Placement (`at`) was added to the primitives in the same pass**: without it both solids sit at the origin and a boolean can only bite a **corner** off a wall — a window in the middle, the one operation this product exists to perform, was unreachable. |
| **D26** | How is a Slab, a Column or a GenericSolid actually built — and what names its faces? *(**2026-07-13**, Entry 12 — found by MODELLING A BUILDING, not by reading the protocol)* | **AN `extrude` OP OVER AN AUTHORED PROFILE, plus `chamfer`. Both were in P2 step 1's scope and neither was ever built** — and nothing noticed, because no test had tried to build a floor plate. **`makeBox` cannot express three of the five MVP types** (§5): a **Slab** is *"planar boundary + thickness"*, and a real plate is L-shaped, five-sided, or has a curved edge; a **Column** may have an arbitrary profile; and **GenericSolid** (*"free sketch + extrude/revolve"*) — the escape hatch and the unmapped-IFC import target — has no op behind it at all. **The profile is a closed loop of AUTHORED segments** (line, or a **three-point arc** — three points define exactly one arc, so there is no handedness flag to get backwards and no radius too small for its chord). It is validated **closed and non-self-intersecting** before it reaches a solid op (§5.2), returning a typed **`INVALID_PROFILE`** — a figure-eight boundary is closed and planar and still nonsense. **⚠ THE NAMING RULE, AND IT IS A CONTRACT, NOT AN IMPLEMENTATION DETAIL: the face swept from segment `k` is named `lateral.k`, where `k` is the index in the array THE AUTHOR DREW** — never OCCT's traversal of the wire. That is what lets a user **drag a slab's corner** — moving the boundary and changing every coordinate of two faces — **without re-targeting a single reference into it.** Consequently **reordering the segment array re-targets every reference into the shape**, exactly as renaming a wall would: editing a vertex is safe, permuting the list is not, and a document command must never do it silently. **`chamfer` is the fillet's flat sibling** — same contract (the edge is addressed **by identity**), same resolver, same weak spot. ✅ **`revolve` landed in Entry 14 (D27) — the op set of P2 step 1 is now complete.** |
| **D27** | How is a REVOLVE named — and is it just the prism with a rotation? *(**2026-07-13**, Entry 14 — MEASURED with the probe before a line of the resolver was written)* | **NO. Copying the prism would have shipped a kernel that REFUSES EVERY FULL REVOLVE.** `revolve` completes GenericSolid (*"free sketch + extrude/**revolve**"*, §5) and the P2 step 1 op set, and it carries **the same naming contract as `extrude` (D26): `lateral.k` after the AUTHORED segment**, so re-authoring a column's profile re-targets nothing. But a **full 360° turn is not a partial one with a bigger number**, and all three differences were measured, not guessed (`probe.cpp` cases 12-15, ~60 s to re-run): **(1) it has NO CAPS** — and OCCT's `FirstShape()`/`LastShape()` do **not** return null for it, they return a face **that is not in the result**, so the test is *"is it in the result"*, never *"is it null"*; **(2) every lateral face has a SEAM** (it closes on itself, so no face *pair* bounds the seam edge — the cylinder's problem), but unlike the cylinder **the seam IS the authored profile edge**, returned by *identity*, so it costs nothing: `lateral.k.seam`; **(3) ⚠ a segment PERPENDICULAR to the axis generates NO HISTORY AT ALL** — `Generated()` empty, `IsDeleted()` true — **while its face is right there in the result.** That face is the flat bottom of every column and the base of every dome. It is named from **the circles swept by its segment's own endpoint vertices** — pure topology — and the same relation tells it apart from a segment lying **on** the axis, which sweeps no circle and correctly has no face at all. **Gated by four goldens** (a column that must measure identically to the `makeCylinder` primitive; a hollow tube, whose annuli are the history-less faces; a hemisphere from an arc, whose closed form is 2/3·π·r³; and a 90° control that HAS caps), each checked against **native OCCT and an independent closed form**. |
| **D28** | A real model hit the **genuinely symmetric tie** (a duct through a ROUND column). Implement the bounded positional key, or ship without it? *(**2026-07-13** — **OWNER RULING**; the case was measured in Entry 14, the key built in Entry 15)* | **IMPLEMENT THE BOUNDED POSITIONAL KEY.** §4.5 reserved it and said a genuinely symmetric tie *"is the signal to implement the key **deliberately**"* — and one fired: a service penetration through a circular column, which every building has. A round column has **ONE lateral face that wraps 360°**, so the duct enters and leaves through **the same face**: the two rims share a derivation, bound the same two faces, and have identical endpoint signatures. **Nothing structural separates them** — Entry 11's groove was saved because its halves touched *different* faces; here nothing differs. **The key, exactly as §4.5 + §6.5 bound it:** the sub-shape's **centroid, rounded to the mm grid** (a fixed rounding mode, `llround` — so the comparison is on integers and is FP-free), **lexicographic**, and consulted **ONLY as the third sort term**, after derivation and the structural signature have both tied. **It never *identifies* a sub-shape — it only ORDERS two the kernel has already proven interchangeable.** A collision (two distinct sub-shapes on one mm-grid centroid) is a **determinism defect**, not a fallback: the kernel **refuses**, exactly as it did before the key existed (§6.5). **⚠ THE ACCEPTED RISK, STATED PLAINLY:** re-authoring a recipe so the duct crosses to the other side of the column would **swap which rim sorts first**, and a ref into one rim would land on the other. **⚠ BUT IT IS SMALLER THAN IT LOOKS, AND FOR A STRUCTURAL REASON:** `transform` is **pure INHERIT** and mints no identities (D25), so the key is computed in the element's **own build frame** — **moving or rotating the column in the world cannot re-rank anything.** Only editing the duct's placement *within* the column can. Both are pinned by tests. **This is the ONLY place in Bunyan where geometry touches the identity path** (`kernel.cpp`, `centroidKey`); `naming.ts` still reads no coordinate. |
| **D29** | Does `geometry-cache.brep` ship in **Bunyan v1.0.0** — or is the recipe alone enough? *(**2026-07-13** — **OWNER RULING: DEFERRED, DELIBERATELY**)* | **DECIDE AT P3 STEP 4, WITH THE MEASURED REBUILD COST IN HAND — and until then, nothing may assume the file exists.** **The finding (Entry 13's audit):** P2 step 2 called for a BREP (de)serializer; **zero code was ever written**, the string appeared only in comments, and **no entry said so for eleven entries** — while `.bnn`, the cache-staleness path and the **untrusted-BREP security rule** all went on promising it. **Why deferring is the right call rather than a dodge:** the cache is a **pure optimisation** (the recipe is truth — §2 — so a document can always rebuild), and what it buys is **load time on a real building**. **That number does not exist yet**: there is no document model to measure it with. Ruling on principle now would either add a feature nobody needs or delete one everybody does. **⚠ What the decision must weigh:** dropping it removes the serializer, the staleness path **and an entire attack surface** (a `.bnn` can be *sent* to a user, and the `.brep` is the one part fed as binary to OCCT's deserializer — spec §9's hostile-input rule); keeping it means owning that hardening. **It is purely additive to a zip**, so v1.0.x can add it later without breaking a saved file — the deferral costs nothing and buys a measurement. |
| **D30** | Does an element own ONE solid, or an ordered list of PARTS? *(**2026-07-13** — **OWNER RULING**; the gap was found by studying Bunyan against Revit/ArchiCAD and against Miqdar's needs)* | **AN ELEMENT OWNS AN ORDERED LIST OF PARTS, each one solid with one Material.** A wall is blockwork + insulation + plaster, not a lump. **⚠ THE ONE-SOLID MODEL FORECLOSED A DECLARED NORTH-STAR.** *"How much plaster is on this wall?"* is **unanswerable** against a monolithic solid at any price — and **quantity take-off is a north-star that domain rule 8 forbids foreclosing**. It also starved **Miqdar** (which idealizes a wall's *structural* layer, not its finishes), and it silently **foreclosed stairs, railings and curtain walls** — all multi-solid elements, all sitting in the "deferred" list as though they were merely unscheduled. **One decision, not five.** **⚠ The kernel was already built for this and the domain had not caught up:** the OCCT build deliberately links **TKOffset** — recorded in Entry 4 as *"wall layers"* — from day one. **⚠ COST TO THE KERNEL: ZERO.** `nodeId` is an opaque string, so a part is **its own DAG node** (`wall-1.structure`). **No new op, no protocol change, no `SubShapeRef` change** ⇒ the **P3 protocol freeze is untouched**; this lands in `scene.json` + `BimObjectType`, which freeze at **P5**. **⚠ THE ANTI-FUSE RULE IS UNCHANGED** (Entry 12): never fuse two **elements**. **A part is not a second element** — it belongs to one element, is built by its recipe, and dies with it. An Opening cutting every layer of its host is an ordinary *intra*-element boolean. |
| **D31** | Is there a Type/Style layer between the *kind* of thing and the *instance*? *(**2026-07-13** — **OWNER RULING**)* | **YES — `ElementStyle`: a named, versioned, SHARED parameter set.** Revit has Category → Family → **Type** → Instance; ArchiCAD has **Composites/Profiles/Favourites**. **Bunyan had only two levels** (`BimObjectType` → instance), and that was **the single largest modelling gap between it and the tools it intends to beat.** A Style carries what is **shared** — the **layer stack** (D30), the **Section** (D32/D33), the materials; the instance carries only what is **its own** (its axis, its height). **Editing a Style rebuilds every instance referencing it.** **Three things were impossible without it:** (1) *"change one wall type, update 400 walls"* — the most-used operation in Revit; (2) **scheduling by type**, which is *what a quantity schedule is*; (3) **⚠ Miqdar's `DesignGroup` had nothing to write back into** — an engineer assigns **one section to a GROUP of columns**, and without a Style, Miqdar would have to set it on each instance one at a time, which is exactly the workflow engineers do not use. **The Style IS the DesignGroup's counterpart in Bunyan.** Freezes with `BimObjectType` at **P5**, and adds `styleId` to `scene.json`. **Cheap now; a contract amendment plus a file migration later.** |
| **D32** | Add **Beam**? And is a Column the same concept? And where is Level's horizontal twin? *(**2026-07-13** — **OWNER RULING**)* | **ADD `LinearMember`, SPECIALISED INTO `Beam` AND `Column`. AND ADD `Grid`.** **⚠ THERE WAS NO BEAM TYPE AT ALL, AND THAT MADE THE ECOSYSTEM'S FLAGSHIP WORKFLOW LOSSY AT LAUNCH.** Miqdar's *structure-first* path — engineer designs the frame, exports to Bunyan, architect builds around it — is **mostly beams and columns**. With no Beam, every beam landed in Bunyan as a **`GenericSolid`**: geometrically correct, **semantically dead** — unschedulable, un-re-sectionable, un-round-trippable. **Miqdar's own spec had written this off as *"lossy but correct… ACCEPTED, not a failure"*.** It is not acceptable: **the ecosystem is the product.** **A LinearMember is a Section swept along an AXIS**, with a **cardinal point / justification** (which line of the section the axis runs through — getting this wrong is a classic BIM error) and a rotation. **Beam and Column are the same concept with a different axis** — exactly how **IFC** sees it (`IfcBeam`/`IfcColumn` are both an `IfcExtrudedAreaSolid` swept along an axis) and how an engineer sees it. Shipping them as two unrelated types would mean reconciling them later. **⚠ KERNEL COST: ZERO** — `extrude` already sweeps a `Profile` along a direction. **The reason Beam was missing was never technical.** **`Grid`** is **Level's missing twin**: Level was called *"required scaffolding"* and organizes the model **vertically**; named axes (`A`/`B`/`C`, `1`/`2`/`3`) organize it **horizontally**. A column sits at **B-3** and *stays* there when the spacing changes. Every BIM tool and every structural tool has both; **Miqdar lays a frame out on grids.** |
| **D33** | Are **Material** and **Section** shared entities, or properties/strings? *(**2026-07-13** — **OWNER RULING**)* | **BOTH BECOME FIRST-CLASS REGISTRIES** (§4.4a, §4.4b) — named, versioned, shared, discoverable. Registries are how Bunyan adds *kinds of things* (domain rule 5), so both are **agent-discoverable for free** (D21). **⚠ `core_logic` §3.11 called a Material *"a property of an object/type"*, and that quietly foreclosed BOTH consumers the concept was reserved for.** A material that is a **string on an object** cannot carry `f_ck`/`E`/density, **cannot be grouped by a schedule**, and **cannot be read by an analysis engine** — so the concept could not do the two things it existed to do. A **Part** references a Material (D30); a **Style's layer stack** references Materials (D31); **Miqdar reads their physical properties as solver input.** **A free-form authored loop is not a Section.** `IPE300` must be a thing *with a name and an identity*, or it cannot be scheduled (*"all IPE300"*), cannot be **design-grouped by Miqdar**, and cannot survive a round-trip as anything but coordinates. The authored `Profile` remains the escape hatch for a shape the catalogue lacks. |
| **D34** | How does Bunyan hand its model to the rest of the ecosystem — and what is the binding currency? *(**2026-07-13** — **OWNER RULING**; found by studying Planitor + BIMsync)* | **`.bnn` IS THE NATIVE MODEL TRANSPORT; THE *CLEAN DELTA PACKAGE* IS THE NATIVE CHANGE FEED; AND BUNYAN IS A FIRST-CLASS PRODUCER OF BOTH. IFC IS THE DOOR FOR THE OUTSIDE WORLD, NOT THE ECOSYSTEM'S INTERNAL TRANSPORT.** **⚠ The joint did not exist.** Bunyan v1.0.0 is **IFC import ONLY** (D3) ⇒ **the authoring tool at the centre of the ecosystem could not hand its model to Planitor at all.** **The Clean Delta Package already exists** as a contract shared by **Planitor** and **BIMsync** (`Planitor/v2.2_spec.md` §4). Bunyan does not invent a transport — **it emits the one that exists**, as `source: "bunyan"`, and **in a strictly stronger form**: `pei` **is** the element's `id` (stable by construction — never minted, never fingerprint-matched); `change_type` is **read off the `UndoableEdit` log**, not inferred by diffing two models; **`reidentified` cannot occur** and `split`/`merge` are reported **authoritatively** (they were *commands*); and `quantity.basis` is **`exact`** — measured on the B-Rep, **per Part, per Material** (D30/D33). **⚠⚠ WHY THIS IS THE MOAT, AND IT IS A STRUCTURAL FACT:** everyone else's GlobalIds **churn on revision**, so a re-issued model **breaks every schedule binding, every quantity and every progress record** — the failure that makes contractors abandon 4D. **BIMsync is an entire platform built to manufacture, for foreign models, the property Bunyan has by construction** (it *mints* a PEI, *fingerprints* to re-link after churn, and where it cannot be sure **puts a human in a "confusing-change resolution queue"**). ⇒ **For a Bunyan model that queue is EMPTY, ALWAYS, BY CONSTRUCTION.** **⚠ It costs Bunyan ONE new concept: a MODEL REVISION** — an *issued* snapshot (`snapshot_number`, `previous`, lineage) in `manifest.json`. **Saving is not issuing.** You cannot compute a delta against a file that was never declared a baseline, and `.bnn` had **no revision concept at all**. |
| **D35** | How far does Bunyan's spatial decomposition go? *(**2026-07-13** — **OWNER RULING**)* | **THE FULL IFC TREE: `Site → Building → Level → Space`.** Bunyan had **only `Level`**. **⚠ It could not express a two-tower project** — `Building` did not exist, and real projects are multi-building. **⚠ And `Space` (IfcSpace) was missing entirely — worse, the spec modelled "a room" as a COMPOSITE VERB** (*4 walls + a slab + a door*), i.e. **as a macro rather than a thing.** A Space is first-class in every BIM tool (Revit *Room*, ArchiCAD *Zone*); it carries a name, a number, and an **area/volume computed from the real geometry** — and **floor area is architecture's most-scheduled quantity** (paint, ceilings, screed, finishes). **⚠⚠ AND THE TREE IS THE ECOSYSTEM'S *LOCATION BREAKDOWN STRUCTURE*.** Planitor schedules against an LBS that reads *"Tower B → Level 03 → Zone B3-East"* — **that is this tree, exactly** — and the Clean Delta carries a `spatial_container_code` per element that must resolve to an LBS node. ⇒ **Planitor's LBS now falls out of the authored model for free instead of being hand-declared.** One concept serves the architect, the engineer and the planner. |
| **D36** | ⚠ **AMENDED BY D45: `discipline` MOVED TO THE PART.** `Classification` is now `{ifcClass, loadBearing}` — an element is almost never of one discipline (an RC wall is a structural core with architectural plaster on it), and **nothing needed an element-level value**: IFC has no such attribute, **Miqdar filters on `loadBearing`**, and **Planitor routes by trade — the plasterer bills plaster.** The rest of D36 stands verbatim. |
| **D36 (original)** | Elements carry no classification — how does anything downstream know what they ARE? And where does the shared contract LIVE? *(**2026-07-13** — **OWNER RULING**)* | **(a) EVERY ELEMENT CARRIES `loadBearing` + `discipline` + its IFC class.** **⚠ Miqdar's spec says it imports *"the structural elements"* — and NOTHING IN BUNYAN COULD ANSWER WHICH THOSE ARE.** A Wall may be a **shear wall or a partition**; that is a **property, not a type** (IFC carries `LoadBearing` for exactly this reason). **Miqdar must never guess** — guessing produces a *wrong structural model* rather than a refused one, which is the one outcome that spec exists to prevent. **Planitor** routes work packages by **discipline** (its WBS/OBS). Two fields; both consumers are **blind** without them. **(b) THE CLEAN DELTA PACKAGE BECOMES ONE VERSIONED JSON SCHEMA, CI-VALIDATED IN EVERY REPO.** ⚠ **It is currently a hand-maintained duplicate, and it says so out loud:** *"The two share one identical contract… **Any change to it must be made in both files**"* (`Planitor/v2.2_spec.md` §4). Adding Bunyan makes it **three**. **This is the exact drift class that has bitten this project three times already** (`capabilities` drifting from the handler map for two sessions; the docs describing a shape nobody had cut). **A contract maintained by REMEMBERING to edit N files WILL drift — and this one carries money and schedule.** The prose is **generated from the schema**, never maintained beside it, and each repo validates its producer/consumer **in CI**. *This is D21's own rule ("generated, never maintained") applied across repos.* |
| **D37** | Bunyan's roadmap promises an *"optional backend: multi-tenant accounts, project sharing/permissions, real-time presence"* (§13, v1.1.0+). **BIMsync is a BIM collaboration platform.** Are we about to build a second one? *(**2026-07-13** — **OWNER RULING; ⚠ SEE THE CAVEAT**)* | **BUNYAN DOES NOT BUILD A BACKEND. BIMsync IS THE ECOSYSTEM'S COLLABORATION LAYER** — hosting, sharing, permissions, versioning, presence — **for Bunyan models AND foreign ones.** Bunyan stays **client-only and serverless**, which is its identity (D11, §7: *"no backend"*), and the v1.1.0+ backend row is **struck**. Building a second multi-tenant platform — a second auth system, a second permission model, a second presence layer, a second place a user's models live — **would be the most expensive duplication in the portfolio.** **⚠⚠ CAVEAT, AND IT IS LOAD-BEARING: `BIMsync_cloude` IS NOT ON THIS BOX AND ITS SPEC HAS NOT BEEN READ.** Everything known about BIMsync here is **inferred from `Planitor/v2.2_spec.md`** (which names it as the producer of the Clean Delta and cites `BIMsync_cloude/v1.1_spec_planitor_compatibility.md`). ⇒ **This ruling is a DIRECTION and an INTERFACE OBLIGATION, not a description of BIMsync.** **Nothing may be committed *to* BIMsync, and no BIMsync doc may be edited, until its spec is read.** What is safe to act on now: **do not start building a Bunyan backend.** |
| **D38** | The project format was called `.bimproj`. Keep it, or brand it? *(**2026-07-13** — **OWNER RULING**)* | **RENAMED TO `.bnn`.** ⚠ **Free now, expensive later:** the format has **never been written to disk** (no document model exists yet), so there are **zero files to migrate** — it was a rename of docs plus three code *comments*. **Why:** `.bimproj` reads like a **category**, not a product — an odd name for the thing that is now the **ecosystem's native currency** (D34). Authoring formats are conventionally brand-opaque (`.rvt`, `.pln`, `.skp`, `.3dm`, `.dwg`), and `.bnn` is Bunyan's. **Lowercase**, because extensions are, and a case-sensitive filesystem would eventually punish `.Bnn`. **Unchanged by the rename:** everything about *what it is* — a zip of documented, versioned, migratable JSON, with the **parametric recipe as truth** (§2). **IFC stays the door to the outside world; `.bnn` is what actually holds a Bunyan model**, because IFC cannot hold a recipe, a `SubShapeRef`, or a stable id. |
| **D39** | ⚠ **AMENDED BY D42:** the *“warn first” half of this ruling is no longer a `planDelete()` query — it is `execute('core.deleteElement', args, { dryRun: true })`, whose would-be `UndoableEdit` **already lists the cascade**. The ruling stands; only its mechanism is now universal. |
| **D39 (original)** | **What happens when you delete a wall that hosts a window?** P3's exit criteria demanded a *"defined, tested outcome"* — and **no document said which outcome.** *(**2026-07-13** — **OWNER RULING**)* | **CASCADE-DELETE THE DEPENDENTS, WARN FIRST, IN ONE UNDOABLE EDIT.** Deleting a wall deletes the windows hosted in it; **undo restores all of them**, which is why it must be one edit and not three (an undo that restored the wall and left its windows deleted would be a data-loss bug wearing an undo button). **Why not "orphan them and mark broken":** **domain rule 3** ("a broken reference is a first-class state awaiting **manual retargeting**, never auto-healed") plainly governs a reference whose **sub-shape** vanished — the face a window sat on disappearing when its host is re-authored. It does **not** settle the case where the **host itself** is deleted, and reading it as though it did would make **"broken" the normal state of the document after any routine delete**, which drains the rule of its meaning. An **Opening is *defined by* its host** — a window floating in space is not a thing, and its geometry is a boolean against a solid that no longer exists. Revit and ArchiCAD both cascade. **Why not "refuse the delete":** it is the behaviour users hate most, and it makes every delete a multi-step transaction — while **transaction grouping is explicitly NOT built in v1.0.0** (D23). ⚠ **The warning is the caller's job, the cascade is the command's:** `planDelete(id)` is a **query** that reports exactly what will go, so a UI can say *"3 elements will also be deleted"* and **an agent can look before it leaps** — through the same door, with no second API (D19). **Built and tested, Entry 18.** |
| **D40** | **The Clean Delta's `change_type` is "READ from the `UndoableEdit` log, never inferred" (D34, domain rule 14) — but WHAT log?** *(**2026-07-13** — **OWNER RULING**; found by review, not by reading)* | **THE CHANGE FEED IS AN APPEND-ONLY *JOURNAL*, AND IT IS NOT THE UNDO STACK.** ⚠ **The spec contradicted itself and the code built the wrong half.** §6.1 called `history.json` *"optional, session-continuity only, never the source of truth"*; §7a leaned the entire moat on it. What shipped was therefore a **200-deep undo stack that `undo()` pops from, with NO anchor to a Model Revision** ⇒ ***"what changed since revision N?" had no answer in the file***, and a producer would have had to **diff two models** — the precise guessing BIMsync exists to do for foreign models and that Bunyan's whole argument says it never has to. **⇒ TWO STRUCTURES:** the **undo stack** (bounded; an undo pops it; a session convenience) and the **JOURNAL** (append-only, **never trimmed**, a monotonic **`seq`** on every edit, an undone edit journalled as a **REVERSAL** — never erased, because a consumer may already hold the state being reversed *from*). A **Model Revision** records **`issued_at_seq`**, so the delta is `journal.filter(e => e.seq > revN.issued_at_seq)` — **read, not inferred, BY CONSTRUCTION.** ⚠ **Free today** (the Clean Delta schema is not yet agreed — D36b — and no `.bnn` has ever been issued); **a three-repo amendment + a migration of every issued file tomorrow.** *Honest caveat: `seq` is document-scoped, which suffices for single-user v1.0.0; co-editing will need a merge-ordered journal — not solved, but not foreclosed (PEIs are already globally unique, D44).* |
| **D41** | Is **issuing a revision** an action on the Document, or a file operation? *(**2026-07-13** — **OWNER RULING**)* | **IT IS A COMMAND — `core.issueRevision`** — and `DocumentContext` owns the current revision. It was a free function in the persistence codec, so **an agent could author a building but not release one**, and `listCommands()` never mentioned it. **Domain rule 9 admits no second path**, and issuing is *"a deliberate act"* by D34's own words. It also makes D40's `issued_at_seq` fall out for free: the document knows its own journal position. |
| **D42** | When an edit touches 400 elements and **one** refuses, what does "reject + keep last-good" (rule 4) mean — and how does an actor find out *before* committing? *(**2026-07-13** — **OWNER RULING**)* | **ALL-OR-NOTHING, BY CONSTRUCTION — PLUS A UNIVERSAL `dryRun` ON THE EXECUTOR.** ⚠ Rule 4 was **false for every multi-element edit**: the scene rolled back and **the geometry did not**, leaving a sibling nobody had edited standing at a **rejected** thickness and reporting **double its true volume** as an `exact` quantity (measured, review 2026-07-13). ⇒ The rebuild is **staged and committed atomically**: one refusal, nothing applied. **And every Command may be run with `{ dryRun: true }`** — it executes **for real** (staged state, real kernel, real solids) and **discards the result**, returning the `UndoableEdit` it *would* have produced, or the typed failure **naming the offender** (which a UI highlights in 3D and an agent reads). ⚠ **Deliberately NOT a cheap semantic-only check** — a preflight that cannot promise the booleans succeed promises nothing. ⚠ **And it replaces per-command preview queries: `planDelete()` is DELETED.** A hand-written `plan…()` beside every verb is a second description of one behaviour, and **domain rule 10 forbids those** (D21: *generated, never maintained*). One executor, one code path, one set of failure modes — **and the P5 API surface stays lean**: `dryRun` lives on the **executor's options**, never on the `Command` contract, so commands stay passive and know nothing of transactional state. |
| **D43** | A `.bnn` carries an element whose **Type is not registered**, or whose **`typeVersion` is NEWER than this app's**. What happens? *(**2026-07-13** — **OWNER RULING**)* | **THE DOCUMENT OPENS. THE ELEMENT IS `failed`, VISIBLE, NAMED — AND PRESERVED VERBATIM THROUGH SAVE.** It is never built, never edited, never a host; it can be deleted. ⚠ **Today `rebuildAll()` THROWS and the whole file is unopenable** (measured). Given **decoupled release clocks** (§4g: *"Miqdar may never require of Bunyan coupled release schedules"*), version skew is **guaranteed by design, not pathological** — a Miqdar `.bnn`, a script-registered type (a declared north-star), a colleague's plugin. And `core_logic.md` §3.5 already rules the analogous IFC case the other way (*unrecognised entities survive as geometry rather than blocking*): refusing would make Bunyan **stricter with its own family's files than with strangers'**. ⚠⚠ **THE SAVE HALF IS THE POINT:** dropping what we do not understand would mean *open a Miqdar file → save → **its columns are gone, and their PEIs with them.*** A round-trip must be **lossless for a file it only partly understands**. ⚠ **And an element from the future is NEVER built against an older schema** — that is a *wrong building* rather than a refused one, and the same rule covers a `scene.json` whose `schemaVersion` is from the future. |
| **D44** | What SHAPE is an element's `id` — the **PEI**, the currency three products bind to forever? *(**2026-07-13** — **OWNER RULING**)* | **A PREFIXED ULID — `wall-01J8Z3K7Q2…`**: a readable type prefix + a globally-unique, time-sortable suffix. ⚠ **This is domain rule 8, not taste.** A document-local counter is unique only *within one document*, so **two peers co-editing would both mint `wall-7`** — and making that safe needs a central id allocator, i.e. **a backend, which D37 forbids Bunyan to build**. A counter therefore **forecloses the real-time-co-editing north-star** (§13) and the merging/superposition of two models — and **re-minting a PEI later is "not a refactor; it is a breaking change to three other products"** (rule 13). A ULID needs **no counter, no allocator, no server**. ⚠ **And it makes id REUSE impossible by construction** — which the counter did not: it was rebuilt on load from the **surviving** ids, so deleting `wall-3`, saving and reopening handed its PEI to the next new element (measured, review 2026-07-13). *Still greppable; Miqdar and Planitor store it as an opaque string either way.* |
| **D45** | Elements were declared **Structural / Architectural / MEP** (D36). But an RC wall is structural **and its plaster is architectural** — and those are two **PARTS** of one element. Where does `discipline` live? *(**2026-07-13** — **OWNER RULING**)* | **ON THE PART. NOWHERE ELSE — NOT EVEN DERIVED ON THE ELEMENT.** ⚠ **An element is almost never of one discipline**, so declaring one on it is *inaccurate about exactly the elements that matter*. **And nothing needs it:** IFC has **no discipline attribute** on an `IfcWall`; **Miqdar filters on `loadBearing`** (and everything it returns is structural by definition — it would never have modelled a non-load-bearing partition in the first place); **Planitor routes by TRADE — the plasterer bills plaster.** ⇒ `discipline` is authored on the **Style's LAYER**, carried by **every Part** (required), and stamped from the Type's **`defaultDiscipline`** where there is no style — **never inferred from the material** (a concrete screed is not structural; a timber shear wall is). **`Classification` becomes `{ifcClass, loadBearing}`.** A discipline view is a **filter over PARTS**: *architectural* shows the ceiling and the wall's plaster skin, **not** the slab and **not** the RC core. **An Opening has no parts ⇒ no discipline** — a void is an *absence*, and its structural significance is a penetration in **the host's** structural part, read from the host's own B-Rep. ⚠ **It propagates into the Clean Delta: `discipline` is carried PER PART** (§7a) — *which is what makes "a task binds to the part it actually builds" true at all.* ⚠ **And a quantity that cannot be measured is OMITTED, never zeroed** (rule 15): an unresolvable Material yields exact volume and area and **no mass** — `"0 kg, basis: exact"` is a wrong number wearing the badge that says trust me. |
| **D46** | May two elements model the **same physical thing** in two disciplines (Revit-style federated superposition)? *(**2026-07-13** — **OWNER RULING; the superposition rules are PROVISIONAL**)* | **NO. ONE PHYSICAL THING IS ONE ELEMENT, WITH ONE PEI.** Multi-disciplinary content is its **PARTS** (D30/D45). ⇒ **a quantity can never double-count**, and a schedule binds to **one** id. **The rules, as ruled — ⚠ AND RECORDED AS PROVISIONAL ON PURPOSE, so nothing freezes against them:** **(1)** MEP never collides with another discipline; **(2)** Architectural and Structural may; **(3)** on a collision **STRUCTURAL always wins** — the duplicated column **is** structural, and only its finishes are architectural *(this is also the priority order for any forced reduction to a single discipline — but there must not be one: filter the parts)*. ⚠ **OPEN, and named so it is not mistaken for settled:** an MEP duct genuinely occupying a structural beam's space is almost certainly a **CLASH** (report and resolve — `distance` already answers it, §4.6.5) rather than a *collision of disciplines*. **Not yet ruled. Do not implement past this line.** |

---

## 15. Previously-Open Questions — Resolved

- **O1 — 2D constraint solving.** **Numeric-only** dimension entry ships in v1.0.0 (with profile validation); a real geometric constraint solver is a v1.0.x additive Command.
- **O2 — 2D view scope.** **Plan + one elevation** in v1.0.0 (Section 5.2, the elevation exercises a non-horizontal cut); further sections/elevations are additive View-registry entries.
- **O3 — autosave depth.** **Capped ring** of ~5 snapshots with `navigator.storage.persist()` (Section 7).
- **O4 — `SubShapeRef` encoding & broken-reference UX.** Functional / operation-DAG naming (Section 4.5, **no `TNaming`/OCAF**); broken references are **marked for manual retargeting**, never auto-reattached.
- **O5 — visual-regression backends.** Run against **both** WebGPU and WebGL2.

---

## 16. Companion documents

- **`core_logic.md`** — the domain model (entities, identity, parametric relationships, states) written to be durable across versions and aware of the long-term north-stars; the reference for *what the app is*.
- **`architecture.md`** — the full architecture reference (layers, worker protocol, registries, data flow, determinism, security) — the reference for *how it is built*.
- **`v1.0.0_imp_plan.md`** — the phased, dependency-sequenced build plan for this spec.
